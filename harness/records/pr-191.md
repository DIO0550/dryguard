# PR #191 場所に依存する綴りが比較に残ったら型シグネチャを測れないにする

- マージ日: 2026-09-13
- 関連 Issue: #180（切った Issue: #192）
- 差分規模: 11 ファイル / +374 -46

## ゴールと結果

**#180 のゴールを達成した。** Issue が「決めること」として挙げた 3 点に、実コーパスで測ってから答えを出している。

| ゴール | 結果 |
|---|---|
| 型名にならない綴りが綴りのまま比べられて出る偽陽性を止める | 止めた（`compare` の実測で `単一化可能` → `測れない`） |
| どこで見るか（比較に残る綴り全体 / `Spelled` に落ちた部分だけ） | **後者**。4 つの形がすべて `Spelled` に落ちることを実測で確かめた |
| 落とす先は「測れない」で合っているか | 合っている。`rules/architecture.md` の表に 4 行目を足した |
| どれくらい減るか | 測った（下記） |

### 実コーパスで測った効き（rxjs 7.8.1 の `src`、251 ファイル / 961 チャンク）

計測はプロダクトと同じコード経路に一時的なプローブを足して行い、計測後に revert した
（`pr-185.md` と同じ形）。**尋ねる相手を 2 通り取った。**

| | 名前を持つ全チャンク 517 | 候補ペアに現れるチャンク 301 |
|---|---|---|
| `Normalized`（変更前 → 変更後） | 321 → **316** | 176 → **174** |
| `SiteDependentSpelling` | **5**（1.6%） | **2**（1.1%） |

落ちた 5 件はすべて `typeof` の後ろの値の名前で、**誤検出は無かった**。

**`pr-185.md` が「尋ねた 517」と書いていたのは「名前を持つ全チャンク」だった**（候補ペアに
現れるチャンクは 301）。1 巡目に候補ペア側だけで測って記録と合わず、denominator の
取り違えと分かったので両方を出す形にした。

**`pr-186.md` の採用後 `Normalized` 320 と、今回 `7cda1d5` で測った変更前 321 が 1 件ずれる。
原因は特定できていない**（基点で測り直した数をそのまま使った）。`UntracedTypeName` の 40 は
記録と一致する。

## 指摘

**この PR に 6 件。** 内訳は **レビュー 3 件**（`006c2a7` に対する Codex 1 巡、すべて P2）と
**自己修正 3 件**。CI は**この branch の Rust ワークフロー 2 回とも `success`**
（run 387 / run 388）で、**CI 由来の指摘は無い**。

### 1. ペアへ畳むとき、環境を直しても消えない理由が捨てられていた

- 分類: `missing-signal`
- 出どころ: レビュー（Codex P2）
- 内容: `pipeline::type_signature_match_of` の doc は**並びの軸を明記している**
  （「環境を直しても変わらない理由を先に出す」「**チャンクの形そのものを理由にする枝を
  足すときは、先頭側に置く**」）。にもかかわらず、足した `SiteDependentSpelling` の枝を
  `UnopenedTypeName` の後ろ（末尾側）へ置いた。片方が `SiteDependentSpelling`、もう片方が
  `NoTypeThere` などのとき、**サーバやファイルを直しても消えない理由のほうが捨てられ**、
  `--explain` が直しても変わらない先を指していた
- 既存ルール: rules/architecture.md「取れなかったシグナルを既定値で埋めない」の
  「理由は落とさない」
- 次にどう防ぐか: 今回は記録のみ。**規約も、その関数の doc に書いた契約もあり、
  守れていなかった回。** 枝を足すとき、その `match` の doc が並びについて何を約束して
  いるかを読んでいなかった

### 2. `--explain` が出す直し先が、そのバリアントに含まれる一部の形で成り立たなかった

- 分類: `なし`
- 出どころ: レビュー（Codex P2）
- 内容: 文言を「測れない (… : **型に名前を付けて注釈を書く**)」としたが、`this` 型には
  この直し方が無い。`this` は非 static のクラス / インターフェースのメンバーにしか書けない
  ので型エイリアスへ移せず、クラス名に置き換えると部分型での `this` の振る舞いが変わる。
  `class Builder { chain(): this }` と `class Maker { chain(): this }` の比較で**実際に
  この文言が出る**ことを確かめた。**直せない相手に直せると言っていた**
- 既存ルール: なし。`AGENTS.md`「記録・PR 本文に数値や主張を書く前に」は宛先を
  **記録・PR 本文・Issue コメント・コミットメッセージ・レビュー返信・通知**と列挙しており、
  **ツール自身の出力（`--explain`）はそこに入っていない**。`missing-signal`（理由を落とす）
  でもない（理由は出ており、添えた直し先が一部の形で成り立たなかった）
- 次にどう防ぐか: 今回は記録のみ。**1 件目なので `AGENTS.md`「規約の更新」の
  「2 回以上」に届いていない。** 2 回目が出たら「`--explain` に直し先を書くときは、
  そのバリアントが含むすべての形で成り立つかを確かめる」を立てる候補

### 3. 欠けていない材料まで「測れない」に数えていた

- 分類: `missing-signal`
- 出どころ: レビュー（Codex P2）
- 内容: `function same(x: string): typeof x` の `x` は**そのシグネチャ自身の引数**なので
  指す先はシグネチャの中で決まるが、`SiteDependentSpelling` に倒している。
  `rules/architecture.md`「どこまでを「取れなかった」に数えるか」は
  「**そのシグナルの答えに効く材料が欠けたときだけ「取れなかった」にする**」と
  決めており、ここでは何も欠けていない。基点 `7cda1d5` では `単一化可能`（正しい答え）
  だったので、**この 1 件を落としている**（実測で再現）。`rules/naming.md` の
  `site-dependent spelling` の定義（「指す先が**書かれた場所で決まる**」）にも当たらない
- 既存ルール: rules/architecture.md「どこまでを「取れなかった」に数えるか」
- 次にどう防ぐか: 今回は記録のみ。**この PR では直さず #192 に切った。** 引数の名前は
  構造として読んだ時点で落ちており（`Parameter` が渡し方と型だけを持つ、正規化の設計）、
  束縛を引くには `names_only_types` に値の名前のスコープを持たせることになる。その判定は
  差し替える側（`is_site_independent`）とも共有していて**安全な倒れ方が逆**なので、
  片方だけ緩められるかを先に決める必要がある（`rules/coding.md`「同じ一覧を、安全な
  倒れ方が違う 2 箇所で使い回さない」）。倒れる向きは偽陰性で、rxjs で出た 5 件に
  この形は 0 件だった

### 4. コードの Why コメントに、実測で誤りと分かった主張を書いた

- 分類: `comment-mismatch`
- 出どころ: 自己修正（コミット前）
- 内容: 答えを出す順の理由として「`import("./local").T` は**型名としても数えられる**ので、
  後に置くと『型注釈を書くと辿れる』と答えることになる」と書いた。実際に
  `type_name_spans_of("import(\"./local\").Thing")` を見ると `Some([])` で、**型名の範囲は
  0 個**。4 つの形はどれも型名として数えられない。理由を「手数の多いほうを先に出す」へ
  書き換えた
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ。**Why に書く事実も、書く直前に一次情報から出し直す
  必要がある**（今回は書いた後に確かめて気づいた）

### 5. Issue に書いたレビューコメントの URL を推測で埋めた

- 分類: `unverified-claim`
- 出どころ: 自己修正
- 内容: Issue #192 を立てるとき、出どころのレビューコメント URL を
  `#discussion_r4001100000` と**存在しない ID で書いた**。`get_review_comments` で
  取り直すと実際は `r3998239582` で、本文を更新した。**指摘の本文は手元にあったのに、
  リンクだけ一次情報に当たらずに書いた**
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ。規約はあり、**「数値や主張」に URL が含まれると
  読んでいなかった**回

### 6. レビュー対応の push 後、PR 本文が古い文言を引用したままだった

- 分類: `unverified-claim`
- 出どころ: 自己修正
- 内容: 指摘 2 で `--explain` の文言を変えたのに、PR 本文の出力例が
  「測れない (… : 型に名前を付けて注釈を書く)」のままだった。テストの件数
  （lib 928 → 930）とテストの表も `006c2a7` のままだったので、`e7737b9` で取り直して
  更新した
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」
  （pr-129 指摘 9・pr-138 指摘 5・pr-185 指摘 3 と同じ「本文が古くなる族」）
- 次にどう防ぐか: 今回は記録のみ。規約はあり、**push の前ではなく後に気づいた**

## 手戻り

**計測の denominator を 1 度取り違えた。** 1 巡目は候補ペアに現れるチャンク（301）だけで
測り、`pr-185.md` の「尋ねた 517」と合わなかった。**517 は「名前を持つ全チャンク」**だと
分かったので、記録と比べられる側と `scan` の実際の経路の両方を出す形にした。
**捨てた作業はプローブの 1 回の実行分だけ。**

**実装は計画どおりで、外れた点は無い。** 1 点目（`Spelled` に落ちた部分だけ）・
2 点目（表に 4 行目）・3 点目（測ってから決める）は Issue #180 のコメントに書いた形のまま
入っている。

**レビュー対応で 1 巡した。** 指摘 3 件のうち 2 件を直し、1 件を #192 へ切り出した。
指摘 1 は**自分が編集した関数の doc に書かれた契約への違反**だったので、避けられた手戻り。

## うまくいったこと

- **測ってから決めた。** Issue が「`typeof` は TS では珍しくないので測ってから決めたい」と
  書いており、実際に測ると失うのは 1.6% だった。数字が無ければ「どれくらい減るか分からない
  から見送る」か「気にせず入れる」のどちらかになっていた
- **`pr-185.md` / `pr-186.md` の計測手順をそのまま踏襲できた。** プローブをプロダクトと
  同じコード経路へ一時的に足し、計測後に revert する形が記録に残っていたので、測り方を
  設計し直さずに済んだ
- **基点を worktree に checkout して before / after を自分で測った。** 最初 `origin/main` が
  古い（`abe740d`）まま worktree を作っており、`git fetch` して基点が `7cda1d5` だと
  分かってから測り直した。Issue 本文の「基点でも同じ結果」を引き写していたら気づかなかった
- **レビュー指摘 3 件とも、直す前にコードと実サーバで裏を取ってから受けた**
  （関数の doc・`class Builder { chain(): this }` の実際の出力・基点との比較）
- **落とした答えを黙って飲まなかった。** 指摘 3 は「偽陰性だから安全側」で終わらせず、
  再現を取って #192 に残し、限界を `Callable::names_only_types` の doc と
  `rules/naming.md` にも書いた

## 規約への反映

**`rules/` への追記はこの PR の本体に含まれる**（`rules/architecture.md` の表の 4 行目、
`rules/naming.md` の語彙 `site-dependent spelling`）。これは #180 のゴールそのもので、
**指摘を受けての介入ではない。**

**指摘 6 件に対する介入は無し。** 内訳は次のとおり。

- 指摘 1 / 3（`missing-signal`）・4（`comment-mismatch`）・5 / 6（`unverified-claim`）は
  **既存の規約がある族**で、規約の抜けではなく守れていなかった回
- 指摘 2（`なし`）は**規約の抜けの候補だが 1 件目**。`AGENTS.md`「規約の更新」は
  「ルールに書かれていない指摘が **2 回以上**」を介入の条件にしているので、材料を残すところ
  までにする

回数を数えて規約やフックへ手を入れるのは別の回（`harness/records/README.md`
「記録は溜まり始めている」）。この回では `count.sh` を実行していない。
