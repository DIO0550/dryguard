# PR #246 #48 層 3: .rs を編集したら fmt をかけ clippy の診断を返すフックを足す

- マージ日: 2026-09-26
- 関連 Issue: #48
- 差分規模: 6 ファイル / +254 -0（コミット 2 本・マージを除く）

## ゴールと結果

Issue #48 のゴール「`PostToolUse`（Edit|Write）で `.rs` を編集したら `rustfmt` をかけ、
`cargo clippy` の診断を返す」は達成した。

| Issue の要件 | 結果 |
|---|---|
| プラグインを有効化するか、リポジトリへ持ち込むかを先に決める | 判断待ちで一度止め、オーナーの回答で**持ち込む（案 B）**に決めた。`rust-hooks-plugin` はプラグイン単位でしか有効化できず、#59 が保留したスキルゲートも一緒に入るため |
| `.rs` の編集で fmt をかける | `.claude/hooks/post-edit-rust.sh` が、編集したファイルから上へ最も近い `Cargo.toml` のクレートに `cargo fmt` をかける |
| clippy の診断を返す | `cargo clippy --all-targets -- -D warnings` が落ちたら stderr + exit 2 で返す。fmt が落ちたら clippy は走らせない |
| ゲートではない | `.claude/hooks/README.md` とフックの冒頭に書いた（exit 2 は編集を取り消さない） |

**Issue とずれた点が 1 つある。** Issue は 2 つの移植元（d-market-rust `format-and-lint.sh` /
design-composer `post-edit-lint.sh`）を挙げていたが、実装したセッションのアクセス範囲外で
どちらも読めなかった。**移植ではなく新規に書いた**ことを Issue・PR 本文・README に書いた。
移植元との突き合わせはしていない。

Issue に無かった変更として、フックのテストを CI（`.github/workflows/rust.yml` の
`claude-hooks`）で走らせるようにした。

## 指摘

レビューコメント・レビューは 0 件（`get_review_comments` / `get_reviews` / `get_comments` で確かめた。
PR コメントは自分が書いたテストケースの表 1 件だけ）。CI は 3 つのチェックがすべて 1 回目で通った。
以下は自己修正で、Issue #48 のコメント（実装の経過）に PR の前に書き残したもの。

### 1. 計画コメントに、最新の main を見ずに「`.claude/hooks/README.md` は存在しない」と書いた

- 分類: `plan`
- 出どころ: 自己修正
- 内容: 計画を書いた時点のチェックアウトは #242 のマージ（cb09b10）で止まっており、
  その間に #244（#49）が `.claude/hooks/README.md` と CI の `claude-hooks` ジョブを入れていた。
  計画は「README を新規に作る」「CI に新しい置き場を作る」前提になっていた。実装前に
  `origin/main` へ揃えて気づき、README は節の追加・CI は既存ジョブへのステップ追加に変えた
- 既存ルール: なし（計画の前に基点を最新へ揃える、という項目は `AGENTS.md`「実装を始める前に」に無い）
- 次にどう防ぐか: 今回は記録のみ

## 手戻り

- 上の指摘 1 のとおり、README と CI の置き場を実装前に変えた
- テストの「整形した結果がファイルに書き戻されている」の比較式を、1 回目に走らせる前に
  `diff` で比べる形へ書き直した（走らせる前なので指摘には数えない）

## うまくいったこと

- **Issue が「先に決める」とした論点と、移植元が読めないことを見つけた時点で止め、**
  選択肢と推奨を Issue に書いて判断を仰いだ。回答（案 B）を受けてから計画・実装に入った
- **テストを先に書いて Red（フック不在で 9 件落ちる）を確かめてから本体を書いた**
- **fmt 失敗後の `exit 2` を消して、「clippy は走らせない」のテストが落ちることを確かめた**
- **実リポジトリの `src/lib.rs` でもフックを走らせ**、exit 0 で作業ツリーが変わらないことを確かめた

## 実行環境メモ

- `echo hook-canary` は deny されずに通った。`.claude/settings.json` の `PreToolUse` は
  `pre-push-check.sh` だけで canary を deny しないため、**canary が無いので層 3 を判定できなかった**
- 追加した `PostToolUse` はセッションの途中で配線したため、このセッションで発火したかは観測していない
- `core.hooksPath` は `harness/githooks` に配線されていた
- ルーティンが指定した `implementation-flow` スキルは、この実行環境では見つからなかった
  （pr-235 / pr-240 / pr-244 と同じ）。手順を手で踏んだ
- PR #245（`harness/records/pr-244.md` だけを変える PR）にはレビューコメント・レビュー・
  PR コメントが 0 件だった（`harness/records/README.md`「記録だけの PR には記録を作らない」の
  併記の対象は無い）

## 規約への反映

**無し。** 指摘 1 は `既存ルール: なし` の `plan` で、再発数は数えていない。集計と介入の判断は棚卸しの回が持つ
（`harness/records/README.md`「数えるのと、規約やフックへ手を入れるのは別の回に行う」）。
