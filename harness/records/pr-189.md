# PR #189 PR #186 のマージ後の記録を追記する

- マージ日: 2026-09-13
- 関連 Issue: #173
- 差分規模: 3 ファイル / +322 -6

## ゴールと結果

| ゴール | 結果 |
|---|---|
| PR #186 の回の評価を `harness/records/pr-186.md` に残す | 達成した |
| `harness/records/` 配下のみの変更で収める | **達成できなかった。** レビューで `rules/architecture.md` と `src/syntax/type_reference.rs` にも同じ事実誤りがあると分かり、3 ファイルに広がった |

**2 つ目のずれが、この PR 自身に記録が要る理由**になった。`harness/records/README.md`
「記録だけの PR には記録を作らない」の条件は「**`harness/records/` 配下だけを変える PR**」で、
範囲が広がった時点で外れている（指摘 10）。

## 指摘

レビュースレッド 8 本に対して 7 ブロック（指摘 6 に重複 1 本を併記）。自己修正が 3 件で、
合わせて 10 ブロック。

### 1. 指摘 12 の分類が違い、既存の記録を数え直していなかった

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex P1）
- 内容: `pr-186.md` で「`rules/` が実装に追随していない」形を `comment-mismatch` に寄せ、
  **「1 件目」と書いた**。`pr-155.md:85-98` に同じ形が `分類: なし` で既にあり、
  「どちらの射程にも入っていない」という理由の文言までほぼ同じだった。
  **既存の記録を数え直さずに件数を書いた**ため、`count.sh` から 2 件目の再発が見えなくなる
  ところだった
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 2. 自分が入れた回帰の数を 1 件少なく書いていた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex P2）
- 内容: 回帰を 3 件と書いたが、指摘 11 も回帰だった。**集める側の漏れ自体は `b8fcd88` の
  前からあった**が、その前は「尋ねていない」で弾く経路が無く、書かれた `Input` はそのまま
  正規化されていた。測れない側へ落ちるようになったのは PR #186 の変更なので、
  利用者から見える回帰は 4 件
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 3. ゴールの表だけを実際より良く書いていた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex P2）
- 内容: 「推論された型名の偽陽性を止める」を表で「止めた」と書いたが、**同じ記録の指摘 7 に
  「実測で再現し #187 に切った」と自分で書いていた**。残りがあることを知りながら要約だけ
  良くしていた形。「一部だけ止めた」に直した
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 4. テストの穴の種類を 1 つにまとめていた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex P2）
- 内容: 「3 件とも単体テストでは捕まらず統合テストで初めて見えた」と書いたが、
  **指摘 8 / 10 は覆い隠されていたのではなく単体の網が無かっただけ**で、PR #186 で足した
  `src/syntax/chunk.rs` の単体テストは LSP 無しでその漏れを落とす。`tracing_all` が
  覆い隠すのは `type_signature` の境界（指摘 6）だけ。
  **書き直しながら「統合テストで初めて見えた」自体も誤りだと気づいた** — 4 件とも
  自分のテストではなくレビューで見つかっている
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 5. コンストラクタの影響範囲を実装より広く書いていた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex P2）
- 内容: 「**クラスのコンストラクタが 1 つ残らず**測れない側へ落ちていた」と書いたが、
  `constructor(other: Box)` のように**クラス名が引数注釈にも現れる形は落ちない**。
  引数側で集まった `Box` に、綴りで引く `is_traced` が戻り値の `Box` も乗せるため
  （乗り方そのものは #187 の偽陽性）。「収集対象の注釈に同じクラス名が現れない
  コンストラクタ」へ狭めた
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 6. テストを足した件数を、直していない分まで広げていた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex P2。**同じ箇所・同じ内容が 2 本立ったので併記**）
- 内容: 指摘 2 の修正で回帰を 3 件 → 4 件に直したとき、**同じ段落の「そのうえでテストを
  足している」まで一緒に 4 件へ広げた**。テストを足したのは PR #186 で直した 3 件だけで、
  指摘 11 は #188 へ送ったので回帰テストは無い。
  **直した拍子に隣の主張を巻き込んだ**形
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

**別ブロックにしなかった理由**: 2 本は同じ箇所への同じ指摘なので、分けると
**同じ再発が 2 件に見えて `count.sh` の数が膨らむ**（`pr-186.md` の Copilot 分と同じ扱い）。

### 7. 同じ事実誤りが rustdoc に残っていた

- 分類: `comment-mismatch`
- 出どころ: レビュー（Codex P2）
- 内容: 指摘 5 を直すとき `grep -rn "1 つ残らず"` を `rules/` と
  `harness/records/pr-186.md` にしか掛けず、**`src/` を探索範囲から外していた**ので、
  `src/syntax/type_reference.rs:146` の rustdoc に 3 箇所目が残った。
  関数の doc が規約と食い違ったままだと、この収集器を次に触る人が広いほうの前提で判断しうる
- 既存ルール: `rules/coding.md`「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ

**「一次情報から出し直す」を、探索範囲そのものを確かめずにやっていた。**
範囲を狭く取ると grep は静かに 0 件を返すので、確かめたつもりになる。

### 8. レビュー返信の中で `pr-155.md` を誤って引用した

- 分類: `unverified-claim`
- 出どころ: 自己修正
- 内容: 指摘 1 への返信で `pr-155.md` を引用したとき、`unverified-claim` を
  **`unquantified-claim` と綴った**。記憶から引用しており、この綴りは
  `harness/records/` にも `src/` にも `rules/` にも存在しない。
  **分類の語彙は 1 件につき 1 つしか持てないので、綴りが違えば別の語に見える**
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」（**レビュー返信**が射程）
- 次にどう防ぐか: 今回は記録のみ

コメント内だけの誤りで記録ファイルは正しかったため、差分は出していない。

### 9. PR 本文で `.claude/settings.json` が「存在しない」と書いた

- 分類: `unverified-claim`
- 出どころ: 自己修正
- 内容: 層 3 の確認を `test -f … && grep -c … || echo "no settings.json"` の 1 行で行い、
  **`||` が grep の exit 1（該当 0 件）で発火したのを、ファイルの不在と読み違えた**。
  実際にはファイルは存在し（841 バイト）、`PreToolUse` の配線が 0 件。
  **複合コマンドの出力を、どの節が出したか確かめずに読んだ**形
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

**層 3 の結論自体は変わらない**（canary が無いので判定できない）。誤っていたのは根拠の側。

### 10. PR 本文の「この PR 自身の記録は作りません」が、範囲の拡大で古くなった

- 分類: `unverified-claim`
- 出どころ: 自己修正（マージ後）
- 内容: 起票時は `harness/records/` 配下のみだったので
  `harness/records/README.md`「記録だけの PR には記録を作らない」に当たっていた。
  だが 2 巡目で `rules/`、3 巡目で `src/` へ広がった時点で、例外の条件
  「**`harness/records/` 配下だけを変える PR**」から外れている。
  **本文を 2 度書き直しながら、この 1 行だけ条件に照らし直していなかった**
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
  （**書いた時点では正しかった主張が、後から入った変更で古くなる**）
- 次にどう防ぐか: 今回は記録のみ

この記録が存在すること自体がこの指摘への対応にあたる。

## 手戻り

**同じ 1 つの事実誤りを 3 箇所に書いており、直すのに 2 回の push が要った。**

| 箇所 | 直した巡 |
|---|---|
| `harness/records/pr-186.md`（この PR で追加） | 2 巡目 `4327183` |
| `rules/architecture.md:132`（PR #186 でマージ済み） | 2 巡目 `4327183` |
| `src/syntax/type_reference.rs:146` の rustdoc（同上） | 3 巡目 `469b700` |

**行番号は直す直前の木で数えた**（規約は `bee7e1d`、rustdoc は `4327183`）。
どちらも置換が同じ行から始まるので**マージ後の木でも同じ行**だが、
`pr-185.md` 指摘 1 が「どの木で数えたか」を落として指摘されているので明記する。

2 巡目で 3 箇所目を取りこぼした原因は、grep の探索範囲から `src/` を外していたこと（指摘 7）。

**記録だけのつもりの PR が `src/` に触ることになった。** 3 箇所のうち 1 箇所だけ広い主張を
残すほうが害が大きいと判断して同じ PR に入れたが、**その判断の結果としてこの記録が要る**
（指摘 10）。分けていれば連鎖は止まっていた一方、食い違いを抱えた期間ができていた。

## うまくいったこと

- **指摘を鵜呑みにせず、実装から確かめてから直した。** 指摘 5 は使い捨ての単体テストで
  `class Box { take(other: Box) {} }` の `type_names_of` が `["Box"]` を返すことを確認し、
  メソッドに `constructed_class_references_of` が何も足さないことを既存テストで裏取りしてから
  範囲を狭めた（確認後にプローブは戻した）
- **自分の誤りを 3 件、レビューより先に自分で見つけて出した**（指摘 8 / 9 / 10）。
  うち 2 件は誰も指摘していない
- **直しながら、指摘されていない誤りにも気づいた**（指摘 4 の「統合テストで初めて見えた」）
- CI は 4 回（run 380〜383）すべて success で、CI 由来の指摘は無い

## 規約への反映

**無し。**

`unverified-claim` が 9 件・`comment-mismatch` が 1 件で、`unverified-claim` は
`AGENTS.md`「記録・PR 本文に数値や主張を書く前に」として**既に規約がある**分類。
新しい判断基準がレビューで示されたわけではないので、`rules/` へ足すものは無い。

**数えるのと、規約やフックへ手を入れるのは別の回に行う**
（`harness/records/README.md`）。この回は材料を残すところまでにする。
`対策済` の行も書かない — 何も介入していないので、書くと再発数が 0 に戻る。

## 実行環境メモ

- 層 3（Claude Code hooks）は**判定できなかった**。`echo hook-canary` は deny されずに
  通ったが、`.claude/settings.json` に `PreToolUse` の配線が無い（`grep -c` → `0`）。
  canary が無いので「フックが発火しない環境」とは断定できない
- 層 2（`harness/githooks/pre-push`）は push のたびに実際に走った
- 検査は 1 つずつ独立に実行した。`cargo test` の件数は 3 巡とも同一
  （919 + 22 + 12 passed / 0 failed）で、**実行されるコードが変わっていないことの裏取り**
  になっている
