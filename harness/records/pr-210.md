# PR #210 ✨ 共用体の相手の並べ替えを試して、正規化の最小を採る (#195)

- マージ日: 2026-09-18
- 関連 Issue: #195
- 差分規模: 2 ファイル / +509 -29

## ゴールと結果

Issue #195 のゴールは「`syntax::type_structure` の正規化を、共用体の相手の書かれた並びに
依存しない形にする（案 C: 並べ替えの探索）」。**達成した。** PR #194 が限界として固定していた
特性テスト 2 本は、同じ構造になることを見る形へ書き換わっている。

Issue が「決めること」として残した 3 点も、この回で決めて実装に入れた。

| 決めること | 結論 |
|---|---|
| 組み合わせの上限 | 120（= 5!）。数えるのは**番号を割り当てる相手だけ** |
| 上限を超えたらどちらへ倒すか | 書かれた並びのまま返す（今と同じ答え・偽陰性のまま） |
| 計算量の性質が変わることを受け入れるか | 受け入れる |

ずれは無い。スコープ外（`Spelled` に落ちた共用体の並び・交差型の並び）にも手を入れていない。

**この回の PR 本文には `差分規模:` の行が無い。** 層 1 の突き合わせ
（`.github/workflows/pr-body.yml` / `harness/ci/pr-body-diff.sh`）を入れた PR #208 が
main へ入ったのは `2026-09-18T23:51:15+09:00`、この PR を出したのは `22:30 JST` で、
**PR を出した時点の main にその検査も AGENTS.md の項目も無かった**（`git log` の
コミット日時と、この PR に走った check run が `fmt / clippy / test` の 1 つだけだったことによる）。
規約違反として数えない。次の回からは本文に置く。

## 指摘

**0 件。** レビュー 0 件・レビュースレッド 0 件・PR へのコメント 0 件
（`pull_request_read` の `get_reviews` / `get_review_comments` / `get_comments` が
いずれも空で返った）。CI（`fmt / clippy / test`）は 1 回目で緑。

**clippy の `manual_map` は指摘として立てていない。** 下の「手戻り」に書いたとおり
push の前に層 2（`harness/githooks/pre-push`）が落としており、**マージされた結果に
規約違反は残っていない**。`rules/coding.md`「前提として守るもの」の
「`cargo clippy --all-targets -- -D warnings` の警告をすべて解消する」は満たしている。
`分類: なし` を付けると「既存の規約に対応が無い（= 規約の抜けの候補）」を意味してしまい、
**対応する規約はあり、しかもツールが落としている**という事実と食い違う。

## 手戻り

**1 回。** `TypeParameter::reordered` の `constraint` / `default` と
`TypeStructure::reordered` の `narrowed` を `match` で書いたところ、
`cargo clippy --all-targets -- -D warnings` が `manual_map` で 3 箇所落とした。
`match` にしたのは可変借用（`orderings` と `scopes`）が重なるのを避けるためだったが、
いずれも文が分かれているので `Option::map` で通った。

**設計の手戻りは無い。** 探索の形（構造の側を先に並べ替える・最小はシグネチャ全体で採る・
辿るのは常に元の構造の書かれた順）は実装前に決めたものがそのまま入っている。

## うまくいったこと

- **局所の最小では閉じないことを、実装前に紙の上で当てた。** `<V, A>(acc: V | A) => A` は
  どちらの順で辿っても並べ替えた後の相手の並びが同じ（`[%0, %1]`）になり、差が戻り値に出る。
  ここで「最小はシグネチャ全体で採る」が決まった
- **入れ子の共用体に配る並べ替えがずれる形も、実装前に見つけた。** 並べ替えた順で辿ると
  外側の共用体の並びで対応が変わるので、**元の構造を書かれた順に辿る**形にした。
  回帰テストは `test_a_union_inside_a_reordered_member_is_reordered_with_its_own_orderings`
- **新しい assert を 4 通りの壊し方で確かめた**（`rules/testing.md`
  「迷ったら、その 1 件だけ実装を壊して走らせる」）。探索をやめる → 5 本、
  上限を 1000 にする → 1 本、並べ替えの要る相手を絞らない → 1 本、交差型も並べ替える → 1 本
- **Issue 本文の主張を一次情報で確かめ直した。** 「`scan` は候補ペアごとに呼ぶ」は
  実装と食い違っており、実際は `asked_scan_semantics_of` が**尋ねるチャンクごとに 1 回**
  （`src/pipeline.rs:1519-1529`）。計算量の重さの見積もりがこれで変わった
  （AGENTS.md「実装を始める前に」の「計画の入力を鵜呑みにしない」）
- **上限の重さを推測ではなく実測で書いた。** `cargo test --release` に一時プローブを当てて
  1 本あたりを 1000 回の平均で測り（コミットしていない）、3.86us（共用体なし）/
  6.26us（実コーパスの形）/ 436us（上限ちょうど）/ 4.80us（打ち切り）を PR 本文と
  Issue に載せた

## 規約への反映

**無し。** 指摘が 0 件で、`count.sh` が数える再発が 1 件も無い。
`rules/naming.md` の `type structure` の節は同じ PR で出し直しているが、これは
AGENTS.md「実装を変えたら、rules/ 配下の記述も同じ PR で出し直す」に従った**実装に伴う更新**で、
指摘を受けての介入ではない（`対策済` の行は書かない）。

## 実行環境のメモ

`echo hook-canary` は deny されずに通った。ただし `.claude/settings.json` の `hooks` は
`SessionStart` の 1 つだけで `PreToolUse` の配線が無いので、
**canary が無いため層 3（Claude Code hooks）を判定できていない**。
フック不発環境だとは断定しない。

push 前の検査は `harness/githooks/pre-push`（fmt / clippy / test）を 1 つずつ独立に実行し、
いずれも終了コード 0。LSP サーバを要するテストはこのコンテナに入っていないため
ローカルでは 46 + 4 が ignored だが、**CI では 46 passed / 0 failed / 0 ignored** で走っている。
