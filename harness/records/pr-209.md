# PR #209 👷 [CI]: #153 PR 本文が古くなる形への介入を層 1 へ上げ、分類を claim-staleness に分ける

- マージ日: 2026-09-18
- 関連 Issue: #153（`Closes #153` で自動クローズ。`closed_by_pull_requests` が PR #209 を指している）
- 差分規模: 6 ファイル / +378 -8（コミット 3 本・マージを除く。`git diff --numstat e2744ec...8aca31a`）

## ゴールと結果

Issue #153 のゴールは「**介入するかどうかと、どの層に置くか**を判断する」。
判断して入れるところまで済んだ。**ずれは無い。**

Issue が「先に確かめる必要がある」と書いていた「過去の回に層 4 の介入が入っていたか」は、
**入っていた**（PR #145 / 2026-09-04 マージ。`git log -S"後から入った変更で古くなる" -- AGENTS.md`
→ `c4c5fc2`、節の新設が `3edb1fa`）。Issue の起票は 2026-09-05 で、**前日には入っていた**。
`count.sh` では答えられなかった（`unverified-claim` に `対策済` の行が 1 件も無く「未介入」と出る）。

介入後の再発は **9 件 / 記録 6 本**（`pr-146.md` 以降を数えた。pr-169 指摘 93・pr-172 指摘 5 / 7・
pr-185 指摘 3・pr-189 指摘 10・pr-191 指摘 6・pr-194 指摘 5 / 6 / 8）。
`AGENTS.md`「同じ層で再発したら層を 1 つ上げる」に従い、層 2 は push の時点で PR 本文が
存在せず原理的に使えないので、**層 1（CI）へ上げた**（Issue の案 C）。
突き合わせるのは**差分規模だけ**で、残り 6 件は層 4 に残してある。

## この記録が数える件数

**レビュー由来の指摘は 0 件。** PR #209 にレビュー・レビューコメント・コメントはいずれも
付いていない（GitHub API の `get_reviews` / `get_review_comments` / `get_comments` が
3 つとも空）。下の 4 件はすべて **CI と自己修正**から来ている。

**PR #208 からの併記は無い。** PR #208 は `harness/records/pr-207.md` だけを変える PR で
（`git show --stat c24bcbd` が 1 ファイル）、`harness/records/README.md`「記録だけの PR には
記録を作らない」により次に実装が入る PR の記録へ併記する対象にあたるが、
**その回にも指摘が 1 件も付いていない**（同じ 3 つの API が空）。併記するものが無い。

## 指摘

### 1. 入れたばかりの検査が、この PR 自身の本文で落ちた

- 分類: `over-guard`
- 出どころ: CI（`差分規模の宣言` / [run 35350485923](https://github.com/DIO0550/dryguard/actions/runs/35350485923)、head `5022e97`）
- 内容: 宣言と読む行を「`差分規模` と数の並びの両方を持つ行」と決めていたため、
  **本文が書式そのものを説明している箇所**まで宣言として数えた。落ちた時点で拾われていたのは
  4 行（本文の宣言・コードブロックに再掲した同じ宣言・テストケースの表の 2 行）。
  正しい本文を**検査のほうが誤って止めた**形。`8aca31a` で、宣言と読むのを
  **行の頭が `差分規模:` で数の並びも持つ行**に限り、同じ数の重複は通すよう直した。
  **ローカルのテストは 8 件とも通っていた**（本番の入力で初めて壊れた）
- 既存ルール: rules/coding.md「列挙で判定を組むときは、漏れの倒れる向きを選ぶ」の
  隣にある形だが、**分類は `over-guard`**（過剰なブロック）を採った。
  `enum-omission-direction` は「一覧に無い形がどちらへ倒れるか」を問うもので、
  ここで壊れたのは**一覧に無い形ではなく、一覧が広すぎて正しい入力を止めたこと**。
  1 件の指摘は `分類:` を 1 つしか持てないので重ねない（`AGENTS.md`）
- 次にどう防ぐか: 今回は記録のみ。**直した形はテストに固定してある**
  （「表のセルの中の例は宣言にしない」「同じ宣言が 2 行あっても通る」）

### 2. 検査のテストの assert が終了コードだけを見ていた

- 分類: `test-assert-weak`
- 出どころ: 自己修正（push 前、変異を当てたときに検出）
- 内容: 1 巡目の 8 件は `exit 1` かどうかだけを見ていた。**落ちる理由が違っても終了コードは
  1 で揃う**ので、実装を壊しても素通りする。実際に当てた 5 通りのうち **2 通り**
  （「宣言が 2 行でも通す」「語だけの行も宣言にする」）が**落ちなかった**。
  出力の文言まで見る形へ直し、最終的な 10 件に対して 6 通りの変異を当てて
  **6 通りとも落ちる**ことを確かめた
- 既存ルール: rules/testing.md「assert は『落ちうるか』で見る」「迷ったら、その 1 件だけ
  実装を壊して走らせる」
- 次にどう防ぐか: 今回は記録のみ。**規約どおりに変異を当てたから見つかった**回でもある

### 3. push の後に本文を直す順序で、本文が 1 度古くなった

- 分類: `claim-staleness`
- 出どころ: CI（`差分規模の宣言` / [run 35350860870](https://github.com/DIO0550/dryguard/actions/runs/35350860870)、head `8aca31a`）
- 内容: 指摘 1 の修正を push した時点で、本文の宣言は push 前の
  `6 ファイル / +338 -8` のままだった（実際は `+378 -8`）。**この PR が入れた検査が、
  この PR の本文でその形を捕まえた**。本文を直すと `edited` で
  [run 35350993702](https://github.com/DIO0550/dryguard/actions/runs/35350993702) が走り直し、
  success になっている
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」の
  「出し直す時点を『push の後』に置かない」（**この PR が足した項目そのもの**）
- 次にどう防ぐか: 今回は記録のみ。**層 1 が捕まえて緑に戻るまでの往復が実際に回った**ので、
  効いているかどうかは次の回以降の記録で見る

### 4. 修正コミットを `git commit --amend` で誤って別のコミットに載せた

- 分類: `なし`
- 出どころ: 自己修正（push 前）
- 内容: コミットを「層 1 の検査」と「層 4 の規約」の 2 本に分けた後、スクリプトの
  出力書式を直す修正を `git commit --amend` で入れた。`--amend` が対象にするのは HEAD
  （= 後ろの規約側）なので、**検査のコミットに入れるはずの修正が規約のコミットに載った**。
  `git reset --soft HEAD~2` で 2 本を作り直し、メッセージは `git log --format=%B` で
  退避して同じものを使った。**push 前に直しているので履歴には残っていない**
- 既存ルール: なし。分類の語彙に「コミットの分け方」に当たるものが無い
  （d-market-git の「1 コミット = 1 変更」はプラグイン側の規約で、`分類` の表には無い）
- 次にどう防ぐか: 今回は記録のみ

## 手戻り

**2 件。どちらも上の指摘 1 / 4 と同じもの。**

| 何 | 捨てた作業 |
|---|---|
| 検査の設計（指摘 1） | 1 巡目のテスト 8 件のうち 2 件を書き直し、2 件を足した（8 件 → 10 件） |
| コミットの積み直し（指摘 4） | コミット 2 本の作り直し。差分そのものは失っていない |

**計画からのずれは無い。** Issue #153 のコメントに書いた計画（層 1 の検査・層 4 の残り・
`claim-staleness` の 3 つを 1 つの PR に入れる）のまま入っている。
指摘 1 の修正で**宣言の書式が「ちょうど 1 行」から「行の頭に置く」へ変わった**が、
これは計画の変更ではなく、計画の中で決めていなかった細部が本番で決まった形。

## うまくいったこと

- **Issue の問い（「層 4 の介入が入っていたか」）を `count.sh` ではなく `git log -S` で
  辿った。** `count.sh` には PR #145 の起点（`対策済` の行）が無く「未介入」と出るので、
  数だけ見ていたら**介入が無かったことにして案 A を採っていた**
- **再発を通算ではなく「介入以降」で数えた**（`AGENTS.md`「規約の更新」）。
  数える範囲を `pr-146.md` 以降に切ったうえで、9 件を 1 件ずつ記録から読んだ
- **覆える範囲を測ってから層を決めた。** 9 件を「機械が一次情報から出し直せるか」で割ると
  差分規模の 3 件だけだったので、**層 1 はそこに絞り、残り 6 件は層 4 に残した**。
  覆えないものを覆ったことにしていない
- **変異を当ててから push した**（rules/testing.md）。当てなければ、
  終了コードだけを見る 2 件の弱い assert に気づけていなかった
- **ワークフローを分けたので、本文の編集で `cargo test` が走っていない。**
  `rust.yml` の run は 2 本（`5022e97` / `8aca31a` の push ぶん）で、
  `edited` の回には走っていない（`list_workflow_runs` で確認）

## 規約への反映

**この回は介入した回にあたる。** 置いた場所は 3 つ。

| 置いたもの | 層 |
|---|---|
| `.github/workflows/pr-body.yml` + `harness/ci/pr-body-diff.sh` / `pr-body-diff-test.sh` / `README.md` | 1（CI） |
| `AGENTS.md`「出し直す時点を『push の後』に置かない」と、層 1 が覆う範囲 | 4（rules） |
| `harness/records/TEMPLATE.md` の分類の語彙 `claim-staleness` | — |

**それより上の層を採らなかった理由**: 層 1 がこの序列の最上位
（`AGENTS.md`「強制力の序列」）。層 2（git hooks）は push の時点で PR 本文が存在せず、
**この 1 件については層 1 のほうが早く効く**。

**分類を割ったのは `対策済` を置けるようにするため。** `対策済: unverified-claim 層=hook` と
書くと `count.sh` の再発数が分類全体で 0 に戻り、**介入していない半分（書く時点で
確かめていない形）まで対策済みに見える**。割った結果、`unverified-claim` の側の
再発数は動かない。

**この回の指摘のうち、`over-guard`（指摘 1）と `test-assert-weak`（指摘 2）は
既存の規約がある族**で、規約の抜けではなく守れていなかった回。
**数えるのと規約へ手を入れるのは別の回**（`harness/records/README.md`）なので、
ここでは材料を残すに留める。`分類: なし` の指摘 4 も同じ。

**この回の介入（`count.sh` が数える行）:**

- 対策済: `claim-staleness` 層=hook at pr-209

**`unverified-claim` の側には書かない**（そちらには介入していない）。

## 実行環境メモ

- 層 3（Claude Code hooks）は**判定できなかった**。`echo hook-canary` は deny されずに
  通ったが、`.claude/settings.json` に `PreToolUse` の配線が無い（`grep -c PreToolUse` → `0`。
  `hooks` は `SessionStart` の `wire-githooks.sh` 1 つだけ）。**canary が無いので
  「フックが発火しない環境」とは断定していない**（`AGENTS.md`「実装を始める前に」）
- 層 2（`harness/githooks/pre-push`）は配線済みで走った
  （`git config core.hooksPath` → `harness/githooks`、明示実行でも exit 0）
- 検査は 1 つずつ独立に実行した。`cargo test` は 3 巡とも同一
  （973 passed / 0 failed / 6 ignored（lib）、22 passed（compare）、
  12 passed / 4 ignored（scan）、46 ignored（semantics、LSP サーバ無し））で、
  **Rust のコードを 1 行も変えていない**ことの裏取りになっている
- CI は `rust.yml` が 2 本とも success、`pr-body.yml` が 3 本のうち 2 本 failure
  （指摘 1 / 3）・最後の 1 本が success
