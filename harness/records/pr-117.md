# PR #117 ✨ [Phase 2] hover で型シグネチャを取り、正規化して単一化の可否を判定する (#27)

- マージ日: 2026-08-30
- 関連 Issue: #27
- 差分規模: 10 ファイル / +1981 -19（コミット 14 本・マージを除く）

## ゴールと結果

Issue #27 のゴールは「LSP の `hover` で型シグネチャを取り、正規化して**単一化可能かを判定できる**
ようにする」。**達成した。**

チャンクの名前の位置（`Chunk::name_position`）→ `Session::hover` → 綴りの取り出し
（`lsp::hover`）→ 正規化と比較（`semantics::type_signature`）まで通り、実サーバを使った
統合テスト（`tests/semantics.rs`）で端から端まで動くことを確かめている。

**Stage 3 への統合は含まない**（#29 の担当）。この PR が出すのは、判定が受け取るシグナルの
作り手まで。ゴールにこれは入っていないので、達成の評価は変わらない。

**実装中に 4 つの Issue へ分けた。** どれも「独立してマージできるか」で切っている。

| Issue | 何を渡したか | 分けた理由 |
|---|---|---|
| #116 | 型エイリアス・記号の同一性の解決 | 追加の LSP 問い合わせが要る。綴りの解析だけでは閉じない |
| #118 | 綴りではなく構造で比較する | 型そのものの構文解析が要る。7 件の指摘が同じ根から来ていた |
| #119 | オーバーロードの集合の比較 | hover が `(+1 overload)` としか返さない |
| #120 | シグナルが付かないチャンクの形 | `chunk` の定義（`rules/naming.md`）を触る判断が要る |

## 記録だけの PR の指摘の併記

`harness/records/README.md`「記録だけの PR には記録を作らない」により、
**#113 / #114 / #115 の指摘をここへ併記する**（1 段ずれるので出どころを書く）。

**併記も 1 件 1 ブロックで書く。** `count.sh` は `^- 分類:` の行だけを数えるので、
表にまとめると**この 13 件が数える側から見えなくなる**。

### A1. 16 件を 1 つの形に畳んだが、表の ID は 10 件分しかなかった

- 分類: `なし`
- 出どころ: レビュー（Codex P2・PR #113）
- 内容: `pr-110.md` の 16 件を「確かめずに数を書いた」1 つの形として要約したが、
  指摘 7（塊の切り方）/ 14（層の記述）/ 15（却下）は別の原因。**未対応のままマージされた**
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### A2. 「修正が作った誤り」の列挙が 4 件 +「一部」だった

- 分類: `なし`
- 出どころ: レビュー（Codex P2・PR #113）
- 内容: 実際は 9 / 12 / 13 / 14 / 16 の 5 件。「一部」が何を指すか辿れない。**未対応のままマージされた**
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### A3. 「初版に 15 件」と書いたが、初版に付いたのは 5 件だった

- 分類: `なし`
- 出どころ: レビュー（Codex P2・PR #113）
- 内容: 同じ記録が 5 → 2 → 6 → 2 の 4 巡に分けており、残り 10 件は修正後に出たもの。
  **欠陥がいつ存在したかを取り違えている。未対応のままマージされた**
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### A4. canary の連続本数に `pr-72` が入っていない

- 分類: `なし`
- 出どころ: レビュー（Codex P2・PR #113）
- 内容: `pr-72.md` も pr-74 以降と同じ但し書き付きで通過を記録している。**未対応のままマージされた。**
  **この記録の数え方に直接効くので、下の「実行環境」で両方の基準を書いた**
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### B1. 例外を `README.md` にだけ書いた

- 分類: `なし`
- 出どころ: レビュー（Codex P2・PR #114）
- 内容: 「記録だけの PR には記録を作らない」を README にだけ置いたので、`AGENTS.md` しか
  読まない側は例外に気付かず記録を作り続けられる。**止めたつもりの連鎖が止まらない。**
  `AGENTS.md` に「例外があること」と持ち場だけを足して対応済み（条件は再掲していない）
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### B2. 「15 件はどれも同じ形」と書いた

- 分類: `なし`
- 出どころ: レビュー（Codex P2・PR #114）
- 内容: `pr-110.md` 自身が 4 つの形に分けている。**1 つに畳むとその形の証拠を実際より
  多く見せ、次の介入の選び方を誤らせる。** 対応済み
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### C1. `なし` の通算を確かめずに「10 件に届かない」と書いた

- 分類: `なし`
- 出どころ: レビュー（Codex P2・PR #115）
- 内容: 既存の記録に 34 件あり、その回の 4 件を足すと 38 件で昇格条件をとうに超えていた。
  `grep -c` で数え直して対応済み
- 既存ルール: `harness/records/README.md`「推測で埋めない」
- 次にどう防ぐか: 今回は記録のみ

### C2. 配線されていない土台を「ゴールを達成した」と書いた

- 分類: `なし`
- 出どころ: レビュー（Codex P2・PR #115）
- 内容: `Session::open_document` はテストからしか呼ばれておらず、`compare` / `scan` の経路に
  現れない。「土台までで、ゴールそのものには届いていない」に直して対応済み
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### C3. canary の連続本数を、間に入る記録を飛ばして数えた

- 分類: `なし`
- 出どころ: レビュー（Codex P2・PR #115）
- 内容: pr-105 / pr-107 / pr-110 を飛ばしていた。**A4 と同じ対象で、指摘の 2 回目。**
  数え直したところ**指摘より 1 つ多い**ことが分かり、そこまで含めて対応済み
- 既存ルール: `harness/records/README.md`「推測で埋めない」
- 次にどう防ぐか: 今回は記録のみ

### C4. テスト系分類の通算を確かめずに「2 回に届かない」と書いた

- 分類: `なし`
- 出どころ: レビュー（Codex P2・PR #115）
- 内容: `count.sh` は `test-assert-weak` 4 / `test-coverage-gap` 3 を返しており、
  どちらも `対策済` の行が無い。**C1 と同じ形。** 走らせて対応済み
- 既存ルール: `harness/records/README.md`「推測で埋めない」
- 次にどう防ぐか: 今回は記録のみ

### C5. 対策の違う 2 件を同じ分類の再発として数えた

- 分類: `なし`
- 出どころ: レビュー（Codex P2・PR #115）
- 内容: 見出し行の打ち間違い（対策は見出しの検査）と、2 件の修正を 1 コミットに入れたこと
  （対策は分ける単位を先に決めること）。**片方の対策がもう片方を防がない。** 対応済み
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### C6. Linux で検査できる件を「この OS では検査できない」に混ぜた

- 分類: `なし`
- 出どころ: レビュー（Codex P2・PR #115）
- 内容: symlink の件は `std::os::unix::fs::symlink` で再現もテストもできる。
  **同じ括りに入れると、棚卸しが「Windows で走らせる場所を用意する」へ寄って何も直らない。**
  軸を 4 つに分け直して対応済み
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### C7. `対策済` の行が無いことを「介入が無い」と書いた

- 分類: `なし`
- 出どころ: レビュー（Codex P2・PR #115）
- 内容: `plan` への介入は PR #77（層=rules）で既に入っていた。`git log -S` で辿って対応済み。
  **行が無い理由は `pr-77.md` が存在しないこと**で、書く場所そのものが無い
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

**13 件のうち 11 件が「記録自身の正確さ」。** `pr-110.md` が挙げ、`pr-111.md` が
軸として立てた形が、そのまま続いている。**A4 と C3 は同じ対象（canary の数え方）で、
2 回出ている。**

## 指摘

**自動レビュー（Codex）21 件 / リポジトリオーナー 6 件。**
オーナーのコメントは 7 つだが、うち 2 つ（「これは Position が持つべき関数じゃない？」
「それか SourcePosition。」）は**同じスレッドの同じ 1 件**なので、指摘としては 6 件。
Codex は 6 巡（5 → 8 → 3 → 3 → 1 → 1 件）。**CI 由来 0 件**（6 回すべて success）。

行き先の内訳（合計 27 件 = 下のブロック 1〜27）:

| 行き先 | 件数 |
|---|---|
| この PR で修正 | 8（Codex）+ 6（オーナー） |
| Issue へ分離 | 12 |
| 却下（再現しなかった） | 1 |

### 1. 文字列リテラル型の中の空白を畳んでいた

- 分類: `なし`
- 出どころ: レビュー（Codex P2・1 巡目）
- 内容: `split_whitespace()` で一律に畳んでいたので、`"a  b"` と `"a b"` が同じ形になった。
  **別の型を同じと答える側（偽陽性）。** サーバに投げ直して空白 2 つがそのまま返ることを
  確かめ、引用符の中を素通しにした（`f6c0f61`）
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### 2. 型変数の既定の型を落としていた

- 分類: `なし`
- 出どころ: レビュー（Codex P2・1 巡目）
- 内容: `<T = string>` の `= string` を捨てていたので、型引数を省いて呼ぶと戻り値の型が
  違う 2 つが同じ形になった。**偽陽性。** 既定を先に切り離してから制約を読む形にした
  （`T extends X = D` は両方を持つので、順序が逆だと制約が `X = D` になる）（`da15f2f`）
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### 3. 共用体の順序を正規化していない

- 分類: `なし`
- 出どころ: レビュー（Codex P2・1 巡目）
- 内容: `string | number` と `number | string` が別物になる（偽陰性）。
  **素直に「比較の前に並べ替える」を入れると別の偽陰性が生まれる**ことを示して #118 へ送った。
  付け替え前の綴りで整列すると `A | Middle` と `Z | Middle` が別物になり、順序を逆にすると
  並べ替えが構造を見ずに関数型を切る
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ（→ #118）

### 4. 代入で名付けられた関数の名前を見つけていない

- 分類: `なし`
- 出どころ: レビュー（Codex P2・1 巡目）
- 内容: `obj.handler = (…) => …` は親に `name` も `key` も無く、名前の位置が `None` に
  なっていた。**hover を送れるのに送らない**（偽陰性）。tree-sitter と実サーバの両方で
  確かめ、**左辺の先頭ではなくプロパティ**を指す形にした（先頭へ送ると入れ物の型が返る）（`3a678c7`）
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### 5. 同じ綴りで別の記号を指す型を見分けられない

- 分類: `なし`
- 出どころ: レビュー（Codex P2・1 巡目）
- 内容: `billing/User` と `report/User` が同じ `User` として重なる。**偽陽性で、しかも
  `docs/dryguard-plan.md` の判定表（`DO-NOT-EXTRACT` =「構造は似ているが依存先・呼び出し元の
  ドメインが別。偶発的重複」）を真っ向から外す。** hover が型を解決した形で返さないことを
  実測で確かめ、追加の問い合わせが要るものとして #116 へ送った
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ（→ #116）

### 6. 型変数の付け替えがプロパティ名や文字列リテラルまで書き換える

- 分類: `なし`
- 出どころ: レビュー（Codex P2・2 巡目）
- 内容: 文字単位の置換なので `{ T: string; value: T }` の `T:` まで書き換わる。**偽陽性。**
  #118 の表で唯一の偽陽性として優先度を明記した
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ（→ #118）

### 7. `this` 引数の印を落としていた

- 分類: `なし`
- 出どころ: レビュー（Codex P2・2 巡目）
- 内容: `this` は呼び出し時に渡さない引数なので、名前ごと落とすと受け取る値が 1 つの関数と
  2 つの関数が同じ形になった。**偽陽性。** `this:` の印を残す形にした（`a08ab30`）
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### 8. 型述語に残った引数名を付け替えない

- 分類: `なし`
- 出どころ: レビュー（Codex P2・2 巡目）
- 内容: `(value: unknown): value is string` の `value` が戻り値の型に残る（偽陰性）。#118 へ
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ（→ #118）

### 9. エスケープされた引用符でリテラルを閉じていた

- 分類: `なし`
- 出どころ: レビュー（Codex P2・2 巡目）
- 内容: **指摘 1 の修正（`f6c0f61`）が作った欠陥。** 引用符の追跡にエスケープの扱いが
  無かったので、`"a\"  b"` の途中で閉じて以降の空白が畳まれた。**畳む側だけでなく
  深さを数える側にも同じ穴があり**、そちらは引数リストの区切りを取り違えるのでより広く効く。
  同じ判断を 2 箇所に持たせていたのが原因なので `QuoteState` に寄せた（`21bc25c`）
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### 10. `const` 型変数修飾子を名前として読む

- 分類: `なし`
- 出どころ: レビュー（Codex P2・2 巡目）
- 内容: `<const T>` の最初の空白区切りを名前にするので `const` が名前になる（偽陰性）。#118 へ
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ（→ #118）

### 11. オーバーロードの集合を比べていない

- 分類: `なし`
- 出どころ: レビュー（Codex P2・2 巡目）
- 内容: hover は `(+1 overload)` としか返さず、隠れたシグネチャが比べられない（偽陽性）。
  追加の問い合わせが要るので #119 へ
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ（→ #119）

### 12. ラベル付きタプルのラベルを落とさない

- 分類: `なし`
- 出どころ: レビュー（Codex P2・2 巡目）
- 内容: `[left: string, right: number]` と `[text: string, count: number]` が別物になる（偽陰性）。#118 へ
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ（→ #118）

### 13. 入れ子の関数型が持つ型変数を付け替えない

- 分類: `なし`
- 出どころ: レビュー（Codex P2・2 巡目）
- 内容: コールバック自身が宣言する型変数のスコープを見ていない（偽陰性）。#118 へ
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ（→ #118）

### 14. アクセサの hover 形を扱っていない

- 分類: `なし`
- 出どころ: レビュー（Codex P2・3 巡目）
- 内容: getter / setter も `method_definition` なのでチャンクになるが、hover は
  `(getter) Holder.value: string` の形で返し、引数リストが無い。実測で確かめたうえで
  **この PR では決めず #120 へ送った** — 綴りを関数の形へ直す案は偽陽性を作り
  （`get value(): string` と `value(): string` は呼び出し側から見て別物）、
  語彙から外す案は `chunk` の定義を触る
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ（→ #120）

### 15. capability を確かめずに hover を送っていた

- 分類: `comment-mismatch`
- 出どころ: レビュー（Codex P2・3 巡目）
- 内容: `Session` の doc は「サーバができることを知らずに問い合わせを組み立てる形を作らない」
  と書いているのに、`hover` が `self.capabilities` を参照していなかった。**doc と実装が
  食い違っていた。** 判定式は `#[ignore]` の実サーバテストにだけ書いてあり、実装側に無かった
  ものを関数へ引き上げた（`a414144`）
- 既存ルール: `rules/coding.md`「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ

### 16. 読めない応答と答えが無いを混ぜていた

- 分類: `missing-signal`
- 出どころ: レビュー（Codex P2・3 巡目）
- 内容: 返り値が `Option` だったので「hover を提供していない」「その位置に答えが無い」
  「読めない綴りの形」が同じ `None` になっていた。**この PR 自身が
  `rules/architecture.md`「取れなかったシグナルを既定値で埋めない」に反していた。**
  `HoverOutcome`（4 バリアント）に分けた（`a414144`）
- 既存ルール: `rules/architecture.md`「取れなかったシグナルを既定値で埋めない」
- 次にどう防ぐか: 今回は記録のみ

### 17. 透過的な式に包まれた関数の名前を見つけていない

- 分類: `なし`
- 出どころ: レビュー（Codex P2・4 巡目）
- 内容: `const format = ((…) => …)` は親が `parenthesized_expression` / `as_expression` /
  `satisfies_expression` で止まる（偽陰性）。tree-sitter で再現し、サーバが答えられることも
  確かめたうえで #120 へ送った
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ（→ #120）

### 18. コンストラクタの呼び出しの仕方を落としていた

- 分類: `なし`
- 出どころ: レビュー（Codex P2・4 巡目）
- 内容: **指摘が引用した綴りは実際と違った**（`(constructor) Result.constructor(…)` ではなく
  `constructor Result(value: string): Result`）が、**引き起こす問題は指摘のとおり**だった。
  `new` が要る型と要らない型が同じ形になる（偽陽性）。`SignatureKind` を足した（`9dedfc1`）
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### 19. callable object の hover が読めない

- 分類: `なし`
- 出どころ: レビュー（Codex P2・4 巡目）
- 内容: **この指摘は採らなかった。** 前提（「TypeScript hovers the variable in that same
  object form」）が再現しない。実測では呼び出しシグネチャが 1 つだけの callable object は
  値形へ畳まれて返るので、括弧は深さ 0 にあり読める。**前提が成立しないので、この PR でも
  Issue でも扱わない**と書いて閉じた
- 既存ルール: `harness/records/README.md`「推測で埋めない」（**却下の根拠として働いた**）
- 次にどう防ぐか: 今回は記録のみ

### 20. 括弧・共用体に包まれた関数型の引数名が残る

- 分類: `なし`
- 出どころ: レビュー（Codex P2・5 巡目）
- 内容: `cb: ((reason: string) => void) | null` は外側の括弧組が引数リストとして選ばれず、
  中の `reason` が残る（偽陰性）。**これは `normalized_type` の doc が既に限界として
  書いていたものと同じ根**で、包み方ごとに剥がす対応を足すと #118 の「構造で比べる」から
  遠ざかるので #118 へ送った
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ（→ #118）

### 21. 位置を LSP の形へ直す変換が自由関数だった

- 分類: `smart-constructor`
- 出どころ: レビュー（リポジトリオーナー）
- 内容: 「これは Position が持つべき関数じゃない？」「それか SourcePosition。」
  `lsp_position_of` を `SourcePosition::to_lsp_position()` にした（`76dfad0`）。
  `Position` は外部クレートの型で `impl` を書けないため、併記された方を採った。
  **自由関数のままだと、列の数え方を知らない場所でも `Position` を組み立てられる**
- 既存ルール: `rules/coding.md`「生成時に検証し、不正な値を存在させない」。
  **同じ規約が挙げている `LineNumber::from_index` / `to_index` の例と同じ形**で、
  「位置の変換に置き場所が無く、要る場所ごとに同じヘルパーが生える」ことを防ぐもの。
  規約の文は newtype を作るかどうかの判断軸として書かれているが、
  **判断軸そのもの（その型に置き場所が要る操作があるか）はこの件にそのまま当たる**
- 次にどう防ぐか: 今回は記録のみ

### 22. 呼び出しの種類を読む規則が自由関数だった

- 分類: `なし`
- 出どころ: レビュー（リポジトリオーナー）
- 内容: 「SignatureKind が持って欲しいかな」。`signature_kind_of` を
  `SignatureKind::from_prefix()` にした（`cde1742`）。読み取りの規則はその enum 自身の知識で、
  バリアントを増やすときに一緒に目に入る
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### 23. 割った結果を素のタプルで返していた

- 分類: `type-vocabulary`
- 出どころ: レビュー（リポジトリオーナー）
- 内容: 「変える値だけど、単純なタプルにしないで欲しいかも」。`(&str, &str, &str)` を
  `struct SplitSignature` にした（`cde1742`）。**3 つとも `&str` なので、順番を入れ替えても
  型は同じ**で、取り違えがコンパイルを通っていた
- 既存ルール: `rules/coding.md`「値の語彙を型で閉じる」（「取り違えても型エラーにならない」）
- 次にどう防ぐか: 今回は記録のみ

### 24. 走査の結果を素のタプルの `Vec` で持っていた

- 分類: `type-vocabulary`
- 出どころ: レビュー（リポジトリオーナー）
- 内容: 「これも、struct にしてドメインにできないか考えてみて」。`Vec<(usize, char, usize)>` を
  `SignatureScan` + `ScannedCharacter` にした（`cde1742`）。**1 番目と 3 番目がどちらも
  `usize`**（バイト位置と括弧の深さ）。深さを使う操作が 4 箇所に散っていたのも型に集まった
- 既存ルール: `rules/coding.md`「値の語彙を型で閉じる」
- 次にどう防ぐか: 今回は記録のみ

### 25. 戻り値の型の読み取りが自由関数だった

- 分類: `なし`
- 出どころ: レビュー（リポジトリオーナー）
- 内容: 「これも何かの概念じゃないかチェックして」。`SplitSignature::return_type()` にした
  （`cde1742`）。**あわせて概念にしない判断も 1 つ書いた** — 宣言形（`: `）と値形（`=> `）の
  区切りを値として持つ案は、**どちらであるかで分岐する呼び出し側が無い**ので採らなかった
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### 26. 引数の印を型の綴りに埋め込んでいた

- 分類: `type-vocabulary`
- 出どころ: レビュー（リポジトリオーナー）
- 内容: 「これもお願いいします」。引数 1 つを `?string` / `...string[]` / `this:HTMLElement` の
  ような印付き `String` で持っていたものを `Parameter` + `ParameterKind` にした（`cde1742`）。
  **これが指摘 7 で作った「`this:` で始まる型は書けないから衝突しない」という前提を要求していた。
  分けたことでその前提ごと消えた。** `bool` 2 つで持つ案は、成立しない組み合わせ
  （省略可かつ可変長）が表現できるので採らなかった
- 既存ルール: `rules/coding.md`「値の語彙を型で閉じる」/「不正な状態を型で表現できなくする」
- 次にどう防ぐか: 今回は記録のみ

### 27. 呼び出し可能なエイリアスで注釈すると解析に入れない

- 分類: `なし`
- 出どころ: レビュー（Codex P2・6 巡目）
- 内容: `const f: Handler = …` は hover が `const f: Handler` と返し、引数リストが無い（偽陰性）。
  実測で `type` も `interface` も名前のまま返り、インライン注釈だけが展開されることを確かめた。
  **#116 が既に挙げていた形より強い** — 従来の例は綴りが読めて比較が綴りに引っ張られる話だが、
  こちらは解析の入口にすら入れない。「エイリアスを開くのは解析の前か比較の前か」を
  #116 の「決めること」に足した
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ（→ #116）

## 手戻り

- **自分の修正が新しい欠陥を作ったのが 1 件**（指摘 9）。1 巡目の空白の修正で入れた引用符の
  追跡にエスケープの扱いが無かった。**同じ判断を 2 箇所に書いていたので、両方に同じ穴があった**
- **Codex が収束しなかった。** 1 巡目 5 件 → 3 件修正 → 2 巡目 8 件。**個別対応を止めて
  根のほうを Issue（#118）に立てた。** その後の 4 巡は 3 → 3 → 1 → 1 件と減っている
- **却下した指摘が 1 件**（指摘 19）。前提が再現しなかった
- **指摘の引用した綴りが実際と違ったものが 1 件**（指摘 18）。欠陥は実在したので直した
- **Issue へのリンクを確かめずに書いて誤ったのが 3 回。** #116 のコメント / #120 の本文 /
  マージ直前の #116 へのコメント。3 回目は訂正コメントを出した。
  **`pr-110.md` / `pr-111.md` が挙げた「確かめずに数を書いた」と同じ形が、
  数ではなく ID で出ている**
- **PR 本文のテスト表が初版のまま古くなっていた。** レビュー各巡で 17 件足したのに本文の
  「338 passed」が残っていた。**同じ数を 2 箇所に書いて片方だけ直す**（`pr-110.md` の指摘 12 / 13 / 16）
  と同じ形。マージ前に別コメントで最終状態を出した

## うまくいったこと

- **hover の綴りに関する主張を、毎回サーバに投げ直して確かめた。** これが指摘 19（前提が
  再現しない）を却下する根拠になり、指摘 18（引用した綴りが違う）でも実際の綴りを出せた。
  tree-sitter の主張も使い捨てのテストで確かめている
- **偽陽性と偽陰性で扱いを変え、その基準を公開した。** 修正した Codex の 8 件のうち
  **7 件が偽陽性側、1 件（指摘 4）だけが偽陰性側**。指摘 4 を直したのは、名前の位置が
  取れないと**その先の判定が丸ごと立たない**（シグナルが 1 つも付かない）ためで、
  比較の精度が落ちる他の偽陰性とは効き方が違う。**Issue へ送った 12 件のうち 3 件が偽陽性**
  （指摘 5 = #116 の記号同一性 / 指摘 6 = #118 の付け替えがプロパティ名まで書き換える /
  指摘 11 = #119 のオーバーロード）、残り 9 件が偽陰性側。
  **偽陽性を送った 3 件は、いずれも追加の LSP 問い合わせか型の構文解析が要る**ので、
  この PR の中で直せる形ではなかった（#118 の表では指摘 6 を唯一の偽陽性として
  優先度を明記してある）。
  **このツールでは「単一化できないものをできると答える」ほうが危ない**という判断軸を
  各スレッドに書いた
- **収束しないことを検知して止めた。** 個別の文字列処理を足し続けず、#118 に 7 件を
  1 つの表として収容し、なぜ 1 件ずつ直さないかを書いた
- **リファクタリングでテストを 1 件も変えなかった。** 773 行を入れ替えた `cde1742` で
  `git diff | grep 'fn test_'` が 0 件。**33 件が無変更のまま通ることが、振る舞いを
  保っている根拠**になる
- **疑わしい assert を 1 件だけ壊して確かめた。** `=>` の `>` で括弧を閉じないテストが
  実装を壊しても通ることに気付き、タプルの入力に差し替えた
  （`rules/testing.md`「迷ったら、その 1 件だけ実装を壊して走らせる」）
- **守っていないテストを 1 件消した。** 制約と既定の両方を持つ型変数のテストが
  ミューテーションで通ったので、テストごと消して実装も単純化した
- **CI は 6 回すべて success**（`c1653bd` / `3a678c7` / `a08ab30` / `a414144` / `9dedfc1` /
  `cde1742`）。**CI 修正の push は 0 回**

## 規約への反映

**無し。** ルーティンの指示により、この回は記録の追記のみ
（`対策済` の行は書かない。集計・介入判断は棚卸しの担当）。

**`count.sh` は走らせていない。** したがって**この記録を含む通算は書かない**。
最後に確かめられた数は `pr-111.md` の表（`なし` 38 / `plan` 20 / `test-assert-weak` 4 /
`test-coverage-gap` 3、いずれもその記録を含む）。

**この回が積んだ内訳（この記録の中だけを `grep -c '^- 分類: '` で数えたもの。合計 40 ブロック）:**

| 分類 | 件数 | 内訳 |
|---|---|---|
| `なし` | 34 | 本体 21 + 併記 13 |
| `type-vocabulary` | 3 | 本体（23 / 24 / 26） |
| `smart-constructor` | 1 | 本体（21） |
| `missing-signal` | 1 | 本体（16） |
| `comment-mismatch` | 1 | 本体（15） |

**併記の 13 件も 1 件 1 ブロックで書いた。** 表にまとめると `count.sh` が
`^- 分類:` の行だけを数えるため、**数える側から見えなくなる**。

**注記 1（`なし` の軸）**: 本体の 21 件は `pr-111.md` が立てた 4 軸のどれとも別で、
**ほぼすべてが「TypeScript の型の綴りをどこまで読めるか」という 1 つの領域**に属する。
うち 12 件は Issue へ送っており、**#116 / #118 / #119 / #120 が引き取った時点で
「規約の抜け」ではなく「未実装」に変わる。** 併記した 13 件のうち 11 件は
`pr-110.md` が立てた「記録自身の正確さ」の軸で、そちらは続いている。

**注記 2（オーナーのレビューが出した 3 つの形）**: 6 件は 3 つに割れる。

| 形 | 該当 | 既存の規約 |
|---|---|---|
| 位置の変換に置き場所が無い | 21 | **ある**（`rules/coding.md`「生成時に検証し、不正な値を存在させない」の `LineNumber` の例と同じ形） |
| 素のタプル・印付き文字列を型にする | 23 / 24 / 26 | **ある**（`rules/coding.md`「値の語彙を型で閉じる」/「不正な状態を型で表現できなくする」） |
| **既にある型に、その型の概念に属する操作を置く** | 22 / 25 | **無い** |

**抜けとして残るのは 22 / 25 の 2 件。** 21 との違いは、21 が「位置の変換」という
規約が例に挙げているものそのものなのに対し、22（接頭辞から呼び出しの種類を読む）と
25（引数リストの後ろから戻り値の型を読む）は**変換ではなく読み取りの規則**で、
かつ置き先の型（`SignatureKind` / `SplitSignature`）は既にある点。

**規約の判断軸（その型に置き場所が要る操作があるか）が、既にある型への操作の配置まで
及ぶかどうか**が決めどころ。及ぶなら 22 / 25 も既存の規約で拾えるので、
新しい語は要らない。**その判断は棚卸しの担当。**

**注記 3（PR #113 の 4 件が未対応のまま残っている）**: A1〜A4 は返信も resolve もされておらず、
**`pr-110.md` の記述は指摘された状態のまま**。過去の記録は書き換えない決まりなので、
ここに残す以上のことはしない。

## 実行環境の記録

- **`echo hook-canary` は deny されずに通った。** `.claude/settings.json` に `PreToolUse` は
  未配線なので、正しくは「**canary が無いので層 3 が効いているか判定できなかった**」（#50）。
  **数え方が 2 通りある:**
  - `pr-111.md` の基準（pr-74 から）では、`pr-111.md`「15 回連続」+ この回 = **16 本目**
  - **PR #113 の指摘 A4（未対応）に従い pr-72 から数えると 1 本増えて 17 本目。**
    `pr-72.md` は canary の通過を、pr-74 以降と同じ但し書き付きで記録している（自分で開いて確かめた）。
    **どちらの基準を採るかは棚卸しの担当**なので、両方を書いて判断は渡す
- **git hooks（層 2）は配線され、実際に走った。** push のたびにテストまで実行されている
- **CI（層 1）は 6 回すべて success。** 最終 head `cde1742`（46 秒）
- push 前検査は `cargo fmt --check` / `cargo clippy --all-targets -- -D warnings` /
  `cargo test` / `cargo test -- --ignored` を **1 つずつ独立に実行**。
  **`cargo test` はターゲットごとに結果行を出すので、合計で数える:**

  | ターゲット | passed | ignored |
  |---|---|---|
  | `src/lib.rs`（単体） | 355 | 4 |
  | `tests/compare.rs` | 14 | 0 |
  | `tests/scan.rs` | 9 | 0 |
  | `tests/semantics.rs` | 0 | 2 |
  | **合計** | **378** | **6** |

  `cargo test -- --ignored`（実サーバあり）で、その 6 件が **6 passed / 0 failed**。
  **全体で 0 failed。**
- **`#[ignore]` の実サーバテストを、実際に typescript-language-server を入れて走らせた**
  （`rules/testing.md`「LSP を要するテストは、飛ばしたことが分かる形にする」）
- **Discord 通知は送れた**（HTTP 204）。**この記録に書けるのは、レビュー修正の push 後に
  送った 1 回だけ** — それ以前の送信はこのセッションの現在の文脈から確かめられないため、
  回数は書かない（`README.md`「推測で埋めない」）
- **ブランチは PR #117 のマージで残っていたが、`origin/main` から立て直した**
  （マージ済みの履歴の上には積んでいない）
- **PR は作成の約 14 時間後にマージされた**（2026-08-29T10:46Z 作成 → 2026-08-30T00:45Z マージ）
