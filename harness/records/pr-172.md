# PR #172 値を通すだけの包みを抜けて、チャンクの名前の位置を取る

- マージ日: 2026-09-11
- 関連 Issue: #120
- 差分規模: 9 ファイル / +461 -32
  （`git diff --shortstat fadc795^1 fadc795`）

## ゴールと結果

Issue #120 のゴールは 2 つあり、**1 つを達成し、1 つは判断待ちとして分離した**。

| ゴール | 結果 |
|---|---|
| 括弧・`as`・`satisfies` に包まれた関数の名前の位置を取る | **達成**。`syntax::tree` に `TRANSPARENT_EXPRESSION_KINDS`（6 種別）と `transparent_wrappers_of` / `unwrapped_parent_of` を置き、`chunk::name_node_of` と、`type_reference` の `outer_annotated_nodes_of` / `wrapper_types_of` が**同じ包みを抜ける**（`annotated_nodes_of` は最終形ではチャンク自身のシグネチャだけを集める。範囲の分け方は `annotation_scopes_of` が持つ） |
| アクセサ（getter / setter）の型シグネチャを取る | **この PR では実装せず #170 へ分離**。印の置き場所が `chunk` の語彙か `type_structure` の語彙のどちらかを触るので、`AGENTS.md`「設計判断の確認」に当たる |

**Issue の一覧は鵜呑みにせず、`tree-sitter-typescript 0.23.2` の `node-types.json` から数え直した**
（`AGENTS.md`「実装を始める前に」の「計画の入力を鵜呑みにしない」）。

| Issue の主張 | 実測 |
|---|---|
| 透過的な包みは `parenthesized_expression` / `as_expression` / `satisfies_expression` / `non_null_expression` / `type_assertion` の 5 種別 | **6 種別だった。** `instantiation_expression` が漏れていた（`function` フィールドの型にアロー関数は含まれないが、`children` に `expression` があり、`(<T>(v: T): T => v)<string>` が実際にこの形で読まれる） |
| アクセサも包まれた式も「倒れる向きは取りこぼし（偽陰性）」 | **アクセサ側は成り立たない。** 関数型のアクセサ（`(getter) Holder.handler: (a: string) => void`）は `SplitSignature` がプロパティの型の括弧組を引数リストとして掴み、`Normalized` / `(string) => void` として通る（**偽陽性**）。#170 に記録 |

**Issue を分けた。** 判断軸は `AGENTS.md`「タスクの分割」の「独立してマージできるか」で、
2 つは片方だけ入っても壊れない。分けた理由と、分けた側のスコープ外は #120 のコメントと
#170 の本文に書いた。

**この PR から新しい Issue を 2 本出した。** #170（アクセサ）と #173（推論された型名）で、
どちらも `AGENTS.md`「設計判断の確認」に当たるため判断待ちにしてある。

## 指摘

**この PR に 10 件、併記が 42 件、合計 52 件。**

この PR の 10 件の内訳は**レビュー 4 件**（Codex が 4 巡: `9f52974` に 1 件（P1）、
`df37f71` に 1 件（P2）、`d335a9a` に 2 件（P1 / P2）、`44ae9ac` に 0 件）と、
**自己修正 6 件**。CI は**この branch の 4 回**（`9f52974` / `df37f71` / `d335a9a` /
`44ae9ac`）がどれも `success` で、**CI 由来の指摘は無い**。人のレビュアーからの
コメントは無かった。

**併記の 42 件は PR #171 の回のもの。** `main` の first-parent で #172 の直前は #171
（`1df9040`）で、差分が `harness/records/pr-169.md` の 1 ファイルだけの
**記録だけの PR** だった（`git diff --stat 1df9040^1 1df9040`）。
`harness/records/README.md`「記録だけの PR には記録を作らない」に従い、
**その回の指摘をここへ併記する**（下の「併記: PR #171 の回の指摘」）。

### 1. 名前を探す側だけ包みを抜け、型名を集める側を置き去りにした

- 分類: `なし`
- 出どころ: レビュー（Codex 1 巡目 P1）
- 内容: 最初のコミット（`055f077`）は `chunk::name_node_of` だけを包みの先へ進めた。
  `type_reference::annotated_nodes_of` は `node.parent()` のままで、**この 2 つが同じ位置を
  指していることが前提**だった（`annotated_nodes_of` の doc が `name_node_of` を参照している）。
  `const f = ((value: any): any => value) as (v: Shape) => Shape;` では hover が
  `const f: (v: Shape) => Shape` を返すのに `Shape` が解決の対象に入らず、**開かれない綴りが
  宣言の場所も付かないまま比較に残る**（別々のファイルの `Shape` が単一化可能 = 偽陽性）。
- 既存ルール: なし。`rules/architecture.md`「取れなかったシグナルを既定値で埋めない」は
  **取れなかったことをどう表すか**の話で、**共有している関数の意味を変えたときに、
  その意味に依存している別の場所を確かめる**という形には触れていない
- 次にどう防ぐか: 今回は記録のみ。**指摘 2 と合わせて 2 件が同じ形**（指摘 3 は
  形が違うので別に立てた。下の表 A）。語彙への昇格は棚卸しの担当

### 2. チャンクと外側で型変数の束縛が混ざった

- 分類: `なし`
- 出どころ: レビュー（Codex 2 巡目 P2）
- 内容: 指摘 1 の修正で包みの型を `annotated_nodes_of` へ足したが、
  **`bound_type_names_of` が自分で `annotated_nodes_of` を呼んでいた**ので、
  束縛を数える範囲も一緒に広がった。チャンクの型変数と包みの型変数が 1 つの集合に混ざり、
  **同じ綴りが両側にあると外側の宣言を指している側まで落ちる**。

  | 綴り | 直す前 | 直した後 |
  |---|---|---|
  | `(<Shape>(value: Shape): Shape => value)<Shape>` | `[]` | `["Shape"]` |
  | `((value: Shape): Shape => value) as <Shape>(v: Shape) => Shape` | `[]` | `["Shape"]` |

  **指摘は前者の向きだけを挙げていたが、実測すると後者も同じように落ちていた。**
  指摘の例をそのままテストにするだけでは片側しか塞がらなかった。
- 既存ルール: なし（指摘 1 と同じ形。**1 件目は呼び出し元、2 件目は呼び出し先**）
- 次にどう防ぐか: 今回は記録のみ

### 3. 外側どうし（包みの型と代入先の注釈）でも束縛が混ざった

- 分類: `なし`
- 出どころ: レビュー（Codex 3 巡目 P2）
- 内容: 指摘 2 の修正はチャンクと外側を 2 つの範囲に分けたが、**外側をまとめて 1 つの範囲に
  していた**。包みが言い切った型と代入先の注釈は互いに独立していて、片方の型変数が
  もう片方まで届かない。`const f: Handler = ((x: any) => x) as <Handler>(x: Handler) => Handler;`
  で代入先の `Handler` が落ちていた（`[]` → `["Handler"]`）。
- 既存ルール: なし。**指摘 1 / 2 とは形が違う**（あちらは変えた関数に依存している別の場所を
  確かめていない形、こちらは**自分で引いた境界が 1 段粗かった**形）。直し方も違うので、
  引き継ぎでは 1 件の別の行として立てた（下の表 A）
- 次にどう防ぐか: 今回は `annotation_scopes_of` を足し、**範囲の分け方を 1 箇所で持つ**形に
  変えた。呼ぶ側が注釈を並べる形だと**並べ方がそのまま範囲の切り方になる**のが、
  2 巡目と 3 巡目に共通する根だった。**これは分類への介入ではないので `対策済` は書かない**

### 4. 推論された型名が綴りのまま比べられる（この PR では直さず #173 へ）

- 分類: `plan`
- 出どころ: レビュー（Codex 3 巡目 P1）
- 内容: 戻り値の注釈を省いた関数は、hover の綴りに現れる型名が構文木のどこにも無い。
  尋ねる位置を作れないので綴りのまま比べられ、別のファイルの同じ綴りと重なる（偽陽性）。

  **仕組みはこの PR で入ったものではない**ことを確かめた。包みを一切使わない 2 ファイル
  （それぞれ構造の違う `class Result` を宣言し、どちらも
  `export const build = (x: number) => new Result();`）で `型シグネチャ: 単一化可能` が出る。
  **`origin/main`（`e1a8105`）を `git worktree` に checkout してビルドし、同じ 2 ファイルを
  比べたら出力が 1 文字も変わらなかった。**

  **ただし、この穴に載る入力はこの PR で増えている。** 直す前は包まれた無名の関数に
  名前の位置が付かず、`pipeline` が中立の `NoName` を返していた。包みを抜けるようにした
  ことで、**同じ関数が hover まで届き、この偽陽性を出しうるようになった**。
  仕組みは既存、**載る入力は新しく露出した**、が正確な言い方になる。

  **この指摘を追ったときに見つかった `type_reference` のモジュール doc の食い違いは、
  別の指摘として下に立てた**（指摘 10。remedy が別で、`分類` も別になる）。
- 既存ルール: `rules/architecture.md`「どこまでを「取れなかった」に数えるか」が
  **この形を射程に入れていない**。今の表は「**開けなかった**型名が比較に残る綴りに現れるか」で
  線を引いているが、ここで起きているのは**そもそも尋ねていない**型名が残る形で 3 つ目の区分
- 次にどう防ぐか: #173 で判断を仰ぐ。直すと**戻り値の注釈を省いた関数すべて**が
  「測れない」側へ動くので、先に実コーパスで影響を測ることを推しとして書いた

**分類を `plan` にした理由。** 迷った候補は `missing-signal`（取れなかったシグナルを既定値で
埋めない）だが、**規約がこの区分を持っていない**ことがこの指摘の中身なので、
既存の見出しに当てると「規約に反した」と読める。`なし` も候補だが、
**この回に誤ったのは計画の側**（包まれたチャンクを対象へ入れるとき、既存の穴に載る件数が
増えることを計画に書いていなかった）なので `plan` を採った。

### 5. PR 本文の変異の件数が、修正のあと古くなった（1 巡目）

- 分類: `unverified-claim`
- 出どころ: 自己修正（指摘 1 の修正後に検出）
- 内容: 1 巡目の PR 本文に「`TRANSPARENT_EXPRESSION_KINDS` を空にすると 13 件が落ちる」と
  書いたが、指摘 1 の修正で一覧が `wrapper_types_of` と `unwrapped_parent_of` からも
  引かれるようになり、**実測し直すと 19 件**だった。気づいて本文を更新した。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」の 2 つ目
  （「関連する変更が入った後・最終提出の前にも行う」）
- 次にどう防ぐか: 今回は記録のみ（規約は既に射程に入れている）

### 6. コミットメッセージに、走らせていない変異の結果を書きかけた

- 分類: `unverified-claim`
- 出どころ: 自己修正（push 前に検出）
- 内容: 指摘 1 の修正コミットに「追加した 6 件のうち 4 件が落ちる（残る 2 件は
  `unwrapped_parent_of` を `parent()` へ戻すと落ちる）」と書いたが、**走らせていなかった**。
  push 前に 3 種類の変異を実測したところ **4 件 / 1 件 / 1 件**で、残る 1 件は対照だった。
  amend で表に直した（`044a1cf`）。**リポジトリの履歴には誤った数は残っていない。**
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」（コミットメッセージが
  明示的に挙がっている）
- 次にどう防ぐか: 今回は記録のみ

### 7. PR 本文の変異の件数が、修正のあとまた古くなった（2 巡目）

- 分類: `unverified-claim`
- 出どころ: 自己修正（2 巡目の本文を書く前に検出）
- 内容: 指摘 2 の修正でも同じことが起きた。**書く前に 5 種類の変異を全部走らせ直し**、
  1 つ目の変異では落ちた 17 件が全部新規で既存が 1 件も落ちないことまで `comm` で
  突き合わせた。3 巡目でも同じ手順を踏み、6 種類で取り直している。
- 既存ルール: 指摘 5 と同じ
- 次にどう防ぐか: 今回は記録のみ。**指摘 5 / 6 / 7 で 3 回目**

### 8. Discord の通知を、確かめずに「未送信」と断定して二重投稿した

- 分類: `unverified-claim`
- 出どころ: 自己修正（再送時に検出）
- 内容: 通知を送ったあと `wc -m` が 2024 を返したのを見て、
  **「Discord の 2000 文字制限を超えたので送信されていない」と確かめずに断定し、
  同じ内容を送り直した**。実際には送信されており（再送時に 204 が返った）、
  **チャンネルに同じ内容が 2 通並んだ**。
  原因は送信ヘルパーが **API の応答を見ずに `curl` の終了コードだけで成否を出していた**こと
  （長すぎる本文は `{"content": ["Must be 2000 or fewer in length."]}` が返るが、
  `curl` は成功する）。応答が空（204）かどうかで判定する形に直した。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」。
  **`AGENTS.md`「強制力の序列」の「フェイルオープンかつサイレント」と同じ失敗の形**が
  通知の送信にもあった、という重なりもある
- 次にどう防ぐか: 今回は記録のみ。**通知の送信は使い捨てのシェル関数**なので、
  リポジトリに残る仕掛けにはなっていない（ルーティンの指示に書かれたコマンド例そのものが
  応答を見ない形）。仕掛けを置くかは棚卸しの担当

### 9. Issue #173 に、確かめていないレビューコメントの URL を書いた

- 分類: `unverified-claim`
- 出どころ: 自己修正（作成直後に検出）
- 内容: #173 の「参照」に `#discussion_r3988229175` と書いたが、**この ID は記憶から書いたもので
  実在しない**。実際は `r3988207740`。作成直後に GitHub から引き直して食い違いに気づき、
  本文を更新した。
- 既存ルール: 指摘 6 と同じ（Issue コメントが明示的に挙がっている）
- 次にどう防ぐか: 今回は記録のみ。**pr-166 の指摘 2 とまったく同じ形**
  （コミットメッセージに実在しない `#discussion_r...` を書いた）

### 10. `type_reference` のモジュール doc が、倒れる向きを実測と逆に書いていた

- 分類: `comment-mismatch`
- 出どころ: 自己修正（指摘 4 を追う途中で検出）
- 内容: `src/syntax/type_reference.rs` のモジュール doc は、推論された型名が解決できない
  制約を既に言語化しているが、**「倒れる向きは偽陰性で、その型名は綴りのまま比べられる」**と
  書いている。実測すると**偽陽性**（構造の違う 2 つの `Result` が単一化可能に出る）で、
  後段の文と前段の断定が食い違う。**この doc は `main` に入ったまま。**
- 既存ルール: `rules/coding.md`「コメントは doc と Why / Why not に絞る」
  （内容が実装・事実と食い違う）
- 次にどう防ぐか: 今回は記録のみ。**直す先は #173 の判断と同じファイル**なので、
  #173 の対象に含めてある（あちらで案 C を採るなら、この 1 文だけを直す形になる）

**指摘 4 と分けた理由。** どちらも同じ現象から見つかったが、**remedy が別**。
指摘 4 は `rules/architecture.md` の表に区分を足すかどうかで、こちらは**doc の 1 文を
事実に合わせる**だけで閉じる。`harness/records/README.md`「指摘 1 件 = 1 ブロック
（まとめない）」に従い、`分類` の行も別に立てた（畳むと `count.sh` が
`comment-mismatch` を数えない）。

## 併記: PR #171 の回の指摘

**PR #171 は `harness/records/pr-169.md` だけを足した記録だけの PR** なので、
`harness/records/README.md`「記録だけの PR には記録を作らない」に従い、その回の指摘を
ここへ併記する（**どの PR の回のものかを書く**、の適用）。

**42 件すべてがレビュー（Codex、すべて P2）で、対象はすべて `harness/records/pr-169.md`。**
人のレビュアーからのコメントは無く、CI 由来の指摘も無い。**Codex は 11 巡している**
（ボットの起点コメントの `created_at` を数えた: 09:47:42 の 6 件から 12:12:05 の 3 件まで。
6 / 3 / 3 / 4 / 2 / 5 / 3 / 2 / 4 / 7 / 3 で合計 42）。

**42 件を 1 件ずつ見て分類した。** 大半は**記録に書いた数・分類・引き継ぎの主張が、
記録自身の本文や一次情報と食い違っていた**形だが、**それだけではなかった**。
`TEMPLATE.md` の「`plan` など他の分類に当たる指摘はそちらを優先し、`unverified-claim` は
どれにも当たらない裏取り漏れに使う」に従って割り分けた。

| 分類 | 件数 | 何を指すか |
|---|---|---|
| `unverified-claim` | **36** | 書いた数・分類・主張が、記録自身の本文や一次情報と食い違っていた |
| `plan` | **4**（併記 5 / 6 / 16 / 35） | **remedy そのものが効かない形で書かれていた**（走らせれば分かる誤り・引き継ぎ先の選び違い） |
| `なし` | **2**（併記 23 / 28） | **記録の立て方**（1 件 = 1 ブロック）。規約は `README.md` に在るが、`TEMPLATE.md` の分類の語彙に対応する行が無い |

**記録の中身（実装の欠陥）についての指摘は 1 件も無い**（pr-110 の回と同じ性質）。

**39 件は返信のうえ resolve 済み、3 件（40 / 41 / 42）は返信も修正もされないまま
#171 がマージされた。** 未対応のまま `main` に入っているので、下に個別に立ててある。

### 併記 1. `rules/naming.md` を触っていないという記述が事実に反した

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 「この回は `rules/` に手を入れていない」と書いたが、pr-169 の差分は
  `rules/naming.md` の `import` / `specifier` の定義を広げていた。`e0c4bd3` で修正。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 2. 「checked-in の振る舞いはすべて正しい」が記録自身の未解決の欠陥と矛盾した

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 記録自身が指摘 85 / 61 / 76 で再現済みの欠陥を挙げているのに、結論では
  「すべて正しい」と書いていた。`e0c4bd3` で修正。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 3. 落とした指摘を引き取ると二重計上になる、という主張が事実でなかった

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 「引き取ると二重計上になる」と書いたが、当時の事実ではなかった。`e0c4bd3` で修正。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 4. 再発を読むときに規約の条文（規約の抜けであること）を読み違えた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: `AGENTS.md`「規約の更新」の「**ルールに書かれていない**指摘が 2 回以上」という条件を
  落として読んでいた。`e0c4bd3` で修正。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 5. 警告をエラー扱いにする指定を Cargo 側へ渡していた

- 分類: `plan`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 記録に書いた remedy のコマンドで、`-D warnings` を rustdoc ではなく Cargo に
  渡す形になっていた。`e0c4bd3` で修正（実際に走らせて確認）。
- 既存ルール: `rules/` にも `AGENTS.md` にも、**remedy が効くかを確かめてから書く**という決まりは無い
- 次にどう防ぐか: 今回は記録のみ

### 併記 6. フォーマッタを編集の前に走らせる remedy を書いていた

- 分類: `plan`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: `cargo fmt` を編集の**前**に走らせる形で書いていたが、防ぎたいのは編集**後**の
  未整形。`e0c4bd3` で修正。
- 既存ルール: 同上（併記 5 と同じ形）
- 次にどう防ぐか: 今回は記録のみ

### 併記 7. 再発の形を確かめずに「規約が覆っている」と断じた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 1 巡目の修正で逆方向へ振り過ぎ、覆われていない形まで「規約が覆っている」側へ
  寄せていた。`6827a68` で修正。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 8. 「全件を再現した」と「確かめた」を同じ文の中で混ぜた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 同じ文の中で「再現した」と「確かめた」が矛盾していた。`6827a68` で修正。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 9. 85 件という総数が、書いた内訳から導けなかった

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 指摘 22 が内訳から抜けており、85 が導けなかった。`6827a68` で修正。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 10. 3 分割の件数が、1 件を 2 箇所で数えていた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 指摘 65 を 2 行目と 3 行目の両方で数えていた。`5b78b59` で **19 / 3 / 40** へ修正。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 11. タグの合計を「同じ形の再発」として読ませていた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: `over-guard` の合計を 1 つの形の再発数として書いていたが、別の形が混ざっていた。
  `5b78b59` で修正。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 12. `TYPE_ONLY_KINDS` の辿り先に、触っていない 2 件が入っていた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 辿り先にその配列を触っていない 2 件が入り、実際に触った 2 件が抜けていた。
  `5b78b59` で修正。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 13. 無関係な `ModulePath` の指摘を同じ再発に数えていた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: `cf311e5` で **19 / 3 / 39 / 1** へ割り直した。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 14. 指摘の番号を、巡の番号として書いた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 最初の包みのリファクタが何巡目だったかを、指摘の番号で書いていた。`cf311e5` で修正。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 15. 効いた仕掛けが違う 2 件を、1 つの成功として並べた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 送信前の URL 検査と、API の応答での検証を 1 つにまとめていた。`cf311e5` で分けた。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 16. private な doc の置き場所を、この rustdoc のゲートで守れると書いた

- 分類: `plan`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: そのゲートでは守れないことを、使い捨てのクレートで実測して確かめ `cf311e5` で修正。
- 既存ルール: 同上（併記 5 と同じ形）
- 次にどう防ぐか: 今回は記録のみ

### 併記 17. 前の巡で分けた理由が当たる 1 件を、見落として同じ行に残した

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 指摘 39 を分けたときに、同じ理由が当たる指摘 15 を見落としていた。
  `c20ae5c` で 4 行目へ移した。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 18. 静的テンプレートの取りこぼしの分類が違っていた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: `c20ae5c` で `enum-omission-direction` へ直した。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 19. 層を上げる材料が「無い」と書いたが、直前に自分で作った行に 2 件あった

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 4 行目を作った直後に、その 2 件を見落として「材料が無い」と書いていた。
  `5b477d9` で撤回。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 20. 同じ巡で「remedy が違う」と書いた 1 件を、数えるほうでは同じ行に残した

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 指摘 42 について本文と数え上げが食い違っていた。`5b477d9` で分けた。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 21. 「狙ったテストだけが落ちた」が全件には当たらなかった

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 変異テストの成功を無条件に書いていたが、例外があった。`5b477d9` で例外を挙げた。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 22. 同じ記録の中で、分類と引き継ぎが食い違った

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 指摘 75 の分類が引き継ぎの表と合っていなかった。`5b477d9` で `plan` へ直した。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 23. 1 件 = 1 ブロックにせず、別の指摘へ畳んだ

- 分類: `なし`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 指摘 80 の再現を独立したブロックにしていなかった。**指摘 90 が指摘したのと
  まったく同じ形を、記録自身がやっていた。** `5b477d9` でブロックを立てた。
- 既存ルール: `harness/records/README.md`「指摘 1 件 = 1 ブロック（まとめない）」。**規約は在るが、`TEMPLATE.md` の分類の語彙に対応する行が無い**
- 次にどう防ぐか: 今回は記録のみ

### 併記 24. 例外を挙げるときに粒度を落として書いた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: テストされていない印のガードの指し方が曖昧だった。`5a24286` で言い直した。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 25. `unverified-claim` の件数が、修正のあと古くなった

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: **「関連する変更が入った後にも出し直す」を、まさにその自己申告の中でやり損ねていた。**
  `5a24286` で修正。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」の 2 つ目
- 次にどう防ぐか: 今回は記録のみ

### 併記 26. ブロックに書いた区別を、隣の節へ反映し忘れた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 指摘 80 を API 検証の成功から外す必要があった。`5a24286` で外した。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 27. 「適用の話だから層を上げる材料ではない」が誤りだった

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 規約は漏れが偽陰性へ倒れる向きを選べと決めているのに、18 件は偽陽性側へ倒れていた。
  `fe70b2b` で材料に入れた。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 28. フォーマッタの失敗を 1 ブロックに畳み、件数も違っていた

- 分類: `なし`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: `fe70b2b` で **15 ブロック**（89 / 97〜110）に分けた。**数え直すと 13 ではなく 15。**
- 既存ルール: 同上（併記 23 と同じ）
- 次にどう防ぐか: 今回は記録のみ

### 併記 29. 「設計を共有している」の主張が、対象外の指摘まで含んでいた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 該当する 60 件に限る必要があった。`6fe247f` で修正。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 30. 「フォーマッタの失敗が 2 番目に多い」という比較が成り立たなかった

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: `6fe247f` で削除。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 31. 層を上げる材料の一覧から、規約が在るのに再発した形が漏れていた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 引き継ぎを表 A / 表 B に分け、落ちていた 2 行を足した。**各ブロックの「既存ルール」欄を
  grep して数え直した結果、件数は指摘より 1 件ずつ多かった**（6 件 / 3 件）。`6fe247f` で修正。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 32. 前の巡の判断が崩れた形を、規約の抜けの候補に数えていなかった

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 「反映先の案」に規約が覆っているか書かないままだったため、候補の数え上げから
  漏れていた。`6fe247f` で表 A を 4 行にした。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 33. 規約が README 側だからという理由で、1 件を再発から外していた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 表 B の条件の読み違え。同じ形を 2 箇所で違う数にしていた。`9f48147` で 6 → 7 件へ。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 34. リンク切れの指摘を、付け先の指摘と同じ行に畳んだ

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: **表 B の列が「規約」ではなく「形」である**ことを見落としていた。`9f48147` で
  3 → 2 件にし、指摘 42 を新設の表 C へ移した。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 35. ゲートで落ちている形を、規約の抜けの候補に置いていた

- 分類: `plan`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: fmt の 15 件は remedy をゲートとして書いておきながら表 A に置いていた。
  `AGENTS.md`「強制力の序列」の「ツールで落とせるものは規約の文に留めない」が決め手。
  `9f48147` で表 C を新設して移した。
- 既存ルール: `AGENTS.md`「強制力の序列」の「ツールで落とせるものは規約の文に留めない」。**規約は在ったが、引き継ぎ先を選ぶときに引けていなかった**
- 次にどう防ぐか: 今回は記録のみ

### 併記 36. 一覧に無い形と、枝を揃え忘れた形を同じ行に数えていた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 指摘 44 は一覧を増やしても直らない形だった。`9f48147` で
  **`18 + 4 + 38 + 2 = 62`** に組み替えた。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 37. `ModulePath` の再発に 1 件足りなかった

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 指摘 23（`node:fs` と `fs`）が規約の挙げる理由そのものだった。`9f48147` で
  2 → 3 件へ。**指摘の「表 A から外す」という結論だけは当たらず**（23 は元から表 A に無い）、
  その旨を注記した。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 38. `error-variant` の再発が引き継ぎから漏れていた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 67 / 69 / 79 / 85 の 4 件が同じ既存の規約を挙げていた。`9f48147` で表 B へ足した。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 39. 「理由を落とした」形がタグの合計に埋もれていた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 指摘 8 / 70 の 2 件。`9f48147` で表 B へ足した。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 40.（未対応のままマージ）引き継ぎの表が 3 つになったのに「2 つ」と書いたまま

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 表 C を足したあと、「2 つの分類を見る」という文が残っていた。
  **返信も修正もされないまま #171 がマージされた。** `main` の
  `harness/records/pr-169.md` に残っている。
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」の 2 つ目
- 次にどう防ぐか: 今回は記録のみ。**過去の記録は書き換えない**（`README.md`）ので、
  直すなら新しい記録に書く形になる。棚卸しの担当

### 併記 41.（未対応のままマージ）優先順だけを直した 1 件を、バリアント不足と同じ行に数えた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 指摘 69 はバリアントが不足していたのではなく、返す順を変えただけだった
  （`ReboundSpelling` / `UnreadableDeclaration` は既に在った）。
  **返信も修正もされないままマージされた。**
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 併記 42.（未対応のままマージ）最終の引き継ぎが、表 B の 2 行を落としていた

- 分類: `unverified-claim`
- 出どころ: レビュー（Codex、PR #171 の回）
- 内容: 「最終の引き継ぎは表 A / B / C と同じ母集団を覆う」と書きながら、
  18 件の enum の取りこぼしと 3 件の `ModulePath` が落ちていた。
  **返信も修正もされないままマージされた。**
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

## 手戻り

**5 つ。どれも指摘に立てた**（指摘 5 / 6 / 7 / 8 / 9）。

加えて、**レビュー 3 巡のうち 2 巡が「前の巡の修正が持ち込んだ副作用」だった**
（指摘 2 が指摘 1 の修正の、指摘 3 が指摘 2 の修正の副作用）。
**1 巡目の時点で `type_reference` が包みの先を見ていないことには気づいていたが、
「既存の（包みなしの）経路にも同じ穴がある」と誤って判断して見送っていた。**
実際には包みなしの経路は `node.parent()` で注釈を拾えており、穴があったのは
包みを入れた側だけだった。**見送る前に既存の経路を 1 回走らせていれば分かった。**

変異を戻すのに `git checkout -p` を使いかけて対話待ちで止まった件もあった
（定数を手で書き戻して復旧。差分は `git diff --stat` で確認済み）。
**pr-166 の指摘 3（計測用のコードを消すときに実装ごと巻き戻した）と近い形**だが、
今回は実装を失っていないので指摘には立てていない。

## うまくいったこと

- **Issue の一覧を `node-types.json` から数え直したことで、6 種別目
  （`instantiation_expression`）を見つけた。** Issue の 5 件のままだとその形が
  取りこぼしのまま残っていた
- **レビュー指摘を鵜呑みにせず、毎回「この PR で入ったものか」を確かめた。**
  指摘 4 は `git worktree` に `origin/main` を出してビルドし、**同じ 2 ファイルで出力が
  1 文字も変わらない**ことを見てから「この PR のものではない」と書いている
- **指摘の例をそのままテストにせず、逆向きも測った。** 指摘 2 は包みの型名が落ちる向きだけを
  挙げていたが、実測するとチャンク側の型名が落ちる向きも同じように壊れていた。
  どちらも回帰テストに入れた
- **変異を最終の差分に対して取り直した。** 2 巡目以降は本文を書く前に全種類を走らせ、
  1 つ目の変異では落ちた 18 件が全部新規で既存が 1 件も落ちないことまで `comm` で
  突き合わせている
- **フックについて「判定できなかった」と書いた。** `echo hook-canary` は通ったが、
  `.claude/settings.json` に `PreToolUse` が配線されていないことを確かめ、
  「フックが発火しない環境」とは断定していない（`AGENTS.md`「実装を始める前に」）
- **判断を仰ぐ側と、自分で決める側を分けた。** #170 / #173 はどちらも `rules/` の表を
  触るので判断待ちにし、包みの一覧のように漏れが偽陰性へ倒れるものは自分で決めた

## 規約への反映

**`rules/` も `AGENTS.md` も触っていない。**

`AGENTS.md`「規約の更新」は「ルールに書かれていない指摘が **2 回以上**出たら規約の抜けとして
扱う」と定めており、**下の表 A の 4 行のうち 3 行がその条件を満たす**。

| 形 | 件数 | 条件 |
|---|---|---|
| 変えた関数に依存している別の場所を確かめていない（指摘 1 / 2） | 2 | **満たす** |
| 直した範囲の切り方が 1 段粗かった（指摘 3） | 1 | 満たさない |
| 記録の立て方を守れなかった（併記 23 / 28） | 2 | **満たす**（ただし欠けているのは分類の語彙で、規約は README に在る） |
| remedy が効くかを確かめてから書いていない（併記 5 / 6 / 16） | 3 | **満たす** |

ただし `harness/records/README.md`「数えるのと、規約やフックへ手を入れるのは別の回に行う。
記録を書く回は材料を残すところまで」とルーティンの指示（集計・規約や skill の変更・介入判断は
棚卸しの管轄）に従い、**この回は材料を残すところまでにする**。

**`count.sh` が数える `対策済` の行は書かない。** 上のとおり介入していないため
（pr-107 / pr-110 / pr-117 / pr-129 / pr-166 と同じ扱い）。

### 棚卸しへの引き継ぎ

**A. 規約の抜けの候補（覆う条文が無い形）**

| 形 | 件数 | 分類 | 出どころ |
|---|---|---|---|
| **共有している関数の意味を変えたとき、その意味に依存している別の場所を確かめていない** | **2**（指摘 1 / 2） | `なし` | この PR |
| 直した範囲の切り方が 1 段粗く、同じ壊れ方が内側に残った | **1**（指摘 3） | `なし` | この PR |
| 記録の立て方（1 件 = 1 ブロック）を守れなかった | **2**（併記 23 / 28） | `なし` | PR #171 の回 |
| **remedy が効くかを確かめてから書いていない** | **3**（併記 5 / 6 / 16） | `plan` | PR #171 の回 |

**1 行目の 2 件は同じ形。** 1 件目は**呼び出し元**（`name_node_of` を直し、その相棒として
doc に書かれている `annotated_nodes_of` を置き去りにした）、2 件目は**呼び出し先**
（`annotated_nodes_of` に足したものが、それを呼ぶ `bound_type_names_of` の意味まで変えた）で、
どちらも**変えた関数に依存している別の場所を確かめていない**。
`rules/architecture.md`「モジュールの公開 API」は公開範囲の話で、この形は覆っていない。

**2 行目（指摘 3）は 1 行目と分けた。** 指摘 2 の修正で `bound_type_names_of` の引数を
注釈の並びへ変えたあと、**呼ぶ側が外側の注釈をまとめて 1 つの範囲にしていた**。
依存している別の場所を見落としたのではなく、**自分で引いた境界が 1 段粗かった**形で、
直し方も違う（相手を確かめるのではなく、範囲の分け方を 1 箇所へ寄せた）。
**1 件なので「2 回以上」には当たらない。**

**3 行目は「規約の抜け」ではなく「分類の語彙の抜け」。** `harness/records/README.md`
「指摘 1 件 = 1 ブロック（まとめない）」という規約は在るが、`TEMPLATE.md` の分類の表に
対応する行が無いので `なし` になっている。**層を上げる材料としては 2 行目までと性質が違う。**

**4 行目は `分類: plan` だがここに置いた。** 表 A の条件は分類ではなく
**覆う条文が在るか**で、3 件とも「remedy が効くかを確かめてから書く」という決まりが
`rules/` にも `AGENTS.md` にも無い（ブロックの「既存ルール」欄も同じことを書いている）。
**3 件あるので「2 回以上」に当たる。**

**B. 規約が既に在るのに再発した形（層を上げる材料）**

| 形 | 件数 | 既に在った規約 |
|---|---|---|
| 出したもの・測ったものを確かめずに書いた | **41**（この PR の指摘 5 / 6 / 7 / 8 / 9 と、併記の 36 件） | `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」/ `harness/records/README.md`「推測で埋めない」 |
| **引き継ぎ先（規約の文かゲートか）を選び違えた** | **1**（併記 35） | `AGENTS.md`「強制力の序列」（ツールで落とせるものは規約の文に留めない） |
| doc が実測と食い違う | **1**（この PR の指摘 10） | `rules/coding.md`「コメントは doc と Why / Why not に絞る」 |

**1 行目の 41 件のうち 36 件は PR #171 の回のもので、対象はすべて
`harness/records/pr-169.md`。** 記録そのものの正確さに関する指摘で、コードの欠陥は 1 件も無い。
**`AGENTS.md` はこの族について既に 2 つの節を持っており、それでも再発している**ので、
層を上げるかどうかは棚卸しが判断する材料になる。

**2 行目は 1 件なので「2 回以上」には当たらない。** 同じ `plan` でも、
**走らせれば分かる remedy の誤り（併記 5 / 6 / 16）は覆う条文が無い**ので表 A へ置いた。
こちらは規約が在りながら引けていなかった形。

**`plan` の残り 1 件（この PR の指摘 4）はどちらにも入れていない。** 形が違い
（remedy ではなく、既存の穴に載る入力が増えることを計画に書いていなかった）、
**行き先は表 D の #173** になる。

**分類ごとの行き先（合計 52 件）**

| 分類 | 件数 | 行き先 |
|---|---|---|
| `unverified-claim` | 41 | 表 B の 1 行目 |
| `plan` | 5 | 表 A に 3 件（併記 5 / 6 / 16）、表 B に 1 件（併記 35）、表 D に 1 件（指摘 4 → #173） |
| `なし` | 5 | 表 A |
| `comment-mismatch` | 1 | 表 B の 3 行目（行き先は #173 と同じファイル） |

**C. ゲートの位置で決まるもの（規約の文を足しても直らない）**

| 形 | 件数 | remedy の形 |
|---|---|---|
| 通知の送信が、API の応答を見ずに成否を出していた | 1（指摘 8） | 応答が空（204）かどうかで判定する。**ただし送信はルーティンの指示に書かれたコマンド例そのもので、リポジトリに残る仕掛けではない** |

**D. 判断待ち（この PR から出した Issue）**

| Issue | 内容 | なぜ判断待ちか |
|---|---|---|
| #170 | アクセサ（getter / setter）の型シグネチャ。関数型のアクセサは**偽陽性**になることを実測 | 印の置き場所が `chunk` の語彙か `type_structure` の語彙のどちらかを触る |
| #173 | 推論された型名が綴りのまま比べられる（**偽陽性**）。`origin/main` でも同じことを実測 | `rules/architecture.md`「どこまでを「取れなかった」に数えるか」の表に 3 つ目の区分が要る |

**E. 未対応のまま `main` に入っているもの**

併記 40 / 41 / 42 の 3 件は、**返信も修正もされないまま #171 がマージされた**。
`harness/records/README.md`「過去の記録は書き換えない」があるので、
`harness/records/pr-169.md` を直す形にはならない。**扱いは棚卸しが決める。**
