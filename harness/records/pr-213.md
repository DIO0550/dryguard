# PR #213 hover もサーバの作業が落ち着くまで尋ね直す

- マージ日: 2026-09-19
- 関連 Issue: #198
- 差分規模: 14 ファイル / +352 -30

## ゴールと結果

**#198 のゴールは「決めること」に答えること**で、Issue が挙げた 2 案のどちらを採るかだった。
案 1（hover にも `references` と同じ「落ち着くまで待つ」を入れる）を採り、実装まで通した。

| ゴール | 結果 |
|---|---|
| `any` を「サーバの答え」として受け取ってよいか | **落ち着く前の `any` は受け取らない。** 上限まで落ち着かなければ `ServerStillWorking` として「測れない」へ倒す |
| 案 1 / 案 2 のどちらを採るか | **案 1。** 待ちの置き場所が `lsp::connection` の 1 箇所のままになる |
| 待ち切っても `any` が返る場合 | **Issue 本文がスコープ外と決めた分**なので触っていない。落ち着いた後の `any` は `Answered` のまま |

**却下した案 2 の理由**（Issue のコメントに残した）: `pipeline` が待ちの手順を持つことになり
`rules/architecture.md`「`lsp` はオーケストレーションを持たない」の裏返しになるうえ、
開かせた直後に 1 度落ち着かせても**その後に始まる作業には届かない**（monorepo が次の
プロジェクトを読む形。references 側では
`test_references_interrupted_again_keeps_asking_until_no_work_is_running` が既に固定している）。

### 実サーバで修正前後を測った

`pnpm install --frozen-lockfile` で typescript-language-server 6.0.0 を入れ、
注釈を 1 つも持たない getter 2 つ（推論される型は別モジュールの `Receipt` と `Shipment`）を
`compare` した。

| `hovered` の形 | `type_signature_match` |
|---|---|
| 1 往復（修正前） | `Unifiable` ← **Issue が報告した偽陽性** |
| 落ち着くまで尋ね直す（修正後） | `UntracedTypeName { reason: OmittedValueTypeAnnotation }` |

**#187 が「書かなかった」とした統合テストが、この回で書けるようになった**
（#198 本文「統合テストを注釈の無いアクセサで書かなかったのはこのため」）。

## 指摘

**この PR に 1 件**（自己修正 1 件。**レビュー由来・CI 由来は 0 件**）。

レビューは `copilot-pull-request-reviewer[bot]` の **1 巡だけ**で、
本文が `**Findings:** None` と書いており、レビュースレッドは 0 本
（`pull_request_read` の `get_reviews` が 1 件・`get_review_comments` が `totalCount: 0`）。

CI は **push 1 回・チェック 2 つがどちらも `success`**
（`fmt / clippy / test` と `差分規模の宣言`。head は `2400d9f`）。

### 1. hover を落ち着かせると、hover を「前の問い合わせ」に使っていた既存テスト 2 本が成り立たなくなることを、計画に織り込んでいなかった

- 分類: `plan`
- 出どころ: 自己修正
- 内容: 計画（Issue のコメント）は配線先として `semantics` / `pipeline` /
  `classification` / `report` を挙げたが、**`lsp::connection` のテストモジュール自身**が
  漏れていた。`test_references_answered_while_work_prepared_earlier_ran_asks_again` と
  `test_references_with_prepared_work_that_has_not_begun_does_not_settle` は
  「前の問い合わせ（hover）の最中に progress の token が用意される」形で書かれており、
  hover が落ち着くまで尋ね直すようになると**用意されただけの作業が残るうちは hover が
  答えを返さない**ので、前の問い合わせ役を務められなくなる。実装後にテストが落ちて気づき、
  落ち着きを待たない問い合わせ（typeDefinition）へ置き換えた
- 既存ルール: `AGENTS.md`「実装を始める前に」の
  「計画の入力を鵜呑みにしない。完了条件を grep 等で機械的に確かめられる形に落とし、
  自分で数え直す」。**この回に当てはめると「`hover` を呼んでいる箇所を数え直す」**で、
  実際にやったのは `HoverOutcome::` の match 先を grep することだけだった。
  マージ前の `src/lsp/connection.rs` を `grep -n "\.hover(&document"` すると 5 箇所あり、
  うち 2 箇所（1515 / 1543 行）が references のテスト。**着手前に引けた**
- 次にどう防ぐか: 今回は記録のみ（介入判断は棚卸しの回）

## 手戻り

**上の指摘 1 件ぶんだけ。** 既存テスト 2 本の「前の問い合わせ役」を hover から
typeDefinition へ置き換えた。守っている振る舞い（前の問い合わせで用意された作業が動いたなら
尋ね直す）は変えていないので、テストの意図は書き換えていない。

**実装の方針（案 1）は着手から変えていない。** ステージをまたぐ移動も、
既存モジュールの再配置も無し。

## うまくいったこと

- **修正を外して落ちることを確かめてから提出した**（`rules/testing.md`
  「迷ったら、その 1 件だけ実装を壊して走らせる」）。`hovered` を 1 往復に戻すと、
  新しい単体テスト 2 本が落ち、統合テストが `left: Unifiable` で落ちることまで見ている。
  `test_hover_answered_without_any_work_asks_once` は壊さなくても通るが、
  これは「往復を増やさない」側の回帰を止めるために置いたもの
- **実サーバをこのセッションに入れて `--ignored` まで走らせた。** CI 任せにせず、
  `PATH="$PWD/node_modules/.bin:$PATH" cargo test -- --ignored` を手元で通している。
  Issue が実測で報告した現象を、**統合テストの形で再現してから**直せた
- **既にある形へ乗せ、語彙を増やさなかった。** `ServerStillWorking` は
  `ReferencesOutcome` に既にあり、`progress` は `rules/naming.md` の表が持っている
  （`rules/naming.md`「語を増やすときは、既にある語で言えないかを先に確かめる」）

## 実行環境メモ

**層 3（Claude Code hooks）は判定できなかった。** `echo hook-canary` は deny されずに
通ったが、`.claude/settings.json` に `PreToolUse` の配線が無い
（`grep -c PreToolUse .claude/settings.json` が `0`）。**canary が無いので、フックが
不発だったのかどうかは分からない**（`AGENTS.md`「実装を始める前に」）。

層 2（`harness/githooks/pre-push`）はこのセッションで実際に走らせ、
fmt / clippy / test を 1 つずつ独立に実行してすべて通っている。

## 規約への反映

**無し。** 指摘 1 件は `plan` で、対応する項目は `AGENTS.md`「実装を始める前に」に
**既にある**（pr-72 / pr-74 から来た「計画の入力を鵜呑みにしない」）。今回はその項目に
当たる再発なので、新しい規約は足していない。

**数えるのと手を入れるのは別の回**（`harness/records/README.md`「記録は溜まり始めている」）。
`count.sh` による集計と介入判断は棚卸しの回に行う。
