# PR #217 名前付きチャンクで代入先の注釈を集めない判断を、実コーパスで測って固定する

- マージ日: 2026-09-20
- 関連 Issue: #202（#188 から分離した「設計判断の確認」）
- 差分規模: 2 ファイル / +29 -2

## ゴールと結果

**#202 のゴールは「`outer_annotated_nodes_of` が名前を持つノードで空を返す判断を変えるか」を
測ってから決めること。** Issue 自身が決定則（取り戻した件数が 0 なら PR #199 と同じ理由で
採らない）を先に置いていたので、測定の結果がそのまま答えになった。

| ゴール | 結果 |
|---|---|
| 案 2 の採否 | **採らない**（Issue の決定則にそのまま当たった） |
| 実コーパスで取り戻す件数 | **0 件**（`Normalized` 314 → 314。チャンク単位の 517 行も全行一致） |
| 判断の残し方 | `outer_annotated_nodes_of` の doc に Why not、`syntax::chunk` に回帰テスト 1 件 |
| 振る舞い | 変えていない |

### 測った内訳

rxjs 7.8.1（npm パッケージ同梱の `src`、`.ts` 251 ファイル）に、`scan` と同じ経路を辿る
一時プローブで**全チャンク 517 件**の型シグネチャを尋ねた（プローブはコミットしていない。
PR #199 と同じやり方）。`typescript-language-server 6.0.0` / `typescript 5.9.3`。

| `TypeSignatureOutcome` | main (`9b4fcdf`) | 案 2 を当てた木 |
|---|---|---|
| `Normalized` | 314 | 314 |
| `UntracedTypeName / OmittedTypeAnnotation` | 41 | 41 |
| `UntracedTypeName / NoTracedRecord` | 1 | 1 |
| `UnopenedTypeName` | 122 | 122 |
| `UnreadableSignature` | 33 | 33 |
| `SiteDependentSpelling` | 5 | 5 |
| `NoTypeThere` | 1 | 1 |

案 2 を広げた形（名前付きチャンクで包みが言い切った型まで集める）でも同じ 517 行が一致した。

### Issue の「賛成の材料」が外していたこと

Issue は「多めに集めても答えは変わらない。増えるのは LSP への往復だけ」と書いていたが、
**出方は変わる**。`single_outcome_of` は**記録が 1 つも無いこと**を根拠に `NoTracedRecord` を
先に出しているので、案 2 で記録が付くと `OmittedTypeAnnotation`（注釈を書くと辿れる）へ倒れる。
代入先には注釈がもう書いてあるので、#188 の案 3 が直した嘘の案内が戻る。実サーバで
`compare --explain` を通して確かめた。

取り戻せないほうの理由は、代入先の注釈から来る型名が hover の綴りに出るのは**注釈を省いた
位置**だけで、その出現は #199 以降**辿った記録を綴りで引かない**ため
（`NormalizedSignature::spelled_positions`）。

## 指摘

**この PR に 1 件**（レビュー 0 本 + 自己修正 1 件）。**レビューは 1 本も付かずにマージされた**
（レビュー・レビュースレッド・PR コメントのいずれも 0 件）。CI はこの branch の 2 チェック
（`fmt / clippy / test` / `差分規模の宣言`）がどちらも 1 回で `success` で、
**CI 由来の指摘は無い**。

### 1. 測定の母集団を、比べる相手（PR #199）と揃えずに測り始めた

- 分類: `plan`
- 出どころ: 自己修正
- 内容: 最初の測定は**候補ペアに現れるチャンクだけ**（301 件）に尋ねていた。PR #199 が記録した
  「尋ねた 517 チャンク」と母集団が違うので、そのまま並べると**前回と比べられない数**を
  「前後の比較」として出すところだった。プローブを全チャンクへ広げたら 517 件・内訳とも
  PR #199 の head と一致し、比較の基準が揃った
- 既存ルール: AGENTS.md「実装を始める前に」の「計画の入力を鵜呑みにしない」
  （Issue が挙げた数も「調べた結果」であって、こちらで数え直す対象）
- 次にどう防ぐか: 今回は記録のみ。**先行する記録と数を比べるときは、母集団の定義を先に
  突き合わせる**という形で、既存の項目の範囲に収まっている

## 手戻り

- **測定の母集団**（指摘 1）。301 件で 1 度測ってから、全チャンク 517 件で測り直した
- **テストの入力文字列の書き方。** 1 行で書いたら `cargo fmt --check` が落としたので、
  隣接するテストと同じ行継続の綴りに直した。push 前の検査（層 2 と同じ内容）で拾ったので、
  CI には出ていない

## うまくいったこと

- **Issue が決定則を先に書いていたので、測った時点で判断が確定した。** 「0 件なら採らない」が
  本文にあったおかげで、測定→採否の間に新しい判断を挟まずに済んだ
- **却下の理由をコードの側にも残した。** doc の「名前を持つチャンクでは空」だけでは、
  案 2 が「集め損ねの修正」として再提案されうる。Why not に測った数を書いた
- **回帰テストを、案 2 を実際に当てて落ちることまで確かめた**（`rules/testing.md`
  「迷ったら、その 1 件だけ実装を壊して走らせる」）。既存の
  `..._leave_out_the_annotation_it_is_assigned_to` は代入先も引数も注釈済みの入力なので、
  **注釈を省いた形にだけ案 2 を入れる狭い実装では落ちない**ことも同時に分かった

## 規約への反映

**無し。** この回の指摘は 1 件で、分類は既存の `plan`。`AGENTS.md`「実装を始める前に」に
専用の項目が既にあり、**その項目の射程に収まっている**（数え直す対象が「Issue の挙げた一覧」から
「先行する記録の数」へ広がっただけで、「入力を鵜呑みにしない」という形は同じ）。
1 件では `AGENTS.md`「規約の更新」の 2 回に届かないので、材料を残すところまでにする。

## 実行環境メモ

`echo hook-canary` は deny されずに通ったが、`.claude/settings.json` に `PreToolUse` の配線は
無く（`hooks` は `SessionStart` のみ）、**canary が無いので層 3 は判定できなかった**。
LSP を要するテストは、この環境に typescript-language-server を入れて
`cargo test -- --ignored` まで走らせてある（semantics 49 件すべて `passed`）。
