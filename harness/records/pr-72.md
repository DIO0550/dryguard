# PR #72 ♻️ [Refactoring]: #70 chunk.rs の文字種の判定を source_character へ寄せる

- マージ日: 2026-08-17
- 関連 Issue: #70
- 差分規模: 2 ファイル / +47 -6（コミット 4 本・マージを除く）

## ゴールと結果

Issue のゴールは「`chunk.rs` の文字種の判定を `syntax::source_character` から呼ぶ形にする」。
完了条件（`chunk.rs` から引用符・識別子文字の判定が消え、既存テストがそのまま通る）は満たした。
振る舞いは変えていない。

**ずれ**: Issue が挙げていたのは 2 箇所だったが、**3 箇所目があった**。

`find_closing_index` の `text_crosses_the_line_end` に
`matches!(state, ScanState::Text('`'))` が残っていた。これは「引用符かどうか」ではなく
「その引用符で開いた文字列が行をまたげるか」で、`source_character` に対応する関数が無かった。
`is_multiline_quote` を新設して寄せ、コミット 2 本（回帰テスト + 付け替え）を足した。
計画では 2 コミットの予定が 4 コミットになっている。

スコープを広げる判断の根拠は**完了条件の文**（「引用符・識別子文字の判定が消え」）。
生のバッククォートが 1 つ残っているとこれを満たさない。ただし 3 箇所目は
**重複を消したわけではない**（呼び出し元が 1 箇所しかない）ので、
スコープ外と判断された場合に落とせるよう独立した 2 コミットに分けた。

## 指摘

**レビューコメント 0 件・レビュー 0 件でマージされた**（GitHub API で確認）。
以下の 2 件はいずれも実装中の自己修正。

### 1. Issue が挙げた 2 箇所の他に、3 箇所目があった

- 分類: `plan`
- 出どころ: 自己修正
- 内容: Issue 本文の表に 2 箇所が挙がっていたので、計画をその 2 箇所だけで立てた。
  付け替え後に完了条件を grep で確かめて 3 箇所目に気づいた。
  **Issue の表を「調べた結果の一覧」として受け取り、自分で数え直していなかった**
- 既存ルール: なし。`AGENTS.md`「タスクの分割」は分ける側の判断軸を定めているが、
  Issue が挙げた対象が網羅されているかを実装側で確かめることは書かれていない
- 次にどう防ぐか: 今回は記録のみ

  **注記（数えるための材料）**: 効いたのは目視ではなく**完了条件を grep に落として確かめた**こと。
  計画の段階でこれをやっていれば最初から 3 箇所を数えられていた。
  「完了条件を機械的に確かめられる形にしてから計画を立てる」は観点（層 3）に置ける候補だが、
  **`plan` はこれが 1 回目**なので昇格させない

### 2. 引用符の集合を守るテストが 1 件も無かった

- 分類: `test-coverage-gap`
- 出どころ: 自己修正
- 内容: `chunk.rs` の既存テストに**バッククォートを使ったものが 1 件も無かった**。
  文字列の中の `}` を数えないことは `"` で 2 件確かめていたが、引用符の集合は 3 つある。
  この状態で付け替えると、集合からバッククォートが落ちても既存テストは全部通る。
  行をまたぐテンプレートリテラル（`text_crosses_the_line_end`）も同様に未カバーだった
- 既存ルール: rules/testing.md「assert は『落ちうるか』で見る」。
  ただし**この項が想定しているのは自分が書く assert** で、
  「これから触る既存コードが、既存テストで守られているか」を確かめる話は書かれていない
- 次にどう防ぐか: 今回は記録のみ。回帰テストを 2 件足し、
  どちらも実装を壊して落ちることを確認してから付け替えた

## 手戻り

1 回。**捨てた実装は無く、計画の不足による追加。**

指摘 1 のとおり、計画時点で 3 箇所目を数え落としていた。計画コメントには
「行をまたぐテンプレートリテラルのテストは今回付け替える箇所ではないので足さない」と
書いていたが、**そこが付け替える箇所になったので前提ごと消えた**。
Issue にはこの経緯を追記済み。

コミットは積み増しただけで、書き直し・revert は発生していない。

## うまくいったこと

- **完了条件を grep に落として確かめた。** 「`chunk.rs` から引用符・識別子文字の判定が消え」を
  `grep -n "is_alphanumeric\|is_alphabetic\|'\$'\|'\`'" src/syntax/chunk.rs` に翻訳して実行し、
  3 箇所目を見つけた。**目視で「消えたはず」と判断していたら見落としていた**
- **テストを先に置いてから付け替える順番を 2 回とも守った。** 付け替え対象を守るテストが
  無いことに気づくたび、回帰テストを先にコミットしてから付け替えた
  （2c7aa7d → b09caf8、0799fd6 → e712291）
- **足したテスト 2 件とも、実際に実装を壊して落ちることを確認した**
  （`rules/testing.md`「迷ったら、その 1 件だけ実装を壊して走らせる」）。
  引用符の集合から `` ` `` を外す / `text_crosses_the_line_end` からテンプレートリテラルの
  条件を外す、の 2 通りで FAILED を確認してから元に戻した
- **スコープ外になりうる分を独立して落とせる形に分けた。** 3 箇所目は重複の解消ではないので、
  レビューで「Issue の範囲を超えている」と判断されたら 2 コミット落とせば
  Issue 本文の 2 箇所ぶんだけが残るようにした

## 規約への反映

**無し。**

指摘は 2 件ともこの回が 1 回目（`plan` / `test-coverage-gap`）で、
`AGENTS.md`「規約の更新」の「**2 回以上**出たら規約の抜けとして扱う」に達していない。
どちらも「次にどう防ぐか」を記録のみに留めた。

`対策済` の行は書かない（介入していない回に書くと `count.sh` の再発数が 0 に戻るため）。

## 実行環境の記録

- **`echo hook-canary` はそのまま通った。ただし pr-67 / pr-69 の書き方より切り分けを 1 段進めた。**
  `.claude/settings.json` を確認したところ、このリポジトリには **`PreToolUse` フックが
  1 つも配線されていない**（あるのは `SessionStart` の `wire-githooks.sh` のみ）。
  canary フック自体が #50 として未実装なので、正しくは
  「**フックが不発だった**」ではなく「**canary が無いので層 3 が効いているか判定できなかった**」。

  **過去の記録は書き換えないので、pr-67 / pr-69 の「1 度も発火しなかった」はそのまま残す。**
  この回で分かったのは、あの 2 件も同じく判定不能だった可能性があるということ。
  数えるときはこの注記込みで読む
- **git hooks（層 2）は発火した。** push の出力に `pre-push: fmt` / `clippy` / `test` が出ている。
  `core.hooksPath` は `harness/githooks` に設定済みだったが、
  これが `wire-githooks.sh` の結果かクローン時の配線かは区別できない
- **CI（層 1）は success。** チェックは `fmt / clippy / test` の 1 本
- push 前検査は `cargo fmt --check` / `cargo clippy --all-targets -- -D warnings` /
  `cargo test` / `cargo doc --no-deps` / `cargo build` を **1 つずつ独立に実行**し、
  すべて exit 0。`&&` で連ねていない
- **Discord 通知は今回も送れていない。** `DISCORD_WEBHOOK_URL` が未設定。pr-67 / pr-69 から変わらず
- **`harness-record` スキルは未移植**（#55）。この記録も `README.md` と `TEMPLATE.md` を
  読んで手で書いた。pr-67 / pr-69 と同じ
- **レビュー 0 件でマージされた。** pr-67 / pr-69 はどちらもレビュー指摘があったので、
  指摘の出どころが「レビュー」から「自己修正」だけになったのはこの回が初めて
- テストは 92 + 6 から **94 + 6** に増えた（`chunk` の回帰テスト 2 件。既存の変更は無し）
- **`harness/records/README.md` の「まだ記録は 1 件も無い」は実態と食い違っている**
  （pr-67 / pr-69 があり、これで 3 本目）。記録の書き換えではなく README の修正なので、
  この回では触らずに事実だけ残す。**規約・ドキュメントへの介入は棚卸しルーティンの管轄**
