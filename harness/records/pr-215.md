# PR #215 既定値つき引数は注釈を省けるので、引数の位置ごとに注釈の有無を持つ

- マージ日: 2026-09-19
- 関連 Issue: #200
- 差分規模: 16 ファイル / +926 -234

## ゴールと結果

**#200 のゴールは「決めること」に答えること**で、Issue が挙げた 2 案のどちらを採るかだった。
案 2（引数の位置を、引数ごとに持つ）を採り、実装まで通した。

| ゴール | 結果 |
|---|---|
| 引数の位置の出どころをどう持つか | **引数ごとに持つ（案 2）。** `syntax::chunk` の `AnnotatedPositions` が値の型 1 つと引数ごとの注釈の有無を運ぶ |
| 添字がずれる形の扱い | **ソースと綴りの引数の数が揃ったときだけ添字を使い**、揃わなければ引数の位置すべてを省かれた扱いへ倒す（偽陰性側）。理由は `UnalignedParameters` に分けた |
| 案 1 を採らない根拠 | Issue 本文の実測（`Normalized` を 4 件失い、塞げたのは 0 件）をそのまま採用。**自分では測り直していない**（案 1 を実装していないため） |

**却下した案は Issue のコメントに残した**（案 1 / 添字を使わない案 / `UnalignedParameters` を
足さない案 / 引数を判定の並びの前に置く案）。

### 実サーバで修正前後を測った

`pnpm install --frozen-lockfile` で typescript-language-server 6.0.0 を入れ、
同じ綴りで構造の違う `Receipt` を既定値から受け取る 2 つのチャンクを `compare` した。

| コード | `--explain` の型シグネチャの行 |
|---|---|
| `5eb4673`（`main`、修正前） | `単一化可能 → 共通化する側` ← **Issue が報告した偽陽性** |
| `f520ae2`（この PR の head） | `測れない (型注釈が省かれている: 注釈を書くと辿れる) → どちらでもない` |

この形をそのまま `tests/fixtures/defaulted-parameters/` に置き、統合テストで固定した。
**フィクスチャを `5eb4673` に当てて `単一化可能` が出ることも確かめてある**
（`rules/testing.md`「迷ったら、その 1 件だけ実装を壊して走らせる」の適用）。

### 実コーパスで副作用が無いことを測った

rxjs 7.8.1 の `src`（尋ねた 517 チャンク）を、`compare` と同じ経路を辿る一時プローブで
`5eb4673` と `f520ae2` の両方について測った（プローブはコミットしていない）。

| | `5eb4673` | `f520ae2` |
|---|---|---|
| `Normalized` | 314 | 314 |
| `UntracedTypeName`（注釈が省かれている） | 41 | 41 |
| `UntracedTypeName`（記録が無い） | 1 | 1 |
| `UntracedTypeName`（引数の数が揃わない） | — | 0 |
| `UnopenedTypeName` | 122 | 122 |
| `UnreadableSignature` | 33 | 33 |
| `SiteDependentSpelling` | 5 | 5 |
| `NoTypeThere` | 1 | 1 |

**分布が 1 件も動かない。** 塞ぐ形（推論された引数の型名が、書かれた出現の記録に乗る）は
このコーパスに現れなかった。案 1 が同じコーパスで 4 件失うのと対照的。

## 指摘

**この PR に 1 件**（自己修正 1 件。**レビュー由来・CI 由来は 0 件**）。

レビューは `copilot-pull-request-reviewer[bot]` の **1 巡だけ**で、
本文が `**Findings:** None` と書いており、レビュースレッドは 0 本
（`pull_request_read` の `get_reviews` が 1 件・`get_review_comments` が `totalCount: 0`）。

CI は **push 1 回・チェック 2 つがどちらも `success`**
（`Rust` = fmt / clippy / test と `PR body` = 差分規模の宣言。head は `f520ae2`）。

### 1. 引数の「注釈が省かれている」を記録が無い側より先に出す計画が、contextual typing で型がチャンクの外から来る引数に嘘の直し先を出していた

- 分類: `plan`
- 出どころ: 自己修正
- 内容: 計画（Issue のコメント）は判定の並びを
  「値の型 → 引数 → 記録が無い」の順にするつもりで書いていた。だが
  `tests/fixtures/references/src/*/contextual.ts` の
  `export const traceBilling: (figure: Shape) => void = function inner(figure) {…}` では、
  チャンク（`function inner(figure)`）の引数リストに注釈が無い一方で、
  `Shape` は**代入先の注釈としてソースに書かれている**（`syntax::type_reference` の
  集める場所の一覧から漏れているだけ。#188）。計画どおりの並びだと、ここに
  「型注釈が省かれている: 注釈を書くと辿れる」が出る。既存の
  `test_compare_with_an_lsp_does_not_blame_a_missing_annotation_for_a_contextually_typed_parameter`
  が落ちて気づき、**引数だけを「記録が無い」側より後ろへ**動かした
- 既存ルール: `rules/naming.md`「`untraced type name` の 2 通りを混ぜない」（当時の表記）が
  **「注釈を書いてある利用者に『書かれていない』と出す」ことを名指しで禁じている**。
  計画を書く時点でこの規約は読んでいたが、**値の型に `AnnotatedPositions::AllWritten` という
  逃げ道があり、引数には無い**という非対称に気づいていなかった。
  `AGENTS.md`「実装を始める前に」の「計画の入力を鵜呑みにしない。完了条件を grep 等で
  機械的に確かめられる形に落とし、自分で数え直す」に当てはめると、
  **「注釈の有無を根拠にしている既存テストを数え直す」**で、
  `grep -rn "OmittedValueTypeAnnotation\|NoTracedRecord" tests/` を着手前に引けば
  `contextual.ts` を使う 1 本が出てきた
- 次にどう防ぐか: 今回は記録のみ（介入判断は棚卸しの回）

## 手戻り

**上の指摘 1 件ぶんだけ。** `single_outcome_of` の並びを
「値の型 → 記録が無い → 引数 → 引数の数が揃わない」へ直し、
`omitted_position_reason_of` を `omitted_value_type_holds_a_name` と
`omitted_parameter_reason_of` の 2 つに割った。`pipeline::type_signature_match_of` の並びも
同じ順へ揃え、そこで書いていたテストの期待値（`UnalignedParameters` が残る）も
`NoTracedRecord` が残る形へ直した。

**実装の方針（案 2）は着手から変えていない。** ステージをまたぐ移動も、
既存モジュールの再配置も無し。**手戻りは push の前に収まっており、push は 1 回**。

## うまくいったこと

- **修正前のコードで再現を確かめてから着手した。** Issue は `fd3fbd6` / `c9bf00b` での実測を
  載せていたが、`main` が `5eb4673` まで進んでいたので**同じ再現をもう一度取り直した**
  （`AGENTS.md`「記録・PR 本文に数値や主張を書く前に」）。結果は Issue と同じ `単一化可能`
- **対照を同じフィクスチャに置いた**（`rules/testing.md`「『出ない』を確かめるときは、
  対照を 1 件置く」）。`echo-annotated-billing.ts` / `echo-annotated-report.ts` が
  `Unifiable` のままであることを同時に見ているので、「測れない」側へ広く倒しただけでは通らない
- **実コーパスで副作用の有無を測った。** Issue が案 1 について出していた数（4 件失う）と
  同じ土俵で案 2 を測り、**分布が 1 件も動かない**ことを確かめてから本文に書いた
- **`rules/` を同じ PR で出し直した**（`AGENTS.md`「実装を変えたら、rules/ 配下の記述も
  同じ PR で出し直す」）。`rules/naming.md` の `value type` は「**注釈を省ける唯一の位置**」と
  書いており、**この Issue がその記述を反証していた**。放置すると規約が既に無効になった
  前提を説明したまま残る
- **語を増やす前に既にある語で言えないかを確かめた**（`rules/naming.md`）。
  `type annotation` を位置に依らない語として置き、`value type annotation` を
  その 1 つの場合に狭める形にしたので、`untraced type name` の内訳は 3 通りに増えたが
  語彙表に足したのは `type annotation` / `annotatable position` / `parameter annotation` の
  3 語で、既存の語の意味は狭めただけ

## 実行環境メモ

**層 3（Claude Code hooks）は判定できなかった。** `echo hook-canary` は deny されずに
通ったが、`.claude/settings.json` に `PreToolUse` の配線が無い
（`grep -c PreToolUse .claude/settings.json` が `0`）。**canary が無いので、フックが
不発だったのかどうかは分からない**（`AGENTS.md`「実装を始める前に」）。

層 2（`harness/githooks/pre-push`）はこのセッションで実際に走らせ、
fmt / clippy / test を 1 つずつ独立に実行してすべて通っている。
LSP を要するテストも `pnpm install --frozen-lockfile` でサーバを入れて
`cargo test -- --ignored` まで通した（マージ後の `9b4fcdf` でも
`1003 + 22 + 12` passed / `--ignored` は `6 + 4 + 49` passed を確認）。

## 規約への反映

**無し。** 指摘 1 件は `plan` で、対応する項目は `AGENTS.md`「実装を始める前に」に
**既にある**（pr-72 / pr-74 から来た「計画の入力を鵜呑みにしない」）。今回はその項目に
当たる再発なので、新しい規約は足していない。

**`rules/` への追記はしているが、それは介入ではない。** `rules/naming.md` /
`rules/architecture.md` の変更は**この PR が実装を変えたことへの追従**
（`AGENTS.md`「実装を変えたら、rules/ 配下の記述も同じ PR で出し直す」）で、
指摘の再発を止めるために層を上げたものではない。

**数えるのと手を入れるのは別の回**（`harness/records/README.md`「記録は溜まり始めている」）。
`count.sh` による集計と介入判断は棚卸しの回に行う。
