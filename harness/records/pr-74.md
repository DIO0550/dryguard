# PR #74 ✨ [New Feature]: #14 import 収集とディレクトリ距離でドメインシグナルを作る

- マージ日: 2026-08-18
- 関連 Issue: #14
- 差分規模: 9 ファイル / +646 -1（コミット 7 本・マージを除く）

## ゴールと結果

Issue のゴールは「Stage 2 を飛ばした状態でも『依存ドメインが違う』を言えるようにする」。
完了条件（出力イメージの `依存先ドメイン不一致` に相当する判断材料が数値として取れる）は満たした。

フィクスチャ 2 組の実測:

| 組 | 構造類似度 | 依存の重なり | モジュール距離 |
|---|---|---|---|
| `utils/formatDate.ts` <-> `report/dateHelper.ts`（共有ユーティリティ） | 0.88 | 1.00 | 2 段 |
| `billing/discount.ts` <-> `inventory/reorder.ts`（別ドメイン） | 0.94 | 0.00 | 2 段 |

**ずれ**: 依存先集合の置き場所が計画と変わった（`pipeline` → `Chunk`）。詳細は指摘 1。
フィクスチャ 2 件の追加も計画に無かったが、これは対照を置くために要ったもので、
`rules/testing.md`「『出ない』を確かめるときは、対照を 1 件置く」に従った結果。

## 指摘

**レビューコメント 0 件・レビュー 0 件でマージされた**（GitHub API で確認）。
pr-72 に続いて 2 回連続。以下 1 件は実装中の自己修正。

### 1. 計画で決めた依存先集合の置き場所が、実装に入ると成立しなかった

- 分類: `plan`
- 出どころ: 自己修正
- 内容: Issue に投稿した計画では `pipeline` に `import_overlap_of` を足す形にしていた。
  実装に入って 2 点が分かった。**(a)** `chunk_pair_of` が読んだファイルをもう一度読むことになる。
  **(b)** 読み込みのエラーに `ChunkPairError` を使い回すと、`ChunkingFailed` が
  決して起きないバリアントとして API に残る（`rules/coding.md`「エラー型は原因ごとに
  バリアントを分ける」の趣旨から外れる）。`Chunk` が持つ形に変えて両方とも解消した。

  **根本原因は `docs/dryguard-plan.md` の読み落とし。** 「Stage 1: 候補抽出」に
  「同時に import / use 文を収集し、**各チャンクの依存モジュール集合を持たせる**」と
  書いてあり、計画の想定は最初から `Chunk` が持つ形だった。計画を立てる前に
  この一文を読み込めていなかった
- 既存ルール: なし。`AGENTS.md`「実装の進め方」は計画を Issue に書くことを定めているが、
  計画の前に `docs/dryguard-plan.md` の当該セクションを読むことは書かれていない
- 次にどう防ぐか: 今回は記録のみ

  **注記（数えるための材料）**: **分類 `plan` としては pr-72 に続いて 2 回目。**
  `AGENTS.md`「規約の更新」の「ルールに書かれていない指摘が 2 回以上出たら、
  規約の抜けとして扱う」に到達している。

  2 件の形は近い。pr-72 は「Issue が挙げた 2 箇所の表を調べた結果の一覧として受け取り、
  自分で数え直さなかった」、今回は「計画の根拠になる `docs/dryguard-plan.md` を
  読まずに設計を決めた」。**どちらも、計画の入力を自分で確かめずに受け取っている。**

  **昇格させるかの判断と、置く層の選択は棚卸しの回の管轄**（この回では行わない）。

## 手戻り

2 回。**どちらも実装中の書き損じで、捨てた設計は無い。**

1. **`specifiers_of` の「直前の語」を 1 文字ずつ足す実装にした。** `const path` が
   `constpath` という 1 語になり、直前の語の判定が壊れて **13 件中 6 件が落ちた**。
   語ごと読む形に直して解消。Red のあと Green にする途中で踏んだもので、
   テストが先にあったので即座に分かった
2. **「import でない文字列を拾わない」テストを、囮だけのソースで書いた。**
   `ImportSet::from_source` が `None` を返し、ヘルパーの `expect` で落ちた。
   同じソースに本物の import を 1 件置く形に直した。結果として
   `rules/testing.md`「対照を 1 件置く」に沿った形になり、囮を拾う実装だと
   1.0 が 0.5 に落ちる（= 落ちうる）assert になった

なお指摘 1（置き場所の変更）は計画の変更であって手戻りには数えていない。
`pipeline` 案のコードを書いてから捨てたわけではなく、書く前に気づいている。

## うまくいったこと

- **3 サイクルとも Red から回した。** `ModulePath` / `ImportSet` / `ModuleDistance` の
  それぞれで、テストだけを書いてコンパイルエラー（`cannot find type`）を確認してから
  実装した（`rules/tdd.md`「コンパイルエラーも Red として扱ってよい」）
- **ミューテーションで 2 件確認した**（`rules/testing.md`「迷ったら、その 1 件だけ
  実装を壊して走らせる」）。指定子の判定を素通しにすると囮を無視するテストが
  1.0 → 0.5 で落ち、相対指定の解決を止めると共有ユーティリティの統合テストが
  `Some(1.0)` → `Some(0.0)` で落ちる。**後者は Issue のスレッドが実データで記録した
  偽陽性そのもの**で、再現テストとして固定できた
- **Issue のスレッドに残っていた実データの知見を、実装前に読んで設計に入れた。**
  相対 import を解決しないと共有ユーティリティが `DO-NOT-EXTRACT` になる、という
  前のセッションの記録がそのまま `ModulePath` の設計理由になっている。
  **記録を残す運用が実際に効いた 1 例**
- **実測値を取って Phase 0 の検証材料にした。** モジュール距離が 2 組とも 2 段になり、
  距離だけでは共有ユーティリティと別ドメインを分けられないことが分かった。
  Issue のスレッドは「重なり 0 だけで倒さず距離と併せて見る」と書いていたが、
  **逆向き（距離だけでも分けられない）も成り立つ**。#32 の材料として Issue に残した
- **判断待ちの間に計画を出しておいた。** ブランチが空くのを待つ間に計画を Issue へ
  投稿してあったので、空いた回にそのまま着手できた

## 規約への反映

**`count.sh` が数える介入は無し。**

`rules/naming.md` の語彙表に 4 語（`import` / `specifier` / `module path` /
`module distance`）を足したが、**これは「同じ指摘が 2 回出たから層を上げた」ものではない。**
`rules/naming.md` 自身が「語を増やすときは……新しい語を足したら、この表にも足す」と
定めており、その更新手順に従っただけ。介入として数えると、次に同じ分類が出たときの
再発数が誤って 0 に戻る。

指摘 1（`plan`）は 2 回目に到達しているが、**介入判断は棚卸しの回の管轄**なので
この回では行わない（`AGENTS.md`「記録を数えて規約やフックへ手を入れるのは、
別の機会に行う」）。

`対策済` の行は書かない。

## 実行環境の記録

- **`echo hook-canary` は今回も deny されず通った。** pr-72 と同じく、
  `.claude/settings.json` に `PreToolUse` フックが 1 つも配線されていない
  （canary 自体が #50 として未実装）ため、「フック不発」ではなく
  **「canary が無いので層 3 が効いているか判定できなかった」**が正確
- **git hooks（層 2）は発火した。** push の出力に `pre-push` の各検査が出ている
- **CI（層 1）は success。** チェックは `fmt / clippy / test` の 1 本
- push 前検査は `cargo fmt --check` / `cargo clippy --all-targets -- -D warnings` /
  `cargo test` / `cargo doc --no-deps` / `cargo build` を **1 つずつ独立に実行**し、
  すべて exit 0
- **Discord 通知は今回も送れていない。** `DISCORD_WEBHOOK_URL` が未設定。
  pr-67 / pr-69 / pr-72 から変わらず
- **`harness-record` スキルは未移植**（#55）。この記録も `README.md` と `TEMPLATE.md` を
  読んで手で書いた
- **レビュー 0 件でマージされた。** pr-72 に続いて 2 回連続。指摘の出どころが
  自己修正だけになる回が続いている
- テストは 100 から **120**（112 + 8）に増えた
  （`import` 13 / `module_distance` 5 / 統合 2）
- **ブランチの空き待ちが 2 回発生した。** #14 はアサインの webhook で起動したが、
  そのときセッションの指定ブランチを PR #72 が使用中で着手できず、
  #72 のマージ後も記録の PR #73 が挟まったため、着手までにルーティンの起動を
  2 回またいだ。1 ブランチ = 1 PR で回している運用と、マージ後に記録の PR を
  1 本挟む手順が重なると、次の実装の着手が 2 サイクル遅れる。
  **事実のみ記録する**（運用の変更は棚卸しの管轄）
