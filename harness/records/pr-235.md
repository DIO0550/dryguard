# PR #235 signal_names_of が引用符なしのシグナル名を返すようにし、完全一致で比べる

- マージ日: 2026-09-24
- 関連 Issue: #230
- 差分規模: 1 ファイル / +8 -8

## ゴールと結果

Issue #230 のゴールは「`report::json` のテストヘルパー `signal_names_of` が、doc どおり
シグナル名（引用符なし）を返し、それを使う assert が完全一致で比べる」。**達成した。**

- `reason["signal"].to_string()` → `reason["signal"].as_str()`。戻り値を `Vec<&str>` にした
- 呼び出し側 `test_json_of_without_explain_omits_a_signal_it_did_not_ask_for` の assert を
  `contains`（部分一致）から `names.contains(&"...")`（完全一致）へ寄せた

**ずれ**: 無し。Issue が挙げた対象（1 箇所）は `grep -rn '\]\.to_string()' src tests` で
数え直し、1 件だけであることを確かめてから計画に入った（`AGENTS.md`「実装を始める前に」）。

## 指摘

**0 件。** CI（`fmt / clippy / test`・`差分規模の宣言`）は最初の push（`c2ff4db`）で両方 success、
Copilot のレビューは `Findings: None`。自己修正も無い。

## 手戻り

無し。計画どおり 1 コミット（`c2ff4db`）で、push も 1 回。

## うまくいったこと

- **直す前と直した後の両方で mutation を走らせた**（`rules/testing.md`
  「迷ったら、その 1 件だけ実装を壊して走らせる」）。`src/report/json.rs:207` の
  `"structural-similarity"` を `"structural-similarity-v2"` に書き換えると、直す前（`contains`）は
  対象テストが `1 passed`、直した後は `1 failed`。**Issue が挙げた穴を実際に塞いだこと**を
  直した後だけでなく、直す前に通ってしまうことまで含めて確かめた
- **`cargo doc` の失敗を main と突き合わせた。** `RUSTDOCFLAGS='-D warnings' cargo doc --no-deps
  --document-private-items` は exit 101 で落ちるが、`^error` の行数は main と同じ 14 件
  （既存のもの）。総数だけを見て「落ちた」と書かず、差が 0 であることを確かめた

## 実行環境メモ

- `echo hook-canary` は deny されずに通った。`.claude/settings.json` に `PreToolUse` は
  配線されておらず（`SessionStart` のみ）、**canary が無いので層 3 を判定できなかった**
- ルーティンが指定した `implementation-flow` スキルは、この実行環境では見つからなかった
  （`/home/user` 配下と `~/.claude/skills` を探した）。ルーティンの指示に書かれた段階を
  そのまま踏んだ

## 規約への反映

**無し。** 指摘が 0 件で、介入の材料が無い。集計と介入の判断は棚卸しの回が持つ
（`harness/records/README.md`「数えるのと、規約やフックへ手を入れるのは別の回に行う」）。
