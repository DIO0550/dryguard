# PR #244 #49 層 3: git push を見つけたら push 前の検査を走らせるフックを足す

- マージ日: 2026-09-26
- 関連 Issue: #49
- 差分規模: 7 ファイル / +336 -2（コミット 5 本・マージを除く）

## ゴールと結果

Issue #49 のゴール「`PreToolUse`（Bash）で `git push` を検知し、層 2 と同じスクリプトで
fmt / clippy / test を走らせる」は達成した。

| Issue の要件 | 結果 |
|---|---|
| 層 2 と同じスクリプトを呼ぶ | `.claude/hooks/pre-push-check.sh` が `harness/githooks/pre-push` をそのまま呼ぶ |
| `&&` で連結された push にもマッチさせる | `&&` / `\|\|` / `;` / `\|` / `&` / 括弧 / 改行で区切って先頭を見る（`.claude/hooks/lib/git-push-command.sh`） |
| cargo が使えない環境では黙ってスキップ | `harness/githooks/pre-push` 自身が通す。加えて jq が無い・入力が読めない・git の作業ツリーでないときも通す |
| enforcement として数えない | `.claude/hooks/README.md` とフックの冒頭に書いた |

**Issue に無かった変更が 2 つ入った。**

- **`AGENTS.md`「実装を始める前に」の hook-canary の項目を直した。** この PR で `PreToolUse` が
  配線されるが、canary を deny するフック（#50）はまだ無い。「`PreToolUse` が配線されているか」を
  確かめる書き方のままだと、次のセッションが canary が通ったことを不発と誤断定する
- **フックのテストを CI（`.github/workflows/rust.yml` の `claude-hooks`）で走らせるようにした**

## 指摘

レビューコメント・レビューは 0 件（`get_review_comments` / `get_reviews` / `get_comments` で確かめた。
PR コメントは自分が書いたテストケースの表 1 件だけ）。CI は 3 つのチェックがすべて 1 回目で通った。
以下はすべて自己修正で、Issue #49 のコメント（実装中の判断と手戻り）に push の前に書き残したもの。

### 1. 単引用符の除去を消してもテストが落ちなかった

- 分類: `test-assert-weak`
- 出どころ: 自己修正
- 内容: 入力が `echo 'a && git push'` で、除去しなくても区切った先が `push'` になり `push` と
  一致しなかった。実装を壊して走らせて気づき、入力を `echo 'a; git push origin'` に直した
- 既存ルール: rules/testing.md「迷ったら、その 1 件だけ実装を壊して走らせる」
- 次にどう防ぐか: 今回は記録のみ（規約どおりに壊して見つけられた）

### 2. 検査の出力を stderr へ流さなくしてもテストが落ちなかった

- 分類: `test-assert-weak`
- 出どころ: 自己修正
- 内容: テストが `2>&1` で stdout も受けていた。PreToolUse が exit 2 で Claude に返すのは
  stderr だけなので、stderr だけを受ける形に直した
- 既存ルール: rules/testing.md「assert は『落ちうるか』で見る」
- 次にどう防ぐか: 今回は記録のみ

### 3. 計画に書いた「git の作業ツリーでなければ黙って通す」にテストが無かった

- 分類: `test-coverage-gap`
- 出どころ: 自己修正
- 内容: Issue の計画コメントに書いた条件のうち 1 つがテストから参照されていなかった。
  テストを足し、確認を消すと落ちることを確かめた
- 既存ルール: rules/testing.md「振る舞いをテストする」
- 次にどう防ぐか: 今回は記録のみ

### 4. 計画コメントに Claude Code のフックの既定のタイムアウトを確かめずに書いた

- 分類: `unverified-claim`
- 出どころ: 自己修正
- 内容: Issue #49 の計画コメントの「タイムアウト」の行に「既定のまま: 既定を超えうる」と
  書いたが、既定値をこのセッションで確かめていなかった。README とコミットでは
  「長くかかるので既定に頼らず明示する」と書き、Issue に訂正のコメントを残した
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

## 手戻り

- PR 本文の下書きに「層 2 は push 時に走った」と書いたが、push の出力は末尾 5 行しか
  見ておらず確かめていなかったので、PR を作る前に消した（投稿前なので指摘には数えない）
- `.claude/hooks/README.md` は計画では「README」とだけ書いていたが、`rules/coding.md` /
  `rules/testing.md` が `.claude/hooks/README.md`「強制力の序列」を指しているのにファイルが
  無かったので、その見出しを置いて `AGENTS.md` へ渡す形にした

## うまくいったこと

- **実装を 6 箇所壊して、狙ったテストが 1 件ずつ落ちることを確かめた**（区切り文字・単引用符の
  除去・`-C` / `-c` の読み飛ばし・`exit 2`・stderr へのリダイレクト・作業ツリーの確認）。
  指摘 1 / 2 はここで見つかった
- **判定を `lib/` に切り出し、フックの end-to-end は呼ばれたら印を残す偽の検査で確かめた。**
  本物の cargo を毎回走らせずに、「呼ばずに通した」と「呼んで通った」を区別できた
- **実リポジトリでもフックを stdin の JSON から走らせ**、`pre-push: fmt` / `clippy` / `test` が
  stderr に出ることを確かめた
- **計画・却下した案・手戻りを PR の前に Issue へ書いた**

## 実行環境メモ

- `echo hook-canary` は着手時と push 前の 2 回とも deny されずに通った。`.claude/settings.json` の
  `PreToolUse` はこのセッションの起動時点では配線されておらず（`SessionStart` のみ）、
  **canary が無いので層 3 を判定できなかった**
- `core.hooksPath` は `harness/githooks` に配線されていた
- ルーティンが指定した `implementation-flow` スキルは、この実行環境では見つからなかった
  （pr-235 / pr-240 と同じ）。手順を手で踏んだ
- PR #243（`harness/records/pr-242.md` だけを変える PR）にはレビューコメント・レビュー・
  PR コメントが 0 件だった（`harness/records/README.md`「記録だけの PR には記録を作らない」の
  併記の対象は無い）

## 規約への反映

**無し。** 自己修正の 4 件はどれも既存の規約で見つけて直せた。`AGENTS.md` の hook-canary の
項目は書き換えたが、再発への介入ではなく、この PR で `PreToolUse` を配線したことに記述を
合わせたもの。集計と介入の判断は棚卸しの回が持つ
（`harness/records/README.md`「数えるのと、規約やフックへ手を入れるのは別の回に行う」）。
