# PR #178 短い関数の過大評価を測り直し、下限を採らない理由を残す

- マージ日: 2026-09-12
- 関連 Issue: #86（完了条件を満たした）/ #182（この回に分離して登録）
- 差分規模: 5 ファイル / +35 -10
  （`git diff --stat ada5494 68166b5`）

## ゴールと結果

Issue #86 のゴールは「短い関数で構造類似度が過大に出る」を直すこと。**直す実装は入れていない。
測り直したら、既に解けていた。**

| ゴール（Issue #86 の完了条件） | 結果 |
|---|---|
| `allocate` ↔ `renderInvoiceEmail` が候補に上がらない | **達成済みだった。** 構造類似度 0.44（既定の閾値 0.50）で候補外 |
| 妥当だった例（`overdueInvoices` ↔ `itemsInWarehouse`）が候補に残る | **達成済みだった。** 0.79 で候補・`DO-NOT-EXTRACT` |
| 判定ルールを触るので、先にテストを書いて Red にする | **触るルールが無かったので回していない**（下記） |

**Issue が挙げた「検討の方向」の 3 つ目（`tree-sitter` の AST 正規化を入れてから測り直す。
これで解ける可能性があるので、先に確かめる）に従って先に確かめた結果**、構造類似度が
正規化トークンの集合 Jaccard から正規化トークン列の 3-gram（出現回数つき）へ変わった時点で
この形は消えていた。`tests/corpus/src` の全 1081 ペアを既定の閾値 0.50 で測り直すと、
**短い側が 3 行のペアの候補率 1.8%（340 ペア中 6）に対し、10 行のペアは 56.9%（51 ペア中 29）**で、
短さが有利に働く向きが逆転している。

**完了条件は既に層 1 に載っていた。** `GRAM_LENGTH` を `3` → `1`（**並びを見なくなる**。
出現回数は `gram_counts_of` が数えたまま `min` / `max` で突き合わせるので**多重集合の Jaccard**で、
旧実装の集合 Jaccard そのものではない ─ この言い換えはレビューで直した。下記「この回の記録自身に
付いた指摘」）に壊すと `allocate` ↔ `renderInvoiceEmail` は 0.44 → 0.64 に戻り、
`tests/compare.rs::test_compare_of_two_short_functions_without_a_shared_shape_is_review` と
`tests/scan.rs::test_scan_of_the_corpus_leaves_out_a_pair_that_is_not_structurally_similar`
（対照つき）が落ちる。確かめてから、同じ振る舞いを見るテストは足さないと決めた。

**したがってこの PR は doc コメントと Markdown だけ**で、却下した案の理由（`GRAM_LENGTH` の
Why not）と、測り方が変わって古くなった記述の修正に絞った。

**残った形は #182 として分離した。** 本体が 1 文だけの関数どうしは、シグネチャと `return` の
骨格が gram の多くを占めるため、いまも 0.50〜0.53 で候補に乗る（`shouldRetry` ↔ `roundToCents`
0.53 など 3 組）。判定自体は `REVIEW` / `DO-NOT-EXTRACT` に落ちているので、直す動機は
候補列挙のコスト（Stage 2 の LSP 問い合わせ）だけ。`AGENTS.md`「設計判断の確認」に当たるため
選択肢と根拠を Issue に出して判断を仰ぎ、許可を得てから登録した。

## 指摘

**この PR に 6 件**（レビュー 4 件・自己修正 2 件）。

レビューは Codex が 2 巡（`277a3e4` に 3 件・`e4ceb1c` に 1 件。いずれも P2。`68166b5` に対する
巡は走っていない ─ `get_reviews` が返す Codex のレビューは 2 件のみ）。**6 件のうち 5 件が
「書いた主張を一次情報から出し直していない」形**で、コードの欠陥は 1 件も無い
（この PR がコードのロジックを変えていないため）。

**形が同じでも分類は射程で分かれる**（下の各ブロックの `既存ルール`）。内訳は
`なし` 3 件（指摘 1 / 2 / 6）・`comment-mismatch` 2 件（指摘 3 / 4）・`unverified-claim` 1 件（指摘 5）。
**この分け方はこの記録へのレビューで直した**（当初は 5 件を `unverified-claim` にしていた。
下記「この回の記録自身に付いた指摘」）。

### 1. 候補率に、使った閾値を書いていなかった

- 分類: `なし`
- 出どころ: レビュー（Codex、`277a3e4` の巡、P2）
- 内容: `tests/corpus/README.md` に「短い側が 3 行のペアの候補率は 1.8%、10 行のペアは 56.9%」と
  書いたが、候補かどうかは構造類似度の閾値で決まるのに閾値を書いていなかった。**3-gram という
  測り方だけからは再現できない数値**で、閾値を測り直した後は、この数値がまだ成り立つのかを
  読む側が判断できない。`bdb460e` で「既定の閾値 0.50 で」を明記し、閾値を測り直したら
  数値も出し直すことになる旨を足した
- 既存ルール: **なし。** `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」は形は同じだが
  **射程が違う**（列挙しているのは記録・PR 本文・Issue コメント・コミットメッセージ・レビュー返信・
  通知で、**リポジトリのドキュメント（README）は入っていない**）。`comment-mismatch` の射程は
  `rules/coding.md` のコード内 doc コメントなので、そちらでもない
- 次にどう防ぐか: 今回は記録のみ（`なし` として規約の抜けの候補に数える）

### 2. 「足すのは自由だが」が、走査全体の数を固定しているテストと食い違っていた

- 分類: `なし`
- 出どころ: レビュー（Codex、`277a3e4` の巡、P2）
- 内容: `tests/corpus/README.md`「CI が見ている関数がある」節に `tests/scan.rs` を足したが、
  同じ節の「**足すのは自由だが**」を残していた。`tests/scan.rs` は名指しの 4 関数だけでなく
  **走査全体の数**を固定している（`test_scan_of_the_corpus_compares_every_pair_of_chunks_it_found`
  が関数 47・総ペア 1081、`test_scan_of_the_corpus_keeps_every_candidate_pair_it_can_reach` が
  候補 65）ので、**関数を 1 つ足しただけで CI が落ちる**。この 2 本はこのセッションで既に
  読んでいたのに、節を直すときに突き合わせていなかった。`bdb460e` で「走査全体の数も CI が
  固定している」節を新設し、「足すのは自由だが」を削った
- 既存ルール: **なし**（対象が README。**指摘 1 と同じ射程の違い**）
- 次にどう防ぐか: 今回は記録のみ（`なし` として規約の抜けの候補に数える）

### 3. 集合で測っていた前提の rustdoc が残っていた

- 分類: `comment-mismatch`
- 出どころ: レビュー（Codex、`277a3e4` の巡、P2）
- 内容: `harness/phase0/chunk-unit-probe/README.md` を 3-gram の説明に直したのに、同じ crate の
  `print_size_distribution` の rustdoc が「短い単位ほど構文語がトークン**集合**を占める」のまま
  残っていた。この probe は現在の `TokenSequence` をそのまま使うので、**doc だけが古いと
  計測をやり直した人が結果を旧実装の偏り（短いほど高く出る）で読む**。`e4ceb1c` で直した。
  指摘の 1 箇所だけでは次の巡で同じ指摘が出るので、`トークン集合` で repo 全体を grep して
  同型を掃き出した（同 crate の `BLOCK_KINDS` と `src/classification/signal.rs` の
  `Signals::structural_similarity`。`harness/records/` を除き残り 0）
- 既存ルール: `rules/coding.md`「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ

### 4. 「最小トークン数の下限」を却下する理由を、行数で書いていた

- 分類: `comment-mismatch`
- 出どころ: レビュー（Codex、`e4ceb1c` の巡、P2）
- 内容: 却下した案は最小**トークン数**の下限なのに、`GRAM_LENGTH` の Why not に
  「同じ長さの真陽性（`taxFor` ↔ `netOf`）を先に削る」と**行数**で書いていた。`tokens_of` で
  数えると `taxFor` 37 / `shouldRetry` 27 で同じ長さではなく、**その間に下限を置けば
  疑わしいペアだけを外せる**。つまり**書いた却下理由が成り立っていなかった**。実測は
  行数でも取っていたので、そちらの数字で理由まで書いてしまった形。`68166b5` で
  「下限を偽陽性（`allocate` 49 トークン）に届く高さにすると真陽性（`taxFor` 37 / `netOf` 43）が
  先に消える」に書き換えた（却下の結論は変わらない）。**同じ誤った理由を Issue #86 の
  コメントにも書いていた**ので、訂正コメントを出して選択肢の表も直した
- 既存ルール: `rules/coding.md`「コメントは doc と Why / Why not に絞る」。**直した artifact は
  `GRAM_LENGTH` の rustdoc の Why not で、その内容が実測と食い違っていた**ので指摘 3 と同じ分類。
  同じ理由を書いた Issue コメントは `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」の射程内だが、
  `unverified-claim` の定義が「他の分類に当たる指摘はそちらを優先」と言っているのでこちらを採る
  （**この分類はレビューで直した**。当初 `unverified-claim` にしていた）
- 次にどう防ぐか: 今回は記録のみ

### 5. PR 本文で、`--ignored` を走らせるのが「CI の別ジョブ」だと書いていた

- 分類: `unverified-claim`
- 出どころ: 自己修正
- 内容: PR 本文に「`cargo test -- --ignored` は CI の**別ジョブ**が入れて走らせる」と書いたが、
  `.github/workflows/rust.yml` を読み直すと**同じ `check` job の後段のステップ**だった
  （job は `check` 1 つだけ）。ワークフローを開かずに「別ジョブだろう」と書いていた。
  CI の結果を確かめた回に気づいて本文を訂正し、**ignored テストも CI では走って通っている**
  ことを追記した
- 既存ルール: `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」（PR 本文は射程内）
- 次にどう防ぐか: 今回は記録のみ

### 6. 計測の足場が、`Debug` 文字列を数えてトークン数を 1.7 倍に見積もっていた

- 分類: `なし`
- 出どころ: 自己修正
- 内容: トークン数を数える足場を `format!("{:?}", tokens)` の中の `Identifier` / `Literal` /
  `Syntax` の出現数で書いたため、`Syntax(SyntaxKind("..."))` が **`Syntax` を 2 回含む**
  （`SyntaxKind` の中にもある）ぶんを二重に数えていた。`allocate` を 85 と見積もっていたが、
  `tokens_of(node).len()` の実測は 49。**公開した数値には届いていない**（README・Issue・PR 本文に
  出したのは行数の表だけ）が、指摘 4 でトークン数を出し直すときに気づいた。以後は
  `Debug` 文字列を数えず `tokens_of` の長さを使った
- 既存ルール: **なし。** `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」は「書く直前に
  一次情報から出し直す」で、**一次情報を作る道具が間違っている場合**を言っていない
- 次にどう防ぐか: 今回は記録のみ（`なし` として規約の抜けの候補に数える）

## 手戻り

**実装の手戻りは無し**（実装を入れていない）。記述の手戻りが**修正コミット 3 本**。
**巡は 2 回**で、1 巡目の 3 件を 2 本に分けて直したため本数と一致しない
（この数え方はレビューで直した。下記「この回の記録自身に付いた指摘」）。

| 修正コミット | 答えた巡 | 何を直したか |
|---|---|---|
| `bdb460e` | 1 巡目（`277a3e4` への 3 件）のうち 2 件 | `tests/corpus/README.md` の 2 箇所（閾値の明記・走査全体の数） |
| `e4ceb1c` | 1 巡目の残り 1 件 | rustdoc 3 箇所（集合前提の説明） |
| `68166b5` | 2 巡目（`e4ceb1c` への 1 件） | `GRAM_LENGTH` の Why not（却下理由の根拠を行数からトークン数へ） |

**旧実装（集合 Jaccard）の再現は途中で捨てた。** #86 が「16 件中 3 件」と書いた族を数え直そうと
旧実装を再現したが、同じコーパスで 55 ペア（記録は 65）・`allocate` ↔ `renderInvoiceEmail` が
0.83（記録は 0.93）にしかならず、**正規化そのものも同じ回に変わっていた**ため再現とは呼べないと
判断して公開物には使わなかった。**現在の測り方での実測だけで完了条件を確かめる形に切り替えた。**

## うまくいったこと

- **実装に入る前に「本当に直す必要があるか」を測った。** Issue が「先に確かめる」と書いていた
  とおりに確かめたら、実装が不要だった。測らずに最小トークン数の下限を実装していれば、
  真陽性を削る変更を入れていた
- **既存テストが完了条件を守れているかを、実装を壊して確かめた**
  （`rules/testing.md`「迷ったら、その 1 件だけ実装を壊して走らせる」）。「テストがあるから
  大丈夫」で済ませず `GRAM_LENGTH` を壊して 2 本が落ちることを見たので、**テストを足さない
  判断に根拠が付いた**
- **レビュー指摘を 1 箇所で終わらせず、同型を grep で掃き出した**（指摘 3）。指摘された
  probe の rustdoc 以外に 2 箇所あり、次の巡での再指摘を避けられた
- **判断を仰ぐ形を守った。** #182 に当たる形は実装せず、選択肢と根拠を Issue に出して
  許可を得てから登録した（`AGENTS.md`「設計判断の確認」）

## 規約への反映

**介入は無し。** 既存の条文に当たる 3 件は、**どちらの分類も専用の条文をすでに持っている**。

| 分類 | 件 | 条文 |
|---|---|---|
| `comment-mismatch` | 指摘 3 / 4 | `rules/coding.md`「コメントは doc と Why / Why not に絞る」 |
| `unverified-claim` | 指摘 5 | `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」（`TEMPLATE.md` の層の語彙で言う `rules`） |

同じ層へもう一度書いても強制力は上がらないので、この回は材料を残すところまでにする。

**`なし` が 3 件出た。** これが規約の抜けの候補で、2 つの形に分かれる
（`TEMPLATE.md`「`分類: なし` が 10 件溜まったら語彙へ昇格させる」）。

| 件 | どの条文にも当たらない理由 |
|---|---|
| 指摘 1 / 2 | 対象が**リポジトリのドキュメント（`tests/corpus/README.md`）**。`AGENTS.md` の節が列挙するのは記録・PR 本文・Issue コメント・コミットメッセージ・レビュー返信・通知で、README は入っていない。`comment-mismatch` の射程はコード内 doc コメントなのでそちらでもない |
| 指摘 6 | 対象が**一次情報を作る足場**。あの節は「書く直前に一次情報から出し直す」で、出し直す道具が間違っている場合を言っていない |

**当初この 3 件を `unverified-claim` に寄せていたが、レビューで直した。** 根拠に挙げた
`pr-172` の併記 33 は**記録の中の数の食い違い**で、README を対象にした主張の先例ではない。
射程の外を射程内の分類に数えると、**`count.sh` が既存条文の再発として 3 件を足し、
抜けのほうは見えなくなる**（下記「この回の記録自身に付いた指摘」）。

**層を上げるかの判断はこの回では行わない**（`harness/records/README.md`「数えるのと、規約や
フックへ手を入れるのは別の回に行う」）。

## この回の記録自身に付いた指摘

**この記録（PR #183）に Codex が 4 件（すべて P2）。すべて指摘どおりでこのファイルを直した。**

| 直したもの | 内容 |
|---|---|
| 指摘 4 の分類 | `unverified-claim` → **`comment-mismatch`**。直した artifact は `GRAM_LENGTH` の rustdoc で、`unverified-claim` の定義が「他の分類に当たる指摘はそちらを優先」と言っている |
| 指摘 1 / 2 / 6 の分類 | `unverified-claim` → **`なし`**。射程の外と自分で書きながら射程内の分類を付けていた |
| ミューテーションの説明 | `GRAM_LENGTH` を `1` にしても**出現回数は残る**ので多重集合の Jaccard で、旧実装の集合 Jaccard の再現ではない。示せたのは「テストが並びの効き方を守っている」ことまで |
| 手戻りの数え方 | 修正コミット 3 本を「3 巡」と書いていた。**巡は 2 回**で、1 巡目の 3 件を 2 本に分けて直していた |

**この 4 件はここでは数えない。** `harness/records/README.md`「記録だけの PR には記録を作らない」に
従い、**次に実装が入る PR の記録へ併記する**（PR #183 の回の指摘として）。
