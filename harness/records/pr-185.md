# PR #185 推論された型名が倒れる向きを偽陰性から偽陽性へ直す

- マージ日: 2026-09-12
- 関連 Issue: #173
- 差分規模: 1 ファイル / +8 -1

## ゴールと結果

**Issue のゴールは達成していない。この PR はその手前で止めたもの。**

#173 は「決めること」として案 A / B / C を並べ、「いずれにせよ `rules/architecture.md` の
表を触るので、`AGENTS.md`「設計判断の確認」に従って判断をお願いします」で終わっている。
**着手時点で判断待ちだったので、コードの挙動には触れていない。**

| やったこと | 結果 |
|---|---|
| 判断の前提だった「実コーパスでどれくらい減るか」を測る | 測った（下記） |
| どの案を採っても誤りである doc の記述だけを直す | この PR |
| 案 A / B / C の実装 | **未着手**（判断待ち。#173 にコメントで判断を依頼した） |

### 測った結果（#173 のコメントに全文）

rxjs 7.8.1 の `src`（251 ファイル / 961 チャンク / 尋ねた 517）で、`TypeSignatureOutcome` が
`Normalized` になるのは 360 件。案 A をレビューの文言どおり入れると 142 件（39.4%）が
「測れない」へ動く。

**このうち 79 件は推論された型名ではなく型変数が原因だった。** `Callable::type_names()` は
型変数を付け替える前に数えるので `<T>(x: T) => T` が `{"T"}` を返す一方、
`type_references_of` は束縛された名前を意図的に集めない（辿る相手が居ない）ため、
「辿った記録が無い型名」に入ってしまう。束縛した型変数を除く案（#173 で案 A' として提示）
なら 63 件（17.5%）に収まる。

**この材料は `docs/dryguard-plan.md`「Stage 2」を読んで初めて決め手になった** —
「ジェネリクスは単一化を試みる」と正面からぶつかる。

## 指摘

**この PR に 3 件**（レビュー 1 巡: `28d53db` の head に付いた Codex P2 が 2 件。
`bf044cf` に対する Codex のレビューは走っていない）。**自己修正が 1 件**。
CI は**この branch の 2 回とも `success`**（Rust run 365 / run 368）で、CI 由来の指摘は無い。

### 1. 「集められなかった型名」が、このモジュールが持たないと明言している状態を指していた

- 分類: `naming-vocabulary`
- 出どころ: レビュー（Codex P2）
- 内容: 足した doc に「**集められなかった型名**は綴りのまま比べられ」と書いた。だが
  **同じファイルの `type_references_of` の doc**（`src/syntax/type_reference.rs:101-102`）が
  「**空は「書かれていない」。**…集められなかったという状態は無い（構文木からは必ず採れる）」と
  書いており、**1 つのファイルの中で語彙が食い違っていた**。推論された型名はそもそも
  `type reference` ではないので「集め損ねた」ものではなく、集める側を直せば拾えるかのように
  読めてしまう（直前の文が「集める側を変えても動かない」と正しく書いているのと矛盾する）
- 既存ルール: rules/naming.md「このツールの語彙を固定する」
- 次にどう防ぐか: 今回は記録のみ。**規約はあり、守れていなかった回**。とくに
  「同じファイルの別の doc が既にその語を否定していないか」を見ていなかった

### 2. 「開けなかった型名が「測れない」へ倒れる」の参照先が別の型で、限定も落ちていた

- 分類: `comment-mismatch`
- 出どころ: レビュー（Codex P2）
- 内容: 2 つの誤り。(a) 参照先を `semantics::resolved_type` の `UnopenedTypeName` と
  書いたが、これは**型名 1 つと開けなかった理由の記録**（`resolved_type.rs:127`）で、
  「測れない」という答えは `semantics::type_signature` の
  `TypeSignatureOutcome::UnopenedTypeName`（`type_signature.rs:130`）のほう。
  **同名の型が 2 つあり、doc から追うと別の型に行き着く**。(b) `single_outcome_of` は
  `remaining_type_names` に現れた型名しか `unopened_reason_of` に掛けないので、
  開けなかった型名がすべて「測れない」になるわけではない
  （`rules/architecture.md`「どこまでを「取れなかった」に数えるか」の表のとおり）。
  限定を落としたまま書いたので、推論された型名との対比を過大に述べていた
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ。**doc に型名を書くときに、その綴りの定義が
  リポジトリに 1 つしか無いかを確かめていなかった**（`UnopenedTypeName` の定義は
  `resolved_type.rs:127` の struct と `type_signature.rs:130` の enum バリアントの 2 つ。
  綴り自体は `src/` の 7 ファイル・39 行に現れる）

### 3. レビュー対応の push 後、PR 本文が指摘 2 で直したその記述を引用したままだった

- 分類: `unverified-claim`
- 出どころ: 自己修正
- 内容: PR 本文の「足した記述は 3 点」の 2 点目が
  「開けなかった型名（`semantics::resolved_type` の `UnopenedTypeName`）が…」と、
  **指摘 2 で誤りと判明したまさにその綴りを引用していた**。`bf044cf` を push した後に
  気づいて本文を更新した。`AGENTS.md`「記録・PR 本文に数値や主張を書く前に」が
  「関連する変更が入った後・最終提出の前にも出し直す」と定めている形そのもの
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」
  （pr-129 指摘 9・pr-138 指摘 5 と同じ「本文が古くなる族」）
- 次にどう防ぐか: 今回は記録のみ。規約はあり、**push の前ではなく後に気づいた**

## 手戻り

**1 巡目の doc に 2 つ誤りを入れた（指摘 1 / 2）。** 直す対象が「doc の 1 文が事実と違う」
ことだったのに、**書き換えた側にも事実と違う記述を入れた**。差分 8 行に対して指摘 2 件は
密度が高い。原因は、書いた内容を既存の doc と `grep` で突き合わせなかったこと。

**スコープは着手直後に変えた（手戻りではない）。** Issue を読んだ時点で判断待ちと分かったので、
実装に入らず「測る」→「どの案でも誤りな doc だけ直す」に絞った。コードは書いていないので
捨てた作業は無い。

**計測用のプローブは出荷しないと決めた。** `harness/` のクレートとして出すには
`semantics::type_signature` と `pipeline` の private を外へ出すことになり、
`rules/architecture.md`「`pub` は最小限にする」とぶつかる。プロダクトと同じ経路を通すために
一時的な枝としてクレート内に置き、計測後に revert した（patch は #173 のコメントに残した）。

## うまくいったこと

- **`AGENTS.md`「実装を始める前に」の「計画を立てる前に `docs/dryguard-plan.md` の該当節を
  読む」が効いた。** 「Stage 2」の「ジェネリクスは単一化を試みる」が、案 A を却下する
  決め手になった。読んでいなければ数字だけで「39% は許容範囲か」を議論していた
- **測ってから判断を返した。** レビューの提案（案 A）をそのまま実装していたら、
  Stage 2 のシグナルを 39.4% 失っていた。しかもその主因は Issue が問題にしていた
  推論された型名ではなく型変数で、**測らなければ原因の取り違えに気づけなかった**
- **プローブをプロダクトと同じコード経路に載せた。** 別実装にすると出た差が
  「案の違い」なのか「測り方の違い」なのか読めなくなる
  （`harness/phase0/chunk-unit-probe/README.md`「なぜ別クレートか」と同じ考え）
- **指摘 2 件とも、直す前にコードで裏を取ってから受けた**（`grep -rn UnopenedTypeName src/`、
  `single_outcome_of` の実装、`type_references_of` の doc）

## 実行環境メモ

**層 3 は判定できなかった。** `echo hook-canary` は deny されずに通ったが、
`.claude/settings.json` に `PreToolUse` の配線が無い（`grep -c PreToolUse` → `0`。
`SessionStart` の `wire-githooks.sh` のみ）。**canary が無いので「フックが発火しない環境」
とは断定していない**（`AGENTS.md`「実装を始める前に」。pr-95 / pr-97 がこの誤断定を
`分類: plan` で記録している形）。push 前検査は層 1 / 層 2 が担っており、
`cargo fmt --check` / `cargo clippy --all-targets -- -D warnings` / `cargo test` /
`cargo test -- --ignored` を 1 つずつ独立に走らせた（すべて exit 0）。

LSP を要するテストは、`pnpm install --frozen-lockfile` で
typescript-language-server 6.0.0 / typescript 5.9.3 を入れて 36 件すべて実行した。

## 規約への反映

**無し。** 3 件とも既存の規約がある族（`naming-vocabulary` / `comment-mismatch` /
`unverified-claim`）で、**規約の抜けではなく守れていなかった回**。
`AGENTS.md`「規約の更新」は「ルールに書かれていない指摘が 2 回以上」を介入の条件に
しているので、ここでは材料を残すところまでにする。回数を数えて介入を決めるのは別の回
（`harness/records/README.md`「記録は溜まり始めている」）。
