# PR #175 🐛 [Bug fix]: #170 アクセサの綴りを呼べる型として割るのをやめる

- マージ日: 2026-09-12
- 関連 Issue: #170
- 差分規模: 10 ファイル / +262 -1

## ゴールと結果

Issue #170 のゴールは「アクセサ（getter / setter）の型シグネチャが、取れないだけでなく
**関数型のときは誤って取れる**（偽陽性）」を止めること。Issue は案 A / B / C を挙げて
判断を仰ぐ形で終わっていたので、着手時にセッション URL を残したうえで実測を取り直し、
判断を仰いだ。**案 C（理由を足して、`SplitSignature` で割る前に落とす）**で進めることになり、
偽陽性は止まった。

**ずれ: 案 A-3（`TypeSignature` が持つのを `Callable` ではなくチャンクの型の enum にする）は
この回に入れていない。** Issue #120 が挙げた「取れない形を減らす」には届いておらず、
アクセサのシグナルは引き続き付かない。#176 に切って引き継いだ（下の「手戻り」ではなく、
着手前に決めたスコープ）。

## 指摘

### 1. 統合テストの入力が、見たい振る舞いに届く前に降りていた

- 分類: `test-assert-weak`
- 出どころ: CI（`cargo test -- --ignored`）/ レビュー（Codex P1）
- 内容: `test_compare_with_an_lsp_does_not_compare_a_getter_as_a_callable_type` が
  `Unavailable { reason: NotACandidate }` で落ちた。フィクスチャの構造類似度が閾値
  （0.50）に届かず、`measured_pair_of` が LSP を起こす前に降りていた。LSP を使わない側で
  測り直すと 0.303。**綴りの読み取り（このテストが見たいもの）には一度も届いていなかった。**
  ゲッターは引数を取れず本体をメソッドと同じ形にできないため、セッターへ変えたうえで
  3 つのフィクスチャの本体を揃えて 0.649 にした（対照側は 0.652 → 0.830）。
  CI と Codex が同じ 1 箇所・同じ原因を指しているので 1 ブロックにまとめた
  （**別の欠陥ではない**。Codex のレビュー対象は 1 つ前のコミット `7e78665` で、
  指摘が届いた時点では `74dd706` で修正済みだった）。
- 既存ルール: rules/testing.md「assert は『落ちうるか』で見る」/
  「『出ない』を確かめるときは、対照を 1 件置く」（Codex も L52-58 を引いている）。
  規約が書いている形は「入力に該当するものが無いのに通ってしまう」で、今回は
  **同じ「入力が振る舞いに届いていない」だが落ちる側**に出た
- 次にどう防ぐか: 今回は記録のみ。**再発（2 回目）。** `pr-138.md` の「手戻り」が
  同じ形を**逆向き**で記録している（「候補ペアが出ない入力」を選んだつもりが、
  構造類似度 1.00 のペアが 1 組あって候補が残り、入力を差し替えた）。どちらも
  **Stage 1 の候補判定という述語を確かめずに統合テストの入力を選んだ**もの。
  ただし `pr-138.md` 側は**手戻りとして書かれていて `分類:` が付いていない**ので、
  `count.sh` には現れない（PR #177 のレビューで Codex が指摘するまで、この記録も
  「初出」と書いていた）。**介入するかは棚卸しの担当**で、この回では判断しない

### 2. アクセサの理由が、相手側の理由に隠れていた

- 分類: `missing-signal`
- 出どころ: レビュー（Codex P2）
- 内容: `type_signature_match_of` のアクセサの枝を最後に置いたため、片側が
  `AccessorSignature`、もう片側が `UnopenedTypeName` のときに後者が出ていた。利用者は
  型エイリアスを開けるように直して走らせ直すが、アクセサ側は扱えないままなので
  **直しても同じペアは測れない**。枝を、両側が正規化できた枝の直後へ移した。
  1 件だけ動かすと `NoTypeThere` / `HoverNotProvided` と組んだときに同じことが起きるので、
  並びに「**環境を直しても変わらない理由を先に出す**」という軸を入れ、関数の doc と
  回帰テストで固定した
- 既存ルール: rules/architecture.md「取れなかったシグナルを既定値で埋めない」の
  「理由は落とさない」（**利用者が次に何をするか**を理由が持つ）。Codex も L87-89 を引いている
- 次にどう防ぐか: 今回は記録のみ。**再発（確認できたかぎり 3 回目）。** 規約は
  「理由を落とすな」までは書いているが、**理由が複数あるときにどれを出すか**は書いていない。
  同じ形が既に 2 件ある。
  - `pr-158.md` 指摘 13（`分類: over-guard`）: `references` に答えないサーバで、
    `ReferencesNotProvided` と出すべき場面で**所属のほうを理由に出していた**
    （「利用者が直す先を取り違える」と書かれている。今回とまったく同じ帰結）
  - `pr-169.md` 指摘 69（`分類: error-variant`）: 早期 return が宣言を見る前に
    `ReboundSpelling` を返し、2 つの理由の優先順が doc と食い違っていた

  **3 件が 3 つの別々の分類に散っている**（`over-guard` / `error-variant` / `missing-signal`）。
  1 件の指摘は `分類:` を 1 つしか持てず `count.sh` はその行しか数えないので、
  **この族は今の語彙では数えられない**。語彙を足すか既存へ寄せるかは棚卸しの担当で、
  この回では判断しない。今回の実装では関数の doc に軸を置いてある。
  **Codex に指摘されるまで、この記録も「初出」と書いていた**（数えずに書いた）

### 3. Discord へ送れていないのに「送った」と書いた

- 分類: `unverified-claim`
- 出どころ: 自己修正
- 内容: 通知を送るシェルで `curl ... && echo "discord: sent"` と書いたため、
  Discord が `{"content": ["Must be 2000 or fewer in length."]}` を返して**本文が
  投稿されていない**のに `discord: sent` が出て、そのままセッションの返信にも
  「送信しました」と書いた。**curl の終了コードは HTTP のエラーを表さない。**
  次から `-w '\nHTTP:%{http_code}'` を付けて状態コードを見るようにし、2 通に分けて
  送り直して 204 を確認した
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」（**通知**が対象に入っている）
- 次にどう防ぐか: 今回は記録のみ。**`pr-172.md` 指摘 8 とまったく同じ原因**
  （「送信ヘルパーが **API の応答を見ずに `curl` の終了コードだけで成否を出していた**」）。
  向きだけが逆で、あちらは送れているのに「未送信」と断定して二重投稿、こちらは
  送れていないのに「送信した」と報告した。
  あちらも書いている**「通知の送信は使い捨てのシェル関数なので、リポジトリに残る仕掛けには
  なっていない（ルーティンの指示に書かれたコマンド例そのものが応答を見ない形）」**が
  そのまま当てはまる。仕掛けを置くかは棚卸しの担当

## 手戻り

**1 件。** 統合テストのフィクスチャを、ゲッター 1 ファイル + 相手 2 ファイルで書いた後、
**セッターへ変えて 3 ファイルとも本体を書き直した**（指摘 1）。計画の段階で
「アクセサをチャンクにして hover に尋ねる」までは確かめていたが、**その手前に
Stage 1 の候補判定がある**ことを確かめていなかった。

`rules/naming.md` への `accessor` の追加・実装の置き場所（6 箇所）は計画から動いていない。

## うまくいったこと

- **assert を壊して確かめた。** `is_accessor_spelling` を常に `false` / 常に `true` に
  して走らせ、3 件 / 126 件が落ちることを見た（`rules/testing.md`「迷ったら、その 1 件だけ
  実装を壊して走らせる」）。理由の並びについても、枝を元の位置へ戻して落ちることを見た
- **直す前に、純粋な側で偽陽性を再現した。** 実サーバが無い環境でも
  `normalized_outcome_of` へ綴りを直接渡せるので、E2E が使う綴りそのもの
  （`(setter) Holder.handler: (message: string) => void` と
  `(method) Notifier.notify(value: string): void`）で `unifiable = true` を確かめてから
  テストを足せた。`rules/tdd.md`「`lsp` は『応答を受け取ってから先』を切り出す」が
  効いている
- **CI の失敗を、CI を回さずに再現した。** 指摘 1 の `NotACandidate` は Stage 1 の話なので
  LSP 無しで測れた。閾値との余白（0.556 では 1 割しか無い）まで見て 0.649 に直せた
- **Issue が判断を仰ぐ形で終わっていたので、実装に入らず止めた。** 止める前に実測を
  取り直し、Issue が書いていなかった 4 点（偽陽性が `is_unifiable_with` まで届く /
  Stage 1 → Stage 2 の経路がつながる / `(property)` はずれない / A-1 の前提）を足せたので、
  判断の材料が増えた状態で再開できた

## 規約への反映

**無し。ただし「初出だから」ではない。**

3 件とも過去の記録に同じ形があり（上の各「次にどう防ぐか」）、`AGENTS.md`「規約の更新」の
「2 回以上出たら規約の抜けとして扱う」には届いている。**それでもこの回では介入しない**のは、
**数えるのと規約やフックへ手を入れるのは別の回に行う**と決まっているため
（`harness/records/README.md`「記録は溜まり始めている」）。この回は材料を残すところまで。

棚卸しの回へ渡す材料:

| 指摘 | 過去の同じ形 | 今の語彙での見え方 |
|---|---|---|
| 1 | `pr-138.md` の手戻り（逆向き） | **`count.sh` に出ない**（あちらは `分類:` を持たない手戻りとして書かれている） |
| 2 | `pr-158.md` 指摘 13 / `pr-169.md` 指摘 69 | **3 つの別々の分類に散る**（`over-guard` / `error-variant` / `missing-signal`） |
| 3 | `pr-172.md` 指摘 8 | `unverified-claim` に集まっている |

**`対策済` の行は書かない。** この回は介入していないうえ、`count.sh` はその行を
「ここから数え直す」の起点に読むので、介入していない回に書くと再発数が 0 に戻る
（`harness/records/TEMPLATE.md`）。

**`unverified-claim` に `対策済` の行はまだ 1 件も無い**（`grep -rh "^- 対策済:"` で確認。
記録に入っている介入は `naming-vocabulary` / `naming-mismatch` / `layer-dependency` /
`smart-constructor` / `over-guard` の 5 行だけ）。`AGENTS.md`「記録・PR 本文に数値や主張を
書く前に」は規約としては置かれているが、**記録の側に起点が無いので `count.sh` は
通算しか出せない**。これも棚卸しへ渡す材料として書いておく。

`rules/naming.md` へ `accessor` を足しているが、これは**指摘への対策ではなく**
語を増やしたときの手続き（`rules/naming.md`「新しい語を足したら、この表にも足す」）なので、
ここには数えない。

## 実行環境メモ

- **層 3（Claude Code hooks）は判定できなかった。** `echo hook-canary` は deny されずに
  通ったが、`.claude/settings.json` に配線されているのは `SessionStart`
  （`wire-githooks.sh`）だけで `PreToolUse` が無い。**canary が無いので層 3 を
  判定できなかった**（`AGENTS.md`「実装を始める前に」の pr-95 / pr-97 の項目）
- typescript-language-server が入っていない環境だったため、`tests/semantics.rs` の
  `#[ignore]` テストはローカルで一度も走っていない。実際に走ったのは CI の
  `cargo test -- --ignored`（`35 passed; 0 failed; 0 ignored`）
