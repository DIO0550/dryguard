# PR #199 尋ねたかを型名の綴りではなく出現ごとに見る（注釈を省いた値の型を「尋ねていない」に倒す）

- マージ日: 2026-09-13
- 関連 Issue: #187（切った Issue: #198 / #200）
- 差分規模: 15 ファイル / +937 -62

## ゴールと結果

**#187 のゴールは「出現ごとの出どころをどう持つか」を決めて塞ぐことで、Issue の推しは案 1
（戻り値の注釈の有無を `syntax` から運ぶ）だった。案 1 を採ったが、Issue が書いていた
「戻り値」では足りなかった。**

| ゴール | 結果 |
|---|---|
| 出どころの持ち方 | **案 1 を採用。** 案 2（位置つき）・案 3（同じ綴りが 2 回で測れない）は却下 |
| 運ぶ単位 | **「戻り値の注釈」ではなく `value type`（値の型）として立てた**（下記） |
| 実コーパスでいくら失うか | **2 件（0.4%）**（`Normalized` 316 → 314） |
| `rules/` 側の単位 | 「型名 1 つ」から「出現 1 つ」へ出し直した（`6f146a2`） |

### 「戻り値の注釈」では足りなかった

チャンクの宣言が注釈を省ける位置は**形ごとに違う**。setter は引数の型が hover の綴りになり、
コンストラクタは囲むクラスの宣言が綴りを書く。「戻り値の注釈が無ければ倒す」で書いていたら、
**注釈を書いてある setter と全コンストラクタがまとめて測れない側へ落ちていた**。
語彙として `value type` / `value type annotation` を立て、`rules/naming.md` に足した。

### 実コーパスで測った効き

rxjs 7.8.1 の `src`（尋ねた 517 チャンク）。`compare` と同じ経路を辿る一時プローブで、
変更前（`fd3fbd6`）と head（`c9bf00b`）を同じ手順で測った（プローブはコミットしていない）。

| | 変更前 | head |
|---|---|---|
| `Normalized` | 316 | **314** |
| `UntracedTypeName` | 40 | **42** |
| `UnopenedTypeName` | 122 | 122 |
| `UnreadableSignature` | 33 | 33 |
| `SiteDependentSpelling` | 5 | 5 |
| `NoTypeThere` | 1 | 1 |

チャンク単位で差分を取ると**動いたのはちょうど 2 チャンク**（`internal/Observable.ts:488` /
`internal/testing/TestScheduler.ts:134`）。倒れる向きは偽陰性。

## 指摘

**この PR に 17 件**（レビュースレッド 14 本 = 14 件 + 自己修正 3 件）。レビューはすべて
Codex で **8 巡**（`6f146a2` 2 件 / `8ce6306` 3 件 / `ce158a8` 3 件 / `d06140e` 1 件 /
`d49ece8` 1 件 / `adba5f8` 1 件 / `71c3a99` 2 件 / `c9bf00b` 1 件）。**P1 が 5 本、P2 が 9 本。**

CI は**この branch の Rust ワークフロー 8 回すべて `success`**（run 402〜409）で、
**CI 由来の指摘は無い**。

### 1. 値の型の型名を、エイリアスを差し込んだ後で数えていた

- 分類: `missing-signal`
- 出どころ: レビュー（`6f146a2`・P1）
- 内容: 差し込みも綴りで引くので、引数に注釈したエイリアスの右辺が**同じ綴りの、推論された
  戻り値にも入る**。差し込んだ後で数えると値の型の位置に型名が残らず、`inferred_value_type` が
  `false` のままになり、**エイリアス越しに #187 の偽陽性がそのまま通っていた**。
  `compare` で再現を確かめてから `8ce6306` で直した（差し込む前の綴りから数える
  `spelled_value_type_names_of`）
- 既存ルール: rules/architecture.md「取れなかったシグナルを既定値で埋めない」
  （確かめられなかったことを確かめた答えとして出していた）
- 次にどう防ぐか: 今回は記録のみ。`rules/architecture.md` に「差し込む前の綴りで数える」を
  同じ PR で足してある

### 2. `satisfies` を「注釈が書かれている」に数えていた

- 分類: `enum-omission-direction`
- 出どころ: レビュー（`6f146a2`・P1）
- 内容: 値を通すだけの包みの一覧（`syntax::tree` の `TRANSPARENT_EXPRESSION_KINDS`）を
  そのまま流用していたが、**`satisfies` は照らし合わせるだけで推論された型を残す**
  （`tsc 5.9.3` の宣言出力で実測）。`Written` へ倒れるので偽陽性側。`8ce6306` で
  **型を言い切る包みの許可リスト**（`TYPE_ASSERTION_KINDS`）に切り替え、漏れが偽陰性へ
  倒れる形にした
- 既存ルール: rules/coding.md「列挙で判定を組むときは、漏れの倒れる向きを選ぶ」の
  **「同じ一覧を、安全な倒れ方が違う 2 箇所で使い回さない」**（pr-148 指摘 1 と同じ形）
- 次にどう防ぐか: 今回は記録のみ

### 3. 引数を足した回に公開 API の doc を出し直していなかった

- 分類: `comment-mismatch`
- 出どころ: レビュー（`8ce6306`・P2）
- 内容: `type_signature_outcome_of` の rustdoc が、この PR で消した引数
  （`overload_positions`）と消した項目（`Chunk::overload_name_positions`）を指したままで、
  リンクとしても切れていた。`value_type` / `overloads` の説明は無かった。`ce158a8` で直した
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ

### 4. 複数を返す helper を `_at` で名付けていた

- 分類: `naming-mismatch`
- 出どころ: レビュー（`8ce6306`・P2）
- 内容: `overload_declarations_at` が `Vec<OverloadDeclaration>` を返していた。`_at` にしたのは
  既存の `overload_declarations_of`（構文木のノードを返す）との衝突を避けたためだが、
  **衝突していたのは返すものが違う 2 つが同じ語彙を使っていたから**。`ce158a8` で語彙のほうを
  分けた（`overload_declarations_of` / `overload_declaration_nodes_of`）
- 既存ルール: rules/naming.md「複数をまとめて返すものは `<返すもの>_of`」
- 次にどう防ぐか: 今回は記録のみ

### 5. 値の型の扱いが、観測できる側で固定できていない件があった

- 分類: `test-coverage-gap`
- 出どころ: レビュー（`8ce6306`・P2）
- 内容: `Callable::value_type_names` を直接見るテスト 4 件について「中間表現を固定している」
  という指摘。**採ったのは半分**。この関数は private ではなく `pub(crate)` で、
  同じ形のテストが `fd3fbd6` の時点で 7 件あるため、4 件だけ見方を替えるとモジュール内で
  割れる（残した）。一方で 4 件のうち **1 件（外側で束縛された型変数）だけが
  `normalized_outcome_of` の側で固定されていなかった**ので、`ce158a8` でそれを足した
- 既存ルール: rules/testing.md「振る舞いをテストする」
- 次にどう防ぐか: 今回は記録のみ。**既存 7 件ごと観測側へ寄せるかはレビュー返信で問いに
  戻してあり、スレッドは未解決のまま**（マージ時点）

### 6. 文脈から型付けされたオブジェクトのメンバーで benign を失った

- 分類: `over-guard`
- 出どころ: レビュー（`ce158a8`・P2）
- 内容: `const obj: I = { f(x: Receipt) { … } }` の形で、`fd3fbd6` では `単一化可能` だったものが
  `測れない` になる、という指摘。**回帰が起きているのは事実**だったが、`fd3fbd6` と `ce158a8` を
  benign / adversarial の 2 通りで測ると、**まったく同じ構文の adversarial 側でこの PR が
  偽陽性を塞いでいた**。提案どおり `Written` に倒すと adversarial が戻るので、
  測った表を添えて**現状のまま残す判断**にした（正しく閉じるのは #133 の担当）
- 既存ルール: なし（漏れの向きは rules/coding.md の規約どおり偽陰性なので、規約違反ではない。
  分類が `over-guard` なのは、**基点で取れていた答えをこの PR の新しい関門が落とした**
  という効きが pr-158 指摘 10 / pr-191 指摘 3 と同じ形だから。残す判断をしたこととは別に、
  落としたこと自体は数える）
- 次にどう防ぐか: 今回は記録のみ

### 7. setter の引数リストのコメントを引数として数えていた

- 分類: `over-guard`
- 出どころ: レビュー（`ce158a8`・P2）
- 内容: tree-sitter ではコメントが引数リストの**名前付きの子**になる（実測で確認）。
  「ちょうど 1 つ」の突き合わせが外れ、**注釈を書いてある setter が「省かれている」側へ
  落ちていた**。`d06140e` で引数のノードだけを選ぶようにした（`PARAMETER_KINDS`）
- 既存ルール: なし（構文木の子の読み方の誤りで、規約違反ではない。分類が `over-guard` なのは、
  **この PR の新しい関門が、注釈を書いてあるチャンクまで落としていた**ため）
- 次にどう防ぐか: 今回は記録のみ

### 8. 対照のフィクスチャが「注釈を足しただけ」になっていなかった

- 分類: `test-assert-weak`
- 出どころ: レビュー（`ce158a8`・P2）
- 内容: `annotated.ts` は `echoed.ts` と**本体まで違って**おり、共有の型どうしの易しい組を
  比べていた。**注釈の有無だけで答えが変わることを確かめられていない**ので、実装を壊しても
  通りうる。`d06140e` で `localReceipt.ts` を足し、`diff` が戻り値の注釈 1 行だけになる形に
  作り直した
- 既存ルール: rules/testing.md「assert は「落ちうるか」で見る」
- 次にどう防ぐか: 今回は記録のみ

### 9. 宣言と代入が離れている形で、宣言側の注釈を見ていなかった

- 分類: `over-guard`
- 出どころ: レビュー（`d06140e`・P2）
- 内容: `let build: T; build = (x) => …` は代入の式に注釈が載らないので「省かれている」に
  落ち、`fd3fbd6` では `単一化可能` だった組を失っていた（実測）。`d49ece8` で代入先を
  宣言まで辿る形を入れた。**この形は最終的に `c9bf00b` で丸ごと外している**（下記 12）
- 既存ルール: なし（分類が `over-guard` なのは、基点で取れていた答えをこの PR が落としたため）
- 次にどう防ぐか: 今回は記録のみ

### 10. 宣言側の注釈を認めたのに、そこに書かれた型名を集めていなかった

- 分類: `なし`
- 出どころ: レビュー（`d49ece8`・P2）
- 内容: `d49ece8` は宣言側の注釈を「書かれている」と数えたが、**そこに書かれた型名を
  収集側へ渡していなかった**ので尋ねる位置が作られなかった。`fd3fbd6` でも測れない形なので
  回帰ではないが、**非対称は `d49ece8` が作ったもの**なので同じ回で閉じた（`adba5f8`）
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### 11. 「宣言が無い」と「宣言はあるが注釈が無い」を同じ値で表していた

- 分類: `illegal-state`
- 出どころ: レビュー（`adba5f8`・P1）
- 内容: スコープを辿る側が `Option` 1 つで受けていたため、**隠している内側の宣言を飛び越えて
  外側の別の変数の注釈を拾っていた**。構造の違う 2 つのローカル `Receipt` で実測すると
  `単一化可能`（偽陽性）。`71c3a99` で 3 値の enum（`DeclaredBinding`）にし、飛び越えられない
  形にした
- 既存ルール: rules/coding.md「不正な状態を型で表現できなくする」
- 次にどう防ぐか: 今回は記録のみ

### 12. 型名の隠蔽で、代入先の注釈の参照を綴りで捨てていた

- 分類: `なし`
- 出どころ: レビュー（`71c3a99`・P1）
- 内容: 指摘の分析はそのとおりだった。**ただし個別には直さず、`c9bf00b` で「代入先を宣言まで
  辿る」形ごと外した。** 実コーパスで測ると、この形（9 / 10 / 11 を含む 3 コミット）が
  取り戻した `Normalized` は **0 件**（動いたのは 1 チャンクが `UntracedTypeName` →
  `UnopenedTypeName` へ移っただけで、どちらも「測れない」）。**測れる答えが増えないのに、
  指す先を綴りだけで決める経路だけが増えていた**
- 既存ルール: なし（`syntax` にスコープ解決を持たせようとしたのが根。指す先を決めるのは
  `semantics` の担当というのは `rules/architecture.md` のモジュールの表から読めるが、
  「依存方向」ではないので `layer-dependency` には当たらない）
- 次にどう防ぐか: 今回は記録のみ。外した理由は `outer_type_decides_the_spelling` の
  Why not に残した

### 13. 引数への代入を「宣言が無い」として扱っていた

- 分類: `over-guard`
- 出どころ: レビュー（`71c3a99`・P2）
- 内容: `function configure(build: (x: Receipt) => Receipt) { build = …; }` の束縛を
  `Absent` として扱っていた。**12 と同じ巡・同じ族**なので、`c9bf00b` の revert で
  経路ごと無くした（この形は `Omitted` = 測れないのまま残る）。**指摘 15 の訂正で測り直した
  とおり、benign 側は `fd3fbd6` では `単一化可能` だった**ので、6 / 7 / 9 と同じく
  基点で取れていた答えを落としている
- 既存ルール: なし（根は 12 と同じ。分類が `over-guard` なのは 6 / 7 / 9 と同じ理由で、
  **12 は偽陽性の側なのでこちらには入らない**）
- 次にどう防ぐか: 今回は記録のみ

### 14. 「注釈を省ける位置は値の型だけ」という前提が正しくなかった

- 分類: `plan`
- 出どころ: レビュー（`c9bf00b`・P1）
- 内容: **既定値つき引数も注釈を省ける。** `function echo(received = buildLocal()): Receipt` は
  戻り値が書かれているので `value_type` が `Written` になり、判定を素通りする。実測すると
  `fd3fbd6` でも head でも `単一化可能`（**前からある穴で、この PR では塞がっていない**）。
  #187 の Issue 本文も、この PR の設計も、**省ける位置がチャンクにつき 1 つだという前提**で
  書かれていた。小さく塞ぐ案（引数が 1 つでも省いていたら値の型も倒す）を試作して測ると
  `Normalized` が 314 → 310 で、**このコーパスでは 1 件も塞げなかった**ので、#200 として
  切り出した
- 既存ルール: AGENTS.md「実装を始める前に」の**「計画の入力を鵜呑みにしない」**
  （Issue が挙げた対象の一覧は「調べた結果」であって網羅の保証ではない）
- 次にどう防ぐか: 今回は記録のみ。塞ぎ方（案 2: 引数を添字で対応させる）と測った結果は
  #200 に書いた

### 15. レビュー返信に、実測していない主張を書いた

- 分類: `unverified-claim`
- 出どころ: 自己修正
- 内容: 13 への返信で「`fd3fbd6` の時点でも `UntracedTypeName` だった形なので回帰では
  ありません」と書いたが、**測らずに書いたもの**だった。測り直すと `fd3fbd6` では
  benign / adversarial の**両方が `単一化可能`** で、正しくは「一度も測れていない」ではなく
  「**測れていたが、偽陽性と区別できない形だった**」。同じスレッドに訂正を投稿した
  （判断そのものは変わらない）
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」
  （**レビュー返信**が名指しで射程に入っている）
- 次にどう防ぐか: 今回は記録のみ

### 16. Issue の本文に、確かめていないコメント ID のリンクを書いた

- 分類: `なし`
- 出どころ: 自己修正
- 内容: #200 の「参照」に書いた指摘へのリンクの ID が、実際のもの（`r3999834948`）ではなく
  推測した値だった。**書いた先は Issue の本文**（コメントではない）で、Issue を編集して直した
  （作成 14:12:57 → 更新 14:14:04）
- 既存ルール: なし。形は `unverified-claim` そのものだが、AGENTS.md「記録・PR 本文に数値や
  主張を書く前に」が名指しで数えている先は**記録・PR 本文・Issue コメント・コミットメッセージ・
  レビュー返信・通知**で、**Issue の本文は入っていない**。`AGENTS.md`「実装の進め方」は
  計画・却下した案・その理由を Issue に書くと決めており、**数や主張がいちばん集まる場所の
  1 つが射程の外にある**（規約の抜けの候補）
- 次にどう防ぐか: 今回は記録のみ。射程を広げるかは棚卸しの回の判断

### 17. コミットメッセージに書いた既存テストの件数が 1 件ずれていた

- 分類: `unverified-claim`
- 出どころ: 自己修正（**この記録を書く過程で、一次情報から数え直して見つけた**）
- 内容: `ce158a8` のコミットメッセージに「このモジュールの既存のテスト **6 件**が
  `type_names` / `traceable_type_names` を同じ形で見ており」と書いたが、`fd3fbd6` を
  数え直すと **7 件**（`git show fd3fbd6:src/syntax/type_structure.rs | grep`）。
  同じ回に投稿したレビュー返信のほうは 7 件を名前ごと列挙しており、**ずれたのは
  コミットメッセージだけ**。コミットメッセージは書き換えないので、ここに記録として残す
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」
  （**コミットメッセージ**が名指しで射程に入っている）
- 次にどう防ぐか: 今回は記録のみ

## 手戻り

**「代入先を宣言まで辿る」形を 3 コミット（`d49ece8` / `adba5f8` / `71c3a99`）かけて入れ、
`c9bf00b` で丸ごと外した。** 指摘 9 → 10 → 11 → 12 / 13 と**4 巡続けて**同じ族の指摘が出ており、
巡が進むほど倒れる向きが悪くなっていた（9 / 10 は偽陰性、11 / 12 は偽陽性）。

**外す判断の材料は「取り戻せた答えが 0 件」という実測**で、入れた時点では測っていなかった。
9 に着手した時点で「この形が実コーパスで何件の答えを取り戻すか」を先に測っていれば、
3 コミット分の往復は避けられた。

**「取り戻せた答えが 0 件」は「その形が 1 件も現れなかった」ではない。** この経路は実コーパスで
1 チャンクに効いており（`internal/operators/timeoutWith.ts:100` が `UntracedTypeName` →
`UnopenedTypeName`）、どちらも「測れない」なので **`Normalized` が 1 件も増えなかった**、
というのが測った内容。0 件だったのは `let build: T;` `build = (x) => …` の形が
`Normalized` を取り戻した数で、構文としての出現数は数えていない。

2 つ目は指摘 15。**実測していない主張をレビュー返信に書き、次の巡で自分で訂正した。**

## うまくいったこと

- **指摘を確かめてから直した。** 内訳は 3 つに分かれる。
  **(a) 再現を取ってから動いた 10 件**（1 / 2 / 6 / 7 / 8 / 9 / 10 / 11 / 13 / 14）— 直す前に
  `compare` か tree-sitter か `tsc` で前後を測り、レビュー返信に表で載せている
  （**13 だけは順番が逆で、測らずに返信したあと指摘 15 として自分で訂正し、そこで測った**）。
  **(b) ソースと履歴を読んで確かめた 3 件**（3 / 4 / 5）— 走らせて再現する種類ではない
  （3 は消したリンク先、4 は返り値と名前、5 は既存テストの本数を `fd3fbd6` から数え直した）。
  **(c) 再現を取らなかった 1 件**（12）— 分析を認めたうえで**経路ごと外した**ので、
  個別の再現は取っていない。代わりに測ったのは**その経路が実コーパスで取り戻した答えの数**
  （0 件）で、それが外す判断の材料になっている。
  指摘 6 / 13 はこの実測の結果**指摘どおりに直さない**判断になり、指摘 14 は**試作して
  測った結果**スコープ外へ切り出した
- **benign と adversarial を対で作った。** 「失う形」と「塞ぐ形」が**構文上まったく同じ**で
  あることを表で出せたので、直すか残すかの判断が数字で決まった（指摘 6 / 13 / 15）
- **修正ごとに、回帰テストが落ちうるかを確かめた。** 修正を戻すと、その回に足したテストの
  **少なくとも 1 件が落ちる**ことを実際に走らせて確認している
  （`rules/testing.md`「迷ったら、その 1 件だけ実装を壊して走らせる」）。
  **「足したテストがすべて落ちる」ではない** — `d49ece8` は 3 件足して落ちるのは 1 件、
  `71c3a99` は 2 件足して落ちるのは 1 件目だけで、残りは**対照として置いたテスト**
  （落ちない側であることに意味がある。`rules/testing.md`「「出ない」を確かめるときは、
  対照を 1 件置く」）
- **スコープ外を Issue にして閉じた。** #198（hover が `progress` を待たない）と #200
  （既定値つき引数）を測った結果ごと切り出し、#187 を閉じた

## 規約への反映

**無し。** このセッションの担当は**記録を追記するところまで**で、集計（`count.sh`）・規約や
skill の変更・介入判断は行わない（棚卸しの回の管轄。`harness/records/README.md`
「記録は溜まり始めている」）。

**材料として残しておく分:**

- **`over-guard` が 4 件**（指摘 6 / 7 / 9 / 13）。どれも**この PR の新しい関門が、基点
  `fd3fbd6` では取れていた答えを落とした**形（pr-158 指摘 10 / pr-191 指摘 3 と同じ効き）。
  倒れる向きが規約どおり偽陰性であることは、落としたことを数えない理由にはならない
- **`分類: なし` が 3 件**（指摘 10 / 12 / 16）。10 / 12 は 9 / 13 と**同じ族**
  （`syntax` にスコープ解決を持たせようとした形）で、この族は 4 巡続けて出ている。
  16 は下記の射程の穴
- **`unverified-claim` が 2 件**（指摘 15 / 17）。どちらも `AGENTS.md`「記録・PR 本文に
  数値や主張を書く前に」が名指しで射程に入れている先（レビュー返信・コミットメッセージ）で、
  **17 はこの記録を書くために数え直して初めて見つかった**
- **Issue の本文が `unverified-claim` の射程に入っていない**（指摘 16）。同じ規約が
  Issue **コメント**は名指ししているのに本文は挙げておらず、`AGENTS.md`「実装の進め方」は
  計画と却下した案を Issue に書くと決めている。射程を広げるかは棚卸しの回の判断
- **層 3 は判定できなかった。** `echo hook-canary` は deny されずに通ったが、
  `.claude/settings.json` に配線されているのは `SessionStart` だけで **`PreToolUse` は 1 つも
  無い**（`grep -c PreToolUse .claude/settings.json` = 0）。canary が無いので
  「フックが発火しない環境」とは断定できない（`AGENTS.md`「実装を始める前に」）
