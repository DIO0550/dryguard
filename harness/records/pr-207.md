# PR #207 シグネチャ自身の引数を指す typeof を「場所に依存する綴り」から外す

- マージ日: 2026-09-18
- 関連 Issue: #192
- 差分規模: 8 ファイル / +271 -41

**この記録は PR #205 の回の指摘も併記する**（`harness/records/README.md`「記録だけの PR には
記録を作らない」— PR #205（`933d54f`）は `harness/records/pr-203.md` だけを変える PR で
（`git diff --stat 17983d5 933d54f` が 1 ファイル）、この PR #207 がその次に実装が入った PR に当たる）。

## この記録が数える件数

**`- 分類:` の行は 9 本**（`count.sh` が数えるのはこの行）。

| 回 | 件数 | 内訳 |
|---|---|---|
| PR #207 | **2** | レビュー 0 + 自己修正 2 |
| PR #205 | **7** | レビュー 5（すべて P2）+ 自己修正 2（返信で次の回へ引き継ぐと自己申告したもの） |

分類ごとでは `unverified-claim` 6 / `なし` 2 / `missing-signal` 1。
**`対策済:` の行は置いていない**ので、`count.sh` の再発数の起点は動かない。

## ゴールと結果

**#192 のゴールは、`typeof x` の `x` がそのシグネチャ自身の引数を指すときに
`SiteDependentSpelling`（測れない）へ倒さないこと。** PR #191 が入れた判定が、
`rules/naming.md` の `site-dependent spelling` の定義（「指す先が**書かれた場所**で決まる」）より
広く倒れていた。

| ゴール | 結果 |
|---|---|
| 自分の引数を指す `typeof x` を測れる側へ戻す | **達成。** `Callable::bound_value_names` を足して walk に通した |
| 差し込みの可否（`is_site_independent`）を緩めるか | **緩めない**と決めた（Issue に根拠を追記） |
| どこで束縛を引くか | `Parameter` ではなく `Callable`。`renamed` で空にする |
| 引数名が違うだけの 2 つを重ねる | **スコープ外**（Issue 本文の判断どおり） |
| `rules/` 側の記述 | 同じ PR で出し直した（`d9767c8`） |

### 現象は基点と修正後の両方で実測してから閉じた

`typescript-language-server 6.0.0` / `typescript 5.9.3` を入れ、この PR が足した fixture 2 ファイルに
`compare --explain` を走らせた。

| 木 | `型シグネチャ` |
|---|---|
| `933d54f`（この branch の基点 = PR #205 マージ後の `main`） | `測れない (比較に残る綴りが書かれた場所で決まる)` |
| `d9767c8`（この PR の head） | `単一化可能` |

**Issue 本文が測った値を転記していない。** Issue は PR #191 の基点（`7cda1d5`）と head（`006c2a7`）で
測っており、木が違う。

### 実コーパスは測っていない

**rxjs 7.8.1 の `src` はこの実行環境に無いので、この回では測っていない。**
Issue 本文の「`SiteDependentSpelling` 5 件のうちこの形は 0 件」は Issue 側の実測で、
この PR が数え直した数字ではない。`rules/architecture.md` の 1.6% の記述にも触れていない。
PR 本文・Issue コメント・Discord 通知の 3 箇所すべてにこの断りを書いた。

## 指摘（この PR の回）

**2 件（レビュー 0 + 自己修正 2）。**

レビューは `copilot-pull-request-reviewer[bot]` の 1 巡のみ（`d9767c8`・
`#pullrequestreview-5246420243`）で、**"Comments generated: 0" / "I found no objective defects"**。
判定は 🔵 Needs a closer look（人の確認を求めるが、欠陥の指摘は無し）。
レビュースレッドは 0 本（`pull_request_read` の `get_review_comments` が `totalCount: 0`）。
**前回まで付いていた `chatgpt-codex-connector` のレビューは、この PR には付いていない**
（理由は分からないので推測を書かない）。

CI は**この branch の Rust ワークフロー 1 回が `success`**（run 35235325363。push は 1 回）で、
**CI 由来の指摘は無い**。

### 1. 引数の束縛を、型変数の制約・既定の型まで届かせていた

- 分類: `missing-signal`
- 出どころ: 自己修正（push 前の自己レビュー。commit には残っていない）
- 内容: `Callable::names_only_types_of` を最初に書いたとき、`bound | &self.bound_value_names` を
  **関数の先頭に置いていた**ので、束縛が型変数の制約と既定の型にも届いていた。
  **型変数は引数より先に宣言される**ので、`<T extends typeof x>(x: string) => T` の `typeof x` は
  外の値を指す。届かせたままだと**外を指す綴りを「束縛された名前」と読み、確かめていないものを
  `Unifiable` と答える**（偽陽性）。束縛を足す位置を引数の型と戻り値の直前へ下げ、
  回帰テスト（`test_a_callable_type_querying_a_value_in_a_type_parameter_constraint_does_not_name_only_types`）を
  足した。**そのテストは、束縛を先頭へ戻すと実際に落ちることを確かめてある**
- 既存ルール: rules/architecture.md「取れなかったシグナルを既定値で埋めない」の
  「どこまでを『取れなかった』に数えるか」（**そもそも型名でない → 「測れない」**）。**規約は在る。**
  `rules/coding.md`「同じ一覧を、安全な倒れ方が違う 2 箇所で使い回さない」も近いが、
  あちらは**一覧を共有する消費者どうし**の話で、ここは**同じ関数の中の位置ごと**にスコープが違う
- 次にどう防ぐか: 今回は記録のみ（規約への反映は棚卸しの管轄）

### 2. Discord 通知が 1 通に収まらず、最初の送信が落ちた

- 分類: `なし`
- 出どころ: 自己修正
- 内容: テストケースの表を含む通知を 1 通で送ったところ **http 400** が返った。
  小さい payload で webhook 自体は生きていること（http 204）を確かめてから、
  3 通（1516 / 1354 / 1445 文字）へ割って送り直し、**3 通とも 204 を確認した**。
  Discord のメッセージ上限（2000 文字）に当たっていた
- 既存ルール: なし。**通知の分量に関する規約が無い。** ルーティンの指示は
  「PR コメントと Discord 通知に、ファイルごとに次の表を書く」と定めているが、
  1 通に収まらない場合の扱いは書かれていない
- 次にどう防ぐか: 今回は記録のみ。**送る前に文字数を数えて割る**で潰せる形

## 指摘（PR #205 の回・記録だけの PR なので併記）

**7 件。** PR #205（`933d54f`）は `harness/records/pr-203.md` だけを変える PR で、
`harness/records/README.md`「記録だけの PR には記録を作らない」に当たる。

内訳は**レビュー 5 本**（`chatgpt-codex-connector`・すべて P2・すべて記録の正確さ）と、
**そのレビュー返信の中で「次に実装が入る PR の記録が引き受けます」と自己申告した自己修正 2 件**。
レビュー 5 本はすべて解決済み（`8da2a32` / `d1b95ee`）。

### 3. PR #201 の回の指摘を、記録へ併記していなかった

- 分類: `unverified-claim`
- 出どころ: レビュー（PR #205・P2・`#discussion_r4003061365`）
- 内容: PR #201（`4a4bf29`）は `harness/records/pr-199.md` だけを変える PR なので、
  その回の指摘 5 件は `pr-203.md` へ併記される決まりだった。**その確認をせずに書いたので、
  5 件が `count.sh` から見えないままだった。** `8da2a32` で併記し、`- 分類:` の行は 8 → 13 本になった
- 既存ルール: `harness/records/README.md`「推測で埋めない」/ AGENTS.md「記録・PR 本文に数値や
  主張を書く前に」。**規約は在る。** 前の PR が記録だけの PR かどうかは
  `git diff --stat` で機械的に確かめられる
- 次にどう防ぐか: 今回は記録のみ。**記録を書く前に、直前のマージが記録だけの PR かを
  `git diff --stat` で確かめる**で潰せる形

### 4. 3 回繰り返した自己修正を、1 ブロックに畳んでいた

- 分類: `なし`
- 出どころ: レビュー（PR #205・P2・`#discussion_r4003061372`）
- 内容: `b90e712` / `6619e05` / `99350b0` の 3 コミットで同じ形（レビュー URL を確かめずに置く）を
  繰り返したのに、1 ブロックへ畳んで「（3 回）」と括弧書きしていた。`count.sh` が数えるのは
  `- 分類:` の行なので、**括弧書きは数に入らず、3 回の再発が 1 回に見えていた**。
  `8da2a32` で 3 ブロックへ割った
- 既存ルール: `harness/records/README.md`「指摘 1 件 = 1 ブロック（まとめない）」。**規約は在る。**
  **分類を `なし` にしたのは規約が無いからではなく、README のこの節に紐づくタグが語彙に無いから**
  （`pr-203.md`「分類の語彙の穴」と同じ形）。`unverified-claim` へ寄せなかったのは、
  **件数は分かったうえで畳んでいる**ので「確かめずに書いた」に当たらないため
- 次にどう防ぐか: 今回は記録のみ

### 5. 規約が在るのに `既存ルール: なし` と書いた

- 分類: `unverified-claim`
- 出どころ: レビュー（PR #205・P2・`#discussion_r4003061378`）
- 内容: `pr-203.md` の指摘 2 / 3 は `rules/architecture.md`「3 ステージのパイプライン」の
  責務の表に当たるのに、`既存ルール: なし` と書いていた。**分類タグが無いことと、規約が無いことを
  混ぜていた**ので、棚卸しの回に「規約の抜けの族」と読まれる形だった。`8da2a32` で
  `既存ルール` を責務の表へ直し、分類の語彙の穴は別の小見出しへ分けた
- 既存ルール: `harness/records/README.md`「推測で埋めない」/ AGENTS.md「記録・PR 本文に数値や
  主張を書く前に」（`pr-203.md` 指摘 12「既にある分類を確かめずに `なし` と書いた」と同型で、
  こちらは**既にある規約**を確かめずに `なし` と書いた形）
- 次にどう防ぐか: 今回は記録のみ

### 6. 出力が変わる範囲を、実際より狭く書いた

- 分類: `unverified-claim`
- 出どころ: レビュー（PR #205・P2・`#discussion_r4003061381`）
- 内容: `pr-203.md` が「変わるのは `--explain` が出す文だけ」と書いていたが、
  `report_compare` / `report_scan` は常に `report::text_of` を描画し、`CommonOptions::explain` は
  **パース用テスト以外から読まれていない**。つまり `--explain` を付けない通常の
  `compare` / `scan` の出力も変わっていた。`8da2a32` で直し、根拠（`grep -n explain` と実測）を書いた
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 7. 記録を直した後、コミット本文の件数が古いまま残った

- 分類: `unverified-claim`
- 出どころ: レビュー（PR #205・P2・`#discussion_r4003131311`）
- 内容: 記録の件数を 6 件 → 8 件へ数え直したのに、`f5d4232` の本文は「指摘 6 件」「自己修正 2 件」の
  ままだった。**レビューが挙げたハッシュ `02bcb9f` は存在しない**（`git cat-file -t` が
  `fatal: Not a valid object name`）が、**指摘の中身は当たっていた**。
  履歴を書き換えると子の `8da2a32` のハッシュが変わるため、rebase はせず `d1b95ee` で
  **件数を記録そのものへ書いた**（「この記録が数える件数」の節）。
  **返信は「公開済みの 10 箇所から参照されている」と書いている**が、その内訳はこの回では数え直していない
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」の
  「書いた時点では正しかった主張が、後から入った変更で古くなることがある」
- 次にどう防ぐか: 今回は記録のみ

### 8. `8da2a32` のコミットメッセージのレビュー URL を、一次情報に当たらず書いた

- 分類: `unverified-claim`
- 出どころ: 自己修正（PR #205 のレビュー返信 `#discussion_r4003093578` で自己申告。
  「次に実装が入る PR の記録が引き受けます」と書かれている）
- 内容: `pr-203.md` の指摘 5 / 6 / 7 と同じ形の **4 件目**。レビュー返信を書いている最中に、
  `8da2a32` のコミットメッセージへ URL を確かめずに置いた。`pull_request_read` で取り直してから
  `--amend` で直したので**リモートには出ていない**
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」（コミットメッセージも射程）
- 次にどう防ぐか: 今回は記録のみ

### 9. `d1b95ee` のコミットメッセージのレビュー URL を、一次情報に当たらず書いた

- 分類: `unverified-claim`
- 出どころ: 自己修正（PR #205 のレビュー返信 `#discussion_r4003146842` で自己申告。
  「記録だけの PR なので、これも次に実装が入る PR の記録が引き受けます」と書かれている）
- 内容: 指摘 8 と同じ形の **5 件目**。指摘 7 に対応するコミットで同じことをした。
  `--amend` で直したのでリモートには出ていない。**同じ形が 5 回続いている**ので、1 件にまとめず
  別々に数える（指摘 4 が求めた形）
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ。**指摘に対応するコミットを書く前に、そのスレッドの ID を取る**で
  潰せる形（`pr-203.md` 指摘 7 が既に書いている手順）

## 手戻り

**実装の形（`Callable` が束縛を持ち、`renamed` で空にする）の手戻りは無し。** 計画どおりに通した。

手戻ったのは**束縛が届く範囲**で、1 度決め直した（指摘 1）。

| 巡 | 届く範囲 | なぜ外れたか |
|---|---|---|
| 1 | シグネチャ全体（型変数の制約・既定の型を含む） | 型変数は引数より先に宣言されるので、制約の `typeof x` は外を指す |
| 2 | **引数の型と戻り値だけ** | — |

**指摘ブロックにしていない手戻りが 1 件ある。** テストの呼び出しを新しい引数へ揃えるときに書いた
python の一括置換の正規表現が、`fn test_..._names_only_types() {` にも当たり、
テスト関数名を 3 箇所壊した。置換直後に出力を目で見て気づき、同じ手順の中で戻している。
**編集操作のミスであって判断の誤りではない**ので、指摘としては立てずにここへ書く。

## うまくいったこと

- **基点と修正後の両方を自分で実測した。** Issue 本文にも before / after の表があったが、
  測った木が違う（PR #191 の基点と head）ので転記せず、この branch の基点（`933d54f`）で測り直した
- **`#[ignore]` のテストを実際に走らせた。** `pnpm install --frozen-lockfile` で
  typescript-language-server を入れ、`cargo test -- --ignored` の 46 件を通した。
  CI 任せにせず、実サーバの hover がこの形をどう返すかを確かめてから統合テストを足せた
- **assert が落ちうるかを、境界を見る 3 件とも実際に壊して確かめた**（`rules/testing.md`
  「迷ったら、その 1 件だけ実装を壊して走らせる」）。1 件は**その確認の過程で指摘 1 を見つけた**
- **テスト件数を基点と修正後の両方で数えた**（lib 964 → 973 / `tests/semantics.rs` 45 → 46）。
  差分の `fn test_` を数えた 10 件と突き合わせて一致を確かめている
- **測っていないもの（rxjs の実コーパス）を、PR 本文・Issue コメント・Discord 通知の 3 箇所すべてで
  明記した。** Issue 側の実測を自分の測定として出さない形にした
- **判断の履歴は Issue、差分の説明は PR という分担を守った。** 却下した 3 案（`Parameter` が名前を持つ /
  綴り全体をもう一度読む / 綴りの中の引数名を自分で集める）とその理由は Issue #192 のコメントにある

## 規約への反映

**無し。**

この PR は `rules/naming.md` と `rules/architecture.md` を直しているが、それは
**#192 の修正で無効になる記述を出し直したもの**（AGENTS.md「実装を変えたら、rules/ 配下の記述も
同じ PR で出し直す」に沿った計画内の作業）で、再発を数えた結果の介入ではない。

**回数を数えて層を決めるのは棚卸しの管轄**なので、ここでは分類を付けて記録するに留める。
`対策済:` の行は置いていないので、`count.sh` の再発数の起点は動かない。

## 実行環境のメモ

- `typescript-language-server 6.0.0` / `typescript 5.9.3`（`pnpm install --frozen-lockfile` で導入。
  **解決は起きない**ので `harness/deps/README.md` の cooldown を迂回していない）。
  実サーバのテスト（`cargo test -- --ignored`、56 件）を**このセッションで実際に走らせている**
- **フックの発火は判定できなかった。** `echo hook-canary` は deny されずに通ったが、
  `.claude/settings.json` の `hooks` に配線されているのは `SessionStart` だけで
  **`PreToolUse` は無い**（`grep -rn "PreToolUse" .claude/` が 0 件）。
  したがって**canary が無いので層 3 を判定できなかった**（「フックが発火しない環境」とは断定しない）
- 層 2（`harness/githooks/pre-push`）は手で走らせ、fmt / clippy / test の 3 つとも通した
