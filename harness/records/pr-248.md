# PR #248 #50 層 3: echo hook-canary を必ず止めるカナリアのフックを足す

- マージ日: 2026-09-26
- 関連 Issue: #50
- 差分規模: 6 ファイル / +201 -4（コミット 5 本・マージを除く）

## ゴールと結果

Issue #50 のゴール「`echo hook-canary` を必ず deny するフックを置き、silent だったフックの
不発を detected に変える」は達成した。

| Issue の要件 | 結果 |
|---|---|
| `echo hook-canary` を必ず deny する | `.claude/hooks/hook-canary.sh` が `command` の値が `echo hook-canary` そのものなら exit 2 で止める。`.claude/settings.json` の `PreToolUse`（`Bash`）に配線 |
| 判定も出力も bash の組み込みだけで行う | `read` / `[[ =~ ]]` / `printf` / `command -v` だけ。PATH が空でも止めることをテストで確かめた |
| `jq` は在れば使う程度 | **`jq` では読まない**形にした（環境で判定経路が変わると答えが割れうるため）。`jq` は欠けているかを報告する用途だけ |
| 欠けていれば `jq` / `python3` の名前を出す | **`jq` / `cargo` に変えた**（下の指摘 1） |

**Issue に無かった変更が 2 つ入った。**

- **`AGENTS.md`「実装を始める前に」の hook-canary の項目を直した。** 「`PreToolUse` は
  canary を deny しない」と書いてあり、この PR で古くなるため。見るのを「セッションを
  起動した時点の配線」にした
- **テストを CI（`.github/workflows/rust.yml` の `claude-hooks`）で走らせるようにした**

## 指摘

レビューコメント・レビューは 0 件（`get_review_comments` / `get_reviews` / `get_comments` で確かめた。
PR コメントは自分が書いたテストケースの表 1 件だけ）。CI は 3 つのチェックがすべて 1 回目で通った。
以下は自己修正で、Issue #50 のコメント（実装中の判断と手戻り）に PR の前に書き残したもの。

### 1. 報告するコマンドの一覧を、フックが実際に呼ぶかを確かめずに計画へ書いた

- 分類: `plan`
- 出どころ: 自己修正
- 内容: Issue #50 の計画コメントの完了条件に「`jq` / `python3` のうち欠けているもの」と
  Issue の文言のまま書いた。実装中に `.claude/hooks/` と `harness/githooks/` を grep すると
  `python3` を呼ぶフックは無く、欠けると素通りするのは `jq` と `cargo` だった。一覧を
  `jq` / `cargo` に変え、Issue に訂正のコメントを残した
- 既存ルール: AGENTS.md「実装を始める前に」（計画の入力を鵜呑みにしない）
- 次にどう防ぐか: 今回は記録のみ（計画の後・実装の前に grep して気づけた）

## 手戻り

- push 前の `cargo test` を `| head` に通したら exit 101 が出た。パイプを閉じたことによるもので、
  出力をファイルに落として走らせ直すと exit 0（テストの失敗は 0 件）だった

## うまくいったこと

- **実装を 6 通り壊して、それぞれ狙ったテストが落ちることを確かめた**（exit 2 → 0・
  stderr へのリダイレクト外し・値の末尾の引用符の照合外し・空白の許容外し・`grep` への
  置き換え・`jq` の確認外し）。弱い assert は見つからなかった
- **PATH を偽のコマンドだけのディレクトリにして、`jq` / `cargo` の有無を作った。**
  本物の有無に結果を左右させず、CI でも手元でも同じ組み合わせを作れた
- **計画・却下した案・手戻りを PR の前に Issue へ書いた**

## 実行環境メモ

- `echo hook-canary` は push 前に deny されずに通った。このセッションは `main`（73a7369、
  canary の配線前）から起動しており、起動時点の `.claude/settings.json` の `PreToolUse` には
  `pre-push-check.sh` しか無い。**canary が無いので層 3 を判定できなかった**
- `git push` の時点で層 2（`harness/githooks/pre-push`）が走り、`cargo test` の結果が
  push の出力に出た
- ルーティンが指定した `implementation-flow` スキルと `harness-record` スキルは、この実行環境では
  見つからなかった（pr-244 と同じ）。手順を手で踏んだ
- PR #247（`harness/records/pr-246.md` だけを変える PR）にはレビューコメント・レビュー・
  PR コメントが 0 件だった（`harness/records/README.md`「記録だけの PR には記録を作らない」の
  併記の対象は無い）

## 規約への反映

**無し。** 自己修正の 1 件は既存の規約（AGENTS.md「実装を始める前に」）で見つけて直せた。
`AGENTS.md` の hook-canary の項目は書き換えたが、再発への介入ではなく、この PR で canary を
配線したことに記述を合わせたもの。集計と介入の判断は棚卸しの回が持つ
（`harness/records/README.md`「数えるのと、規約やフックへ手を入れるのは別の回に行う」）。
