# PR #83 ✨ [Phase 0] 仮説検証ハーネスを追加する

- マージ日: 2026-08-22
- 関連 Issue: #17
- 差分規模: 3 ファイル / +193 -0（コミット 1 本・マージを除く）

## ゴールと結果

Issue #17 のゴールは「実リポジトリに `compare` を当てて `DO-NOT-EXTRACT` が出るか、
出るとしてどのくらいの頻度かを数え、出たペアを目視して判定が納得できるかを確かめる」。

**ゲートは閉じていない。** この PR で入れたのは検証の足場だけで、
**当てる実リポジトリがこのセッションに無かった**（次節）。

入れたもの:

- `harness/phase0/verify.sh` — 対象ディレクトリの TS から関数の位置を集め、
  全ペアに `compare` を当ててラベルごとの件数を出す
- `harness/phase0/README.md` — 置き場所の理由と、種の取りこぼしの前提
- `.gitignore` — 出力先 `harness/phase0/out/` を除外

`tests/fixtures/` に当てて、ハーネスが `compare` の出力を数えられることは確認した
（関数 5 件 / ペア 10 件 → `REVIEW` 8 / `EXTRACT-CANDIDATE` 1 / `DO-NOT-EXTRACT` 1、
切り出せなかったペア 0）。ただし **fixtures は計画の出力イメージを再現するために
手で作ったもので、答えが先にある。** ハーネスの動作確認にはなるが、
Issue が問うている「意味のある頻度で存在するか」の証拠にはならない。

**ずれ**: 大きい。Issue は判断（Phase 1 へ進むか）を求めていたが、
出せたのは判断の材料を集める道具まで。**判断は次の回に持ち越した。**

## 指摘

**レビューコメント 0 件 / CI の指摘 0 件。** 以下は着手時の自己確認で出たもの。

### 1. Issue が挙げた検証対象が、実行環境から届かなかった

- 分類: `plan`
- 出どころ: 自己修正（実装に入る前の確認）
- 内容: Issue #17 は検証対象として `pdfmod` / `coding-task-lab` を挙げていたが、
  **このセッションの GitHub スコープは `dryguard` / `d-market-git` / `d-market-workflow` の
  3 つで、どちらも入っていない。** 手元に clone もされていない。

  スコープ内の残り 2 リポジトリを数えたが TS は 0 件（どちらも Claude Code プラグインの
  マーケットプレイスで、中身は Markdown とシェル）。dryguard 自身は Rust で、
  Phase 0 は TS のみが対象。**スコープ内に仮説を当てられる実コードが 1 行も無かった。**

  実装に入らず停止し、選択肢と根拠を Issue #17 のコメントへ残した
  （[#17 issuecomment-5343597307](https://github.com/DIO0550/dryguard/issues/17#issuecomment-5343597307)）。
  却下した案は 2 つ:
  **第三者の公開 TS リポジトリを当てる**（仮説は「**自分の**コードベースに偶発的重複が
  あるか」なので、他人のコードで数えてもゲートの答えにならない）、
  **`tests/fixtures/` の 5 ファイルで数える**（答えが先にあるので循環する）。
- 既存ルール: `AGENTS.md`「実装を始める前に」の
  **「計画の入力を鵜呑みにしない。Issue が挙げた対象の一覧は『調べた結果』であって
  網羅の保証ではない」**。この項目は pr-72 / pr-74 の `plan` から昇格したもので、
  **今回はそれが効いて停止できた側**。
- 次にどう防ぐか: 今回は記録のみ

  **注記（数えるための材料）**: 既存の項目が想定しているのは「Issue の挙げた一覧に
  漏れがある」形で、今回は「**一覧は正しいが、実行環境から届かない**」形。
  同じ `plan` タグに畳まれているが**別種**なので、再発したらタグの分割を検討する材料として残す
  （`TEMPLATE.md`「1 つのタグに別種の問題が畳まれていることが分かる。そのときに分割する」）。
  昇格・分割の判断は棚卸しの回の管轄なので、この回では行わない。

## 手戻り

1 回。

**`awk` の変数名に `close` を使ってコンパイルできなかった。** 種を集める `awk` で
括弧の位置を `open` / `close` に入れたが、`close` は `awk` の組み込み関数名なので
`syntax error at or near =` で落ちた。`open_paren` / `close_paren` へ改名して解消。
**捨てた設計は無く、書き方を直しただけ。** 走らせたその場で出たので、そばに理由をコメントで残した。

## うまくいったこと

- **実装に入る前に前提を数えた。** 「TS があるはず」で書き始めず、
  スコープ内 3 リポジトリの `*.ts` / `*.tsx` を実際に数えて 0 件を確認してから止めた。
  書いてから気付くと、当てる先の無いコードが残る
- **却下した案を理由付きで Issue に残した。** 特に「fixtures で数える」は
  一見できてしまうので、**なぜ数えても意味が無いか**（答えが先にある）を書いておかないと
  次に同じ案が出る
- **取りこぼしを黙って消さない形にした。** 種の判定は `is_function_header` に
  合わせてあるが同じ実装ではないので、ずれた分は `compare` が `NoEnclosingFunction` で
  落として `unchunkable.txt` に出る。逆向きの取りこぼし（式本体のアロー関数・複数行
  シグネチャが種に上がらない）は直せないので、**README に前提として明記した**
- **間引かない上限にした。** ペア数が上限を超えたらサンプリングせずに止める。
  黙って一部だけ回すと、出た件数が全体の何割か読めず「網羅した」と読み違える
- **`scan` を先回りで生やさなかった。** 列挙は Phase 1 の仕事なので、
  プロダクトではなく `harness/` に置いた。`tests/` にも置いていない
  （常時走る検査と、対象が手元にあるときだけ走る調査を混ぜない）

## 規約への反映

**`count.sh` が数える介入は無し。**

指摘 1（`plan`）は既存の `AGENTS.md`「実装を始める前に」が**効いた**側で、
規約の抜けではない。タグの分割は棚卸しの回の管轄。

`対策済` の行は書かない。

## 実行環境の記録

- **`echo hook-canary` は deny されずに通った。** ただし `.claude/settings.json` に
  `PreToolUse` フックは 1 つも配線されていない（`SessionStart` の `wire-githooks.sh` のみ）。
  したがって pr-74 / pr-79 と同じく、正確には**「フック不発」ではなく
  「canary が無いので層 3 が効いているか判定できなかった」**（canary 自体が #50 として未実装）。

  **PR #83 の本文には「フックは不発でした」と書いてしまった。** 断定できない区別なので、
  記録側はこの節の表現を正とする。pr-74 / pr-79 で同じ訂正をしており、**3 回連続**
- **git hooks（層 2）は発火した。** `git config core.hooksPath` は `harness/githooks`。
  push 時に `pre-push: fmt` / `clippy` / `test` の各行が出ている
- **CI（層 1）は success。** head `ba62196`、チェックは `fmt / clippy / test` 1 本。
  **1 回目で通っており、CI 修正の push は発生していない**
- push 前検査は `cargo fmt --check` / `cargo clippy --all-targets -- -D warnings` /
  `cargo test` / `bash -n harness/phase0/verify.sh` を **1 つずつ独立に実行**し、すべて exit 0
- **レビューコメントは 0 件。** 人・ボットとも付かないままマージされた
- **Rust のテストは追加・変更していない。** 差分は bash と Markdown のみ。
  マージ後の総数は lib 165 / 統合 12 = **177**
- **Discord 通知は今回も送れていない。** `DISCORD_WEBHOOK_URL` が未設定。
  pr-67 / pr-69 / pr-72 / pr-74 / pr-76 / pr-79 から変わらず **7 回連続**。
  代わりにルーティンの通知（PushNotification）へ載せた
- **`harness-record` スキルは未移植**（#55）。この記録も `README.md` と `TEMPLATE.md` を
  読んで手で書いた
- **ルーティンが指定する `implementation-flow` スキルも、この環境に入っていなかった。**
  手順は `AGENTS.md`「実装の進め方」に沿った。`harness-record` と合わせて
  **スキル 2 つが未配線**の状態で回している
- **ブランチの空き待ちは発生しなかった。** #17 のアサインで起動した時点で
  指定ブランチが空いており、そのまま着手できた
