# PR #155 ワークスペースの根をプロジェクトの印から決め、呼び出し元の取りこぼしを直す

- マージ日: 2026-09-06
- 関連 Issue: #125
- 差分規模: 17 ファイル / +756 -74

## ゴールと結果

Issue のゴールは 2 つで、**両方とも達成した**。

| ゴール | 結果 |
|---|---|
| 根が `tsconfig.json` より下に来たときの参照元の取りこぼしを直す | `WorkspaceRoot::enclosing_project` を足し、ファイルごとに印を探して根を決めるようにした |
| プロジェクト設定が無いコードベースをどう扱うか決める | `CallerDomainOverlap::ProjectUnrooted` を足し、印の下に無いファイルは参照元を尋ねずに落とす |

**着手前に判断を仰いだ。** Issue が案 A / B / C を「実装前に確認したい」として残していたため
（`AGENTS.md`「設計判断の確認」）、コードには触れずに実測と実装案をコメントして止め、
A で進める承認を得てから着手した。

**ずれ**: 案 A の中身が**レビューの最初の 2 巡で 2 回変わった**（下の「手戻り」）。
承認を得た時点の形（共通の祖先から上へ探す）は最終形と違う。

## 実測

この回は**先に実測してから設計した**。`pnpm install --frozen-lockfile` で
typescript-language-server 6.0.0 を入れ、根に渡すパスだけを変えて `references` を測った。

| 条件 | 返った呼び出し元 |
|---|---|
| 根 = 印のある位置 | 2 件（完全） |
| 根 = 印より 1 段下 | **0 件** |
| 根 = 印より 1 段下・呼び出し元を開かせる | 1 件（部分） |
| 印がどこにも無い木・根を目一杯広げる | **0 件** |
| 根の 1 段**下**に印が 2 つ（兄弟プロジェクト） | 1 件（取れる） |
| 印の下だが `exclude` で範囲外 | **0 件** |

**Issue 本文の「空にはならない」は成り立たなかった**（0 件に落ちる）。本文の表が指す印の
位置も現在の木と食い違っており、どちらも実測して書き直した。

## 指摘

**8 件ある。すべて Codex のレビュー**（5 巡: `de0aab2` / `40ba8c8` / `b5c2764` /
`b310ec7` / `4376ea7`。最終巡は指摘 0 件）。CI は全巡 green で、CI 由来の指摘は無い。

### 1. 共通の祖先から上へだけ探すと、兄弟プロジェクトの印が 1 つも見えない

- 分類: `plan`
- 出どころ: レビュー（Codex P1）
- 内容: `packages/a/tsconfig.json` と `packages/b/tsconfig.json` があり、その上に印が無い形だと、
  印は**共通の祖先より下**にある。祖先から上だけを辿る実装では 1 つも見えず、
  **サーバは根の下の印を見つけて答えられるのに**こちらが取れないものと判断していた。
  Issue で承認を得た案 A の説明そのものが「共通の祖先から上へ探す」で、
  **印が下にある形を計画時に想定していなかった**
- 既存ルール: なし（`AGENTS.md`「実装を始める前に」の「計画の入力を鵜呑みにしない」は
  Issue が挙げた**一覧**の網羅性についての項目で、探索方向の設計漏れは射程外）
- 次にどう防ぐか: 今回は記録のみ

### 2. 印の有無を走査全体で 1 つにまとめ、無関係なペアまで巻き添えにしていた

- 分類: `over-guard`
- 出どころ: レビュー（Codex P1）
- 内容: 指摘 1 を直したときに「1 つでも印を持たないファイルがあれば全体を `Unmarked`」と
  決めたが、`scan` は候補ペアの全ファイルを 1 度に渡すので、**印の外のファイルが 1 つ
  混じっただけで、両側とも印の下にあるペアの参照元まで落ちる**。設定済みパッケージと
  設定の無いスクリプトが混在する monorepo で起きる。
  **指摘 1 の修正が生んだ指摘**で、レビュー返信にその判断を明示的に書いていた
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ。**直した回の返信に書いた判断がそのまま次の指摘になった**
  形なので、同じ形が次に出るかを見る

### 3. `rules/naming.md` の語彙の定義が、同じ PR の実装より古くなっていた

- 分類: `なし`
- 出どころ: レビュー（Codex P2）
- 内容: `project root` の定義を「共通の祖先から上へ探した結果」と書いた後、指摘 1 の修正で
  ファイルごとに探す形へ変えたのに、語彙表を直していなかった。
  **規約が既に無効になった振る舞いを説明している**状態で、次に読む人が元へ戻しうる
- 既存ルール: **どれにも当たらない。** `comment-mismatch` は rules/coding.md
  「コメントは doc と Why / Why not に絞る」に紐づくのでコード内コメントが対象。
  `unverified-claim` は `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」に紐づき、
  対象が記録・PR 本文・Issue コメント・コミットメッセージ・レビュー返信・通知に限られる。
  **`rules/` 配下の記述が実装より古くなる形**は、どちらの射程にも入っていない
- 次にどう防ぐか: 今回は記録のみ。**射程を広げるかどうかは数えてから決める**
  （`分類: なし` の材料として残す）

### 4. 印の下にあっても `include` / `exclude` で範囲外なら、参照元は揃わない

- 分類: `missing-signal`
- 出どころ: レビュー（Codex P1）
- 内容: 印のファイルが**存在するか**だけを見ており、そのファイルが**その tsconfig の
  プロジェクトに属しているか**は確かめていない。範囲外のファイルは inferred project に
  なるので参照元が揃わないのに、`is_marked` は `true` を返す。
  **確かめられなかったことを確かめた答えとして出す**形
  （`rules/architecture.md`「どこまでを『取れなかった』に数えるか」）
- 既存ルール: rules/architecture.md「取れなかったシグナルを既定値で埋めない」
- 次にどう防ぐか: **#156 へ切り出した。** 自前で判断するには glob・`extends`・
  project references の解釈が要り、**TypeScript 固有の実装が `lsp` に入る**。
  印のファイル名だけを言語ごとに持つという #125 で確認を取った設計から外れるので、
  改めて判断を仰ぐ形にした。**この PR はこの形を悪化させていない**（それ以前も
  範囲外のファイルの参照元は同じように揃わなかった）ことも確認済み。
  レビュースレッドは意図的に open のまま残した

### 5. `ProjectRoot` をクレートの外へ再輸出していた

- 分類: `module-api`
- 出どころ: レビュー（Codex P2）
- 内容: `ProjectRoot` / `enclosing_project` を `pub use` で外へ出していたが、
  利用は `pipeline` とモジュール内テストだけ。根の決め方という内部の手順が
  公開 API になり、**後で直すと破壊的変更になる**
- 既存ルール: rules/architecture.md「モジュールの公開 API」（`pub` は最小限にする）
- 次にどう防ぐか: 今回は記録のみ

### 6. 印の一覧が空のとき、参照元を落とす側へ倒していた

- 分類: `enum-omission-direction`
- 出どころ: レビュー（Codex P2）
- 内容: `ServerCommand::new(..., Vec::new())` は doc が認めている使い方だが、
  印の一覧が空だとどのファイルも印の下に無い扱いになり、参照元の問い合わせが全部落ちていた。
  **空の一覧は「この検査が当てはまらない」であって「揃わないことを確かめた」ではない。**
  PR 本文には「一覧を空にすると常にシグナルを落とすだけなので判断軸を満たす」と書いており、
  **判断軸そのものの当て方を誤っていた**
- 既存ルール: rules/coding.md「列挙で判定を組むときは、漏れの倒れる向きを選ぶ」
  （判断軸「一覧を空にしたとき、判定は安全側に倒れるか」）
- 次にどう防ぐか: 今回は記録のみ。**PR #154（`f55e407`）で `分類: なし` から語彙へ昇格した
  直後の再発**にあたる（源の記録は `pr-148.md` で、そこでは 5 件とも `なし` に分類されている。
  昇格したのは棚卸しの回）

### 7. 置くべき印の名前を、出力側に書き写していた

- 分類: `duplication-logic`
- 出どころ: レビュー（Codex P2）
- 内容: `ProjectUnrooted` の根拠の文が `tsconfig.json` を直に書いていた。
  印の綴りは `ServerCommand` が 1 箇所で持つ情報なのに 2 箇所に置いたので、
  別の印を使うサーバには**そのサーバに無いファイルを勧める**形になっていた
- 既存ルール: rules/naming.md「このツールの語彙を固定する」
  （`project marker` は言語ごとに決まる情報で、印の一覧は `ServerCommand` が 1 箇所で持つ。
  **この PR 自身が同じ回に語彙表へ足した記述**）
- 次にどう防ぐか: 今回は記録のみ

### 8. 印を探すファイルシステムの参照が、ファイル数ではなく候補チャンク数に比例していた

- 分類: `なし`
- 出どころ: レビュー（Codex P2）
- 内容: `asked_paths_of` が候補ペアの**チャンクごと**にパスを積んでいたので、1 ファイルから
  N 個切り出すと同じパスについて祖先を辿る `is_file` が N 回走っていた。
  印がどこにも無い木では 1 回あたりファイルシステムの根まで辿る
- 既存ルール: **どれにも当たらない**（性能に対応する分類が語彙に無い）
- 次にどう防ぐか: 今回は記録のみ（`分類: なし` の材料として残す）

## 手戻り

**案 A の中身がレビューの最初の 2 巡で 2 回変わった。** Issue で承認を得た形から
最終形までの経路（3 つの状態 = 2 回の変化）:

| 巡 | 印の探し方 | 印の有無の持ち方 |
|---|---|---|
| 承認時 / 初回 push | 共通の祖先から上へ | 走査全体で 1 つ |
| 指摘 1 の修正（`40ba8c8`） | **ファイルごと**に上へ | 走査全体で 1 つ |
| 指摘 2 の修正（`8c7b94c`） | ファイルごとに上へ | **ファイルごと** |

**どちらの手戻りも、承認を得た案の説明に含まれていなかった形**（印が祖先より下にある /
印の外のファイルが 1 つ混じる）で起きている。実測は先に取っていたが、**測った条件が
「根が印より下」の 1 種類だけ**で、兄弟プロジェクトと混在 monorepo を測っていなかった。

**#156 を切り出した**（指摘 4）。元 Issue #125 に分割理由・スコープ外に渡したもの・
残ったものを追記済み（`AGENTS.md`「タスクの分割」）。

## うまくいったこと

- **指摘のたびに実測してから直した。** 8 件のうち再現条件のある 2 件（指摘 1・指摘 4）は、
  指摘の形の fixture を作って実サーバに尋ね、**当たっていることを確かめてから**着手した。
  指摘 4 はその実測をそのまま #156 の根拠として渡せた
- **着手前に止まって判断を仰いだ。** Issue が「実装前に確認したい」と書いていた A / B / C を
  推測で埋めず、実測と実装案を添えて止めた。案 A の中身は後で変わったが、
  **`lsp` が言語ごとの印を持つという骨格は最後まで変わっていない**
- **数値を書く直前に出し直した。** fixture を 2 つ足したことで PR 本文の
  「`tsconfig.json` は 1 つだけ」が古くなったのを `find` で数え直して直した。
  利用者への報告で「6 件の指摘を受け、5 件を修正」と書いたのも、スレッドを数え直して
  **8 件・7 件修正**へ訂正した（`AGENTS.md`「記録・PR 本文に数値や主張を書く前に」）

## 規約への反映

**無し。記録を書く回は材料を残すところまで**で、**数えるのと規約やフックへ手を入れるのは
別の回**（`harness/records/README.md`「記録は溜まり始めている」）。

**この回が何回目の再発にあたるかは、この記録では判断しない。** 判断には
`count.sh` が出す「最後の介入以降の再発数」が要り、それを読んで層を決めるのは
棚卸しの回の担当。

**`対策済` の行は書かない。** この回に介入は無く、書くと `count.sh` がその分類の
再発数を 0 に戻す（`harness/records/TEMPLATE.md`）。

## 実行環境のメモ

- **層 3（Claude Code hooks）は判定できなかった。** `echo hook-canary` は deny されずに
  通ったが、`.claude/settings.json` に配線されているのは `SessionStart`
  （`wire-githooks.sh`）だけで `PreToolUse` は無い。**canary が無いので判定できない**
  （`AGENTS.md`「実装を始める前に」）
- 層 2（git hooks）は `core.hooksPath=harness/githooks` が効いており、push のたびに走った
- 層 1（CI）は全 5 巡 green。`fmt / clippy / test` のジョブが `cargo test` に加えて
  pnpm で typescript-language-server を入れたうえで `cargo test -- --ignored` も走らせるので、
  この回の回帰テストは CI でも実際に実行されている
