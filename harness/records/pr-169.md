# PR #169 ✨ CommonJS の require 形式を依存の宣言として集める

- マージ日: 2026-09-11
- 関連 Issue: #161
- 差分規模: 7 ファイル / +4237 -255
  （`git diff --stat $(git merge-base e1a8105^1 15d03bd) 15d03bd`。47 コミット）

**記法**: 以下 **`⟨esc-i⟩`** と書いたら、**バックスラッシュ 1 文字に続けて `u0069` と書いた 6 文字**
（`i` のエスケープ）を指す。PR #169 のレビュー返信でこの 6 文字が経路で復号され、3 度置き直した
のと同じ形を記録で繰り返さないため、綴りではなく記法で書く（指摘 56 / 57 / 80）。

## ゴールと結果

Issue #161 のゴールは「`syntax::import` が CommonJS の `require` 形式を依存の宣言として集める」で、
**達成した**。採った案は Issue で示した 1-A / 2-A / 3-B（呼び出しの位置を問わない / `import` の集合に
混ぜる / 「読み取れなかった」）。

| ゴール | 結果 |
|---|---|
| `require(...)` と `import x = require(...)` を依存の宣言として集める | `syntax::import` が集める。相対指定は importer の位置から畳む |
| 集めきれないファイルを「測れた」と答えない | **読み取れない・確かめられないものが 1 つでもあれば集合を作らない。**`ImportsUnavailable` を 3 通り（`NoDeclarations` / `ReboundSpelling` / `UnreadableDeclaration`）で返し、`--explain` が出し分ける |
| 判定が実際に動くことを確かめる | `origin/main`（`c03b56b`）と各コミットの 2 つのバイナリを同じ入力に通して比較した。過大（`1.00` / `EXTRACT-CANDIDATE`）と過小（測れない / `REVIEW`）の両方が直っている |

**ずれ: 理由の分け方が 30 巡目まで間違っていた。** `require` の綴りが束縛され直しているだけの
ファイル（宣言はすべて読めている）を `UnreadableDeclaration` に畳んでおり、`--explain` が
利用者を dryguard の穴へ向かせていた。**`..._cannot_be_created` 系のテストの大半が、その誤った分類を
固定していた**ので、指摘が無ければ気付けなかった（指摘 67）。

**Issue を分けなかった。** 集める形 → 集合の意味 → 理由の文は同じ差分の表と裏で、片方だけ入れると
`--explain` が実装と食い違う。理由は Issue #161 のコメントに書いた（`AGENTS.md`「タスクの分割」）。
**分けたいものは 4 つ**挙げてある（`node:` の前置 / `UnreadableDeclaration` の拒否権 /
「出したもの・測ったものを取り直して確かめる」の `AGENTS.md` 追記 / rustdoc の警告の層 1 化）。

## 指摘

**この PR に 92 件**。これに **PR #168 の回の 4 件**を併記するので、**ブロックは 96 件**になる。
内訳は次のとおり（すべて一次情報から数え直した）。

**うち 7 件は `分類:` をバッククォート付きで書いていない**（再現しなかった / 欠陥として確立して
いない / 理由を示して採らなかったもの）。`count.sh` が数えるのは残り **89 件**で、
これは ``grep -c '^- 分類: `'`` の出力と一致する。

| 出どころ | 件数 | 数え方 |
|---|---|---|
| レビュー（PR #169） | **85** | スレッドは 90 本（Codex 87 / Copilot 3）。同じ内容が複数のスレッドに分かれた分を畳んで 85 件（下の表） |
| 自己修正（PR #169） | **6** | スレッドに現れない分（28 / 30 巡目）と、**他のブロックの「次にどう防ぐか」に埋めてあった 3 件**（94 / 95 / 96。レビューで指摘されて立て直した） |
| フック（PR #169） | **1** | `cargo fmt --check` の失敗 |
| レビュー（PR #168 の回） | **4** | 記録だけの PR なので、`harness/records/README.md`「記録だけの PR には記録を作らない」に従いここへ併記 |

**90 本 → 85 件の内訳**（畳んだのは 4 箇所・スレッド 5 本ぶん）。

| 指摘 | スレッド | 巡 | 減る数 |
|---|---|---|---|
| 13（別名に持ち出した `module.require`） | 3 本 → 1 件 | 7 | −2 |
| 22（`require.extensions`） | 2 本 → 1 件 | **11 と 39**（同じ内容が再掲された。どちらも未解決） | −1 |
| 49（本体を持たない宣言の引数名） | 2 本 → 1 件 | 23 | −1 |
| 50（JSX の組み込みタグ） | 2 本 → 1 件 | 23 | −1 |

`90 − 2 − 1 − 1 − 1 = 85`。**22 が巡をまたいでいるので、「同じ巡の重複投稿」だけでは 85 にならない。**

**CI 由来の指摘は無い。** この branch の workflow run は **44 回すべて `success`**
（`rust.yml` / event=pull_request を API から数えた）。HEAD `15d03bd` の check run
`fmt / clippy / test` も `success`（06:58:31–07:00:16 UTC）。

**未解決のスレッドが 9 本ある**（API から取り直して数えた）。8 本は判断待ち、1 本は再現しなかった
指摘への返信。該当する指摘のブロックに「判断待ち」と書いてある。

### 分類の当て方（この回で一貫させた軸）

85 件の大半は 1 つの差分に対する指摘なので、**利用者に見える壊れ方**で分類を選んだ。

| 分類 | この回で当てたもの |
|---|---|
| `over-guard` | **測れるはずのファイルを測れないと答えた**（無害な位置・無害な添字の綴りを束縛や読み込みと数えた）。`rules/` に対応する見出しは無いが、`rules/coding.md`「漏れの倒れる向きを選ぶ」が選ばせた安全側の代償として出ている |
| `enum-omission-direction` | **一覧の漏れが安全でない側へ倒れた**（欠けた集合を `Ok` で返し、Jaccard が完全な集合として計算された = 偽陽性） |
| `missing-signal` | **取れなかったことを取れた値として出した / 理由を畳んで落とした**（一覧の漏れ以外） |
| `error-variant` | **測れない理由の分け方・優先順が実態と違った** |
| `comment-mismatch` | doc が実装・事実と食い違う |
| `unverified-claim` | 確かめずに書いた数値・主張（返信・PR 本文・doc の理由） |
| `plan` | **前の巡で自分が下した判断が、後から前提ごと崩れた** |

**`enum-omission-direction` と `over-guard` を取り違えないため、向きを毎回実測で確かめた。**
どちらに倒れているかは「欠けた集合が `Ok` で返るか / 集合そのものが作られないか」で決まり、
返信にその実測が残してある。

### 1. 束縛し直された `require` を、CommonJS の読み込みとして数えた

- 分類: `enum-omission-direction`
- 出どころ: レビュー（1 巡目）
- 内容: ローカルの `function require(...)` や引数名 `require` への呼び出しを読み込みと数えていた。無関係な 2 ファイルが同じ文字列を渡すだけで `EXTRACT-CANDIDATE` になる。**束縛の形を列挙する**のをやめ、「綴りが読み込み以外の位置に 1 つでも現れたら、そのファイルの `require` 呼び出しを採らない」という否定形へ反転した（`require_spelling_of` / `RequireSpelling`）。
- 既存ルール: rules/coding.md「列挙で判定を組むときは、漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ（規約どおりに反転できた）

### 2. 置換を持たないテンプレートリテラルの指定子を落とした

- 分類: `enum-omission-direction`
- 出どころ: レビュー（1 巡目）
- 内容: ``require(`./pad`)`` は書いた時点で綴りが決まっているのに `string` ノードだけを採っており、引用符の違いで依存を 1 件落として**欠けた集合を測れた値として返して**いた。`is_static_string_literal` を足した。**採る種別の一覧（当時は `string` の 1 件）の漏れが、安全でない側へ倒れた形。**
- 既存ルール: rules/coding.md「列挙で判定を組むときは、漏れの倒れる向きを選ぶ」。落とした結果の読み違え（同じ依存先を書き方の違いで別物と数える）は rules/naming.md「`specifier` と `module path` を混ぜない」
- 次にどう防ぐか: 今回は記録のみ

### 3. 語彙表の `import` だけを広げ、`specifier` を `from` の後ろに限ったまま残した

- 分類: `naming-vocabulary`
- 出どころ: レビュー（1 巡目）
- 内容: 実装は `require("./dep")` の引数を `specifiers_of` へ通しているのに、`rules/naming.md` の `specifier` の定義が「`from` の後ろ」のままだった。語彙表と `ModulePath::from_specifier` の doc を直した。
- 既存ルール: rules/naming.md「このツールの語彙を固定する」
- 次にどう防ぐか: 今回は記録のみ

### 4. 呼ばれていない `require` の参照を、束縛し直しと数えた

- 分類: `over-guard`
- 出どころ: レビュー（2 巡目）
- 内容: `typeof require !== "undefined"` や `require.resolve(...)` がファイル全体を `Rebound` にし、**同じファイルの本物の `require(...)` をすべて潰して**いた。**1 巡目に自分が入れた変更の回帰**。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」（規約どおり安全側へ倒れた結果の過剰ブロック）
- 次にどう防ぐか: 今回は記録のみ

### 5. エスケープを含む指定子から、最初の断片だけを読んでいた

- 分類: `missing-signal`
- 出どころ: レビュー（2 巡目）
- 内容: ``require(`./pa⟨esc-i⟩d`)`` のような綴りで `unquoted_text_of` が最初の `string_fragment` しか返さず、`./pa` を依存先にしていた。**テンプレートに限らず `import` 文でも起き、`origin/main` の時点から在るバグ**だった（実測）。
- 既存ルール: rules/architecture.md「取れなかったシグナルを既定値で埋めない」
- 次にどう防ぐか: 今回は記録のみ

### 6. 読み込む先でない引数を依存先にしていた

- 分類: `missing-signal`
- 出どころ: レビュー（3 巡目）
- 内容: `require(name, "./fallback")` で引数の並びから**最初の文字列リテラルを探して**おり、読み込まれない文字列を共有依存として数えていた。**最初の引数だけを見て、文字列リテラルでなければ `Unreadable`** へ変えた。
- 既存ルール: rules/architecture.md「取れなかったシグナルを既定値で埋めない」
- 次にどう防ぐか: 今回は記録のみ

### 7. 括弧で包まれた読み込み `(require)("./billing")` を呼び出しと認識しなかった

- 分類: `enum-omission-direction`
- 出どころ: レビュー（3 巡目）
- 内容: 呼ばれる側が `parenthesized_expression` になると呼び出しと見なされず、ESM の import と混ざったファイルで**欠けた集合が `Ok` で返って**いた。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ（**指摘 9（4 巡目）**で同じ形が続いたため、そこで構造の側を直した）

### 8. 測れなかった理由を `None` に畳んで落とした

- 分類: `missing-signal`
- 出どころ: レビュー（3 巡目）
- 内容: 読み取れない宣言があるファイルと、宣言が 1 つも無いファイルが同じ `None` になり、`--explain` が区別できなくなっていた。**2 巡目の自分の直しが作った**もの。`ImportsUnavailable`（`NoDeclarations` / `UnreadableDeclaration`）を足した。
- 既存ルール: rules/architecture.md「理由は落とさない」 / rules/coding.md「エラー型は原因ごとにバリアントを分ける」
- 次にどう防ぐか: 今回は記録のみ

### 9. 型だけの包み（`as` / `!`）越しの読み込みを認識しなかった

- 分類: `enum-omission-direction`
- 出どころ: レビュー（4 巡目）
- 内容: `(require as NodeRequire)("./billing")` で包みの一覧が足りず、欠けた集合が返っていた。**同じ形の指摘が 2 巡続いた**（前巡は括弧）ので、一覧を足すのをやめ、**包みを木の上で数え上げて 6 通りの表**にし、落ち方のほうを直した。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 10. 読み込みを行う要素越しの `require.call(...)` を測れる側に残した

- 分類: `enum-omission-direction`
- 出どころ: レビュー（5 巡目）
- 内容: **2 巡前に自分が入れた除外が広すぎた**。`require.resolve` を測れる側に残すため「メンバアクセスの受け側」を外したが、「束縛を作らない」で括ったため**読み込みを行う `require.call` まで一緒に外れて**いた。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 11. `module.require("./stock")` を要素の名前として無条件に外した

- 分類: `enum-omission-direction`
- 出どころ: レビュー（6 巡目）
- 内容: `property_identifier` を一律に無視していたため、Node の `module.require` による読み込みが集合から落ちていた。指摘 12 と合わせ、**「要素アクセスが呼ばれているか」という 1 本の軸**へ置き換えた。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 12. `require.cache` / `require.main` を読むだけのファイルを測れなくした

- 分類: `over-guard`
- 出どころ: レビュー（6 巡目）
- 内容: 許可リストが `resolve` の 1 件しか無く、値を読むだけの標準の要素で `Rebound` に落ちていた。指摘 11 と**逆方向に見えて同じ軸**で収まった。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 13. 別名に持ち出した `module.require` の読み込みを落とした

- 分類: `enum-omission-direction`
- 出どころ: レビュー（7 巡目・**同じ内容が 3 スレッドに分かれて届いた**ので 1 件として数える）
- 内容: `const load = module.require.bind(module); load("./stock")` で、`require` が直接の呼ばれる側でないため除外され、欠けた集合が返っていた。**前巡に置いた「呼ばれているか」の軸が、この形には当たっていなかった**（呼ばれていない参照も値として持ち出せる）。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 14. エスケープで書かれた `require` の束縛を見つけられなかった

- 分類: `enum-omission-direction`
- 出どころ: レビュー（8 巡目）
- 内容: `const requ⟨esc-i⟩re = helper;` は実行時には `require` を束縛するが、木が返すのは**書かれた綴りのまま**なので一致で見つけられず、**存在しない依存を集合に入れて**いた。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ（**この形が以降 10 巡以上にわたって偽陽性の主因になる**。指摘 27 / 34 以降を参照）

### 15. 区切りを伴わない `.` を相対指定と見なさなかった

- 分類: `enum-omission-direction`
- 出どころ: レビュー（8 巡目）
- 内容: `is_relative` が `./` / `../` / `..` だけを見ており、`require(".")` が別々のディレクトリでも同じ `ModulePath` になっていた。**`origin/main` の時点から在り、ESM の `import` だけでも起きる**（実測）。
- 既存ルール: rules/naming.md「`specifier` と `module path` を混ぜない」
- 次にどう防ぐか: 今回は記録のみ

### 16. JSX の地の文の逆立ちを、名前として数えた

- 分類: `over-guard`
- 出どころ: レビュー（9 巡目）
- 内容: `<code>C:\tmp</code>` の `jsx_text` が「エスケープされた `require` かもしれない名前」と判定され、そのファイルの読み取れる import まで落ちていた。**指摘は「識別子らしい種別に限れ」だったが、挙げそこねが偽陽性へ倒れるので採らず、「名前を書けない葉」を外す形**にした。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 17. 型の中で宣言された `require` という名前で、ファイル全体を落とした

- 分類: `over-guard`
- 出どころ: レビュー（10 巡目）
- 内容: `interface Loader { require(path: string): void }` があるだけで ESM の import まで測れなくなっていた。構文木を全件ダンプして**型の中でしか現れない種別 3 つ**を数え、`TYPE_ONLY_KINDS` を足した。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ。**この一覧は 3 → 19 まで増え、最後に 17 へ減る。** 各巡の件数は次のとおり（記録の各ブロックから拾い直した）

  | 指摘 | 何を足した / 外した | 件数 |
  |---|---|---|
  | 17（この指摘） | 型の中でしか現れない種別 3 つ | **3** |
  | 19 | 親の種別ではなく祖先で決める形へ変え、型の中の位置を覆った | 3 → **9** |
  | 20 | 型そのものの宣言・型注釈 | 9 → **12** |
  | 26 | `type_parameter` / `type_parameters` | 12 → **14** |
  | 30 | `type_arguments` | 14 → **15** |
  | 36 | `implements_clause` | 15 → **16** |
  | 46 | `array_type` / `nested_type_identifier` | 16 → **18** |
  | 49 | `function_signature` | 18 → **19** |
  | 66 | 「挙げる」を「辿る」へ変え、**46 で足した 2 つを外した** | 19 → **17** |

### 18. 包んだ指定子 `require(("./pad"))` を読み取れないと答えた

- 分類: `over-guard`
- 出どころ: レビュー（11 巡目）
- 内容: 呼ばれる側の包みは剥がすのに**引数の包みを剥がしておらず**、綴りの決まった指定子を読み取れないことにしていた。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ（29 巡目に上下の走査を 1 つの表へ揃えた。指摘 64 / 65）

### 19. 型の中の引数名 `require` が残っていた

- 分類: `over-guard`
- 出どころ: レビュー（11 巡目）
- 内容: `type Handler = (require: string) => void` で `Rebound` に落ちていた。前巡は**要素の宣言だけ**を外していた。親の種別で足すと深さが違って一覧が二重になるので、**祖先を辿る形**へ変えた。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 20. 型そのものの宣言・型注釈の `require` が抜けていた

- 分類: `over-guard`
- 出どころ: レビュー（12 巡目）
- 内容: `type require = string` / `let value: require` が `Rebound` に落ちていた。前巡に挙げた 9 種類は**型の中の位置**で、**型の宣言と型注釈**が抜けていた（9 → 12）。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 21. `export import dep = require("./dep")` が宣言として見えない（再現せず）

- 分類: 付けない（欠陥として確立していないため。`count.sh` はバッククォート付きの `分類` 行だけを数えるので集計に入らない）
- 出どころ: レビュー（9 巡目）
- 内容: tree-sitter がこの形の右辺を `import_require_clause` として出さないので依存としては見えないが、**実測すると既に `Err(UnreadableDeclaration)`** になっていた（構造の安全網が拾っている）。黙って欠けた集合を返してはいない。
- 既存ルール: rules/architecture.md「取れなかったシグナルを既定値で埋めない」（既に守れている）
- 次にどう防ぐか: 今回は記録のみ

### 22. `require.extensions` を「読み込まない要素」に足すか（判断待ち）

- 分類: 付けない（欠陥として確立していないため。同上）
- 出どころ: レビュー（11 巡目・**39 巡目に同じ内容が再掲**されたので 1 件として数える。どちらのスレッドも未解決のまま）
- 内容: 一覧に無いので `require.extensions` を読むファイルは測れない。**一覧の条件は「持ち出した先で呼んでも読み込まない」**で、`require.extensions[".js"](module, "./x")` は `require` の綴りを 1 度しか使わずに読み込みを起こす（実測）ため、条件を満たさないと判断して足していない。**採否はオーナーの判断待ち。**
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 23. `node:fs` と `fs` が別の依存先になる（別 Issue にしたい）

- 分類: `なし`
- 出どころ: レビュー（12 巡目・未解決のまま）
- 内容: 同じ組み込みモジュールの 2 つの綴りが `ModulePath` の上で畳まれず、重なりが `0.0` になる。**`origin/main` の時点から在り、ESM だけでも起きる。** 正しく畳むには「両方の綴りで読み込める組み込みの一覧」が要り（`node:test` / `node:sqlite` は前置つきでしか読み込めない）、**倒れる向きも逆**（畳みすぎ = 偽陽性）。
- 既存ルール: rules/naming.md「`specifier` と `module path` を混ぜない」が理由としては近いが、**語彙の取り違えではなく解決の抜け**なので、分類の語彙に当たるタグが無い
- 次にどう防ぐか: 今回は記録のみ（別 Issue に分けたい旨を Issue #161 に書いた）

### 24. 型だけの輸入 `import type { require }` でファイル全体を落とした

- 分類: `over-guard`
- 出どころ: レビュー（13 巡目）
- 内容: 型だけの輸入と値の輸入は、**名前のあるノードだけを見ると同じ木**になる。印は名前のないノードとして置かれるので、祖先の種別では届かなかった。**綴りで印を探す案は採らない**（`import { type as require }` と取り違える）。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 25. 別名を付けた輸入の輸入元の綴りを、束縛と数えた

- 分類: `over-guard`
- 出どころ: レビュー（14 巡目）
- 内容: `import { require as load }` は `load` だけが実行時の名前になるのに、輸入元の綴りを数えていた。**2 つの形は木の上で同じ子の並び**（`[identifier, @as, identifier]`）なので、`name` / `alias` の欄で見分ける形にした。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 26. 型変数の名前 `require` が抜けていた

- 分類: `over-guard`
- 出どころ: レビュー（14 巡目）
- 内容: `function parse<require>(value: require)` で `Rebound` に落ちていた（一覧 12 → 14）。**`type_parameter` だけでは足りなかった**：`named_descendants` は内側のノードも訪ねるので `type_parameter` 自身の綴りが一致し、祖先辿りが `type_parameters` から始まる（二分探索で見つけた）。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 27. エスケープを含む計算された欄で `require` を隠した読み込みを落とした

- 分類: `enum-omission-direction`
- 出どころ: レビュー（14 巡目）
- 内容: `module["requ⟨esc-i⟩re"]("./stock")` で木のどのノードも `require` を綴らず、**欠けた集合が `Ok` で返って**いた。**14 巡で唯一「安全でない側」へ倒れていた形**で、実測で偽陽性（食い違う依存を持つ 2 ファイルが `1.00` / `EXTRACT-CANDIDATE`）を再現した。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ（**この族が 39 巡目まで続く**。指摘 34 / 45 / 56 / 58 / 59 / 64 / 71 / 72 / 73 / 75 / 78 / 80 / 82）

### 28. オブジェクトの欄の名前 `{ require: false }` を束縛と数えた

- 分類: `over-guard`
- 出どころ: レビュー（15 巡目）
- 内容: 欄の名前は束縛でも参照でもないのに `Rebound` にしていた。省略記法（`{ require }`）とは木の上で親が違う（`pair` の `key` か否か）ので、位置で分けられた。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 29. 再輸出 `export { require as load } from "./loader"` を束縛と数えた

- 分類: `over-guard`
- 出どころ: レビュー（15 巡目）
- 内容: **14 巡目に自分が書いた「輸出は見ていません」の理由が、再輸出には当たっていなかった。** 輸出元がある形では輸入元の側もこのファイルの束縛を指さない。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 30. 型引数の位置の `require` が抜けていた

- 分類: `over-guard`
- 出どころ: レビュー（15 巡目）
- 内容: `consume<require>()` の `type_arguments` が一覧に無く、消える型名を実行時の束縛と扱っていた（14 → 15）。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 31. 型を言い当てる式の型の側の `require` が抜けていた

- 分類: `over-guard`
- 出どころ: レビュー（16 巡目）
- 内容: `value as require` / `value satisfies require` が `Rebound` に落ちていた。**挙げられた 3 つのうち直したのは 2 つ**で、`<require>value` は `type_arguments` の下にあり既に外れていた（実測で確かめて返信に書いた）。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 32. 名前空間の再輸出 `export * as require from "./loader"` が抜けていた

- 分類: `over-guard`
- 出どころ: レビュー（16 巡目）
- 内容: 別名が `export_specifier` の外（`namespace_export`）に置かれるので、14 巡目に足した再輸出の判定が届いていなかった。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 33. 引用符で囲んだ欄の名前 `{ "require": false }` が抜けていた

- 分類: `over-guard`
- 出どころ: レビュー（16 巡目）
- 内容: 引用符の有無でノードが 1 段ずれる（`string_fragment < string < pair`）ため、直接の親を見る判定が届かなかった。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 34. 文字列リテラルですらない計算された欄で読み込みを落とした

- 分類: `enum-omission-direction`
- 出どころ: レビュー（17 巡目）
- 内容: `module["re" + "quire"]("./stock")` は**どのノードも `require` を綴らない**のに欠けた集合が返っていた（**この PR で 2 つ目の偽陽性**）。14 巡目の直しが「添字を読み取れるか」で切っていたのを、**「呼ばれている添字を `require` でないと言い切れるか」**へ切り直した。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 35. 依存先の名前が `require` のとき、読み取れている宣言を読み取れないと答えた

- 分類: `over-guard`
- 出どころ: レビュー（17 巡目）
- 内容: `import loader from "require"` / `require("require")` の `string_fragment` を名前として数えていた。**この一覧だけは広げると逆向きに倒れる**（除外しすぎると綴りを見逃す）ので、`specifier_of` が読むのと同じ欄・同じ順序で確かめ、境目を対照 2 件で固定した。
- 既存ルール: rules/coding.md「同じ一覧を、安全な倒れ方が違う 2 箇所で使い回さない」
- 次にどう防ぐか: 今回は記録のみ

### 36. `implements require` の型名が抜けていた

- 分類: `over-guard`
- 出どころ: レビュー（18 巡目）
- 内容: `implements_clause` が一覧に無かった（15 → 16）。**`class_heritage` へは寄せていない**（`extends require` は値なので、1 段上げると実行時の束縛まで型扱いになる）。対照のテストで固定した。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 37. JSX の属性の名前 `<Widget require={false} />` が抜けていた

- 分類: `over-guard`
- 出どころ: レビュー（18 巡目）
- 内容: 属性の名前は欄の名前でしかないのに `Rebound` にしていた。値の側（`<Widget loader={require} />`）とは親が違うので位置で分けられた。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 38. 呼ばれる前に持ち出す計算された読み込みを「取りこぼし」と誤って分類した

- 分類: `plan`
- 出どころ: レビュー（18 巡目）
- 内容: `const load = module[key]; load("./stock")` の穴を、**17 巡目の返信で「取りこぼし（偽陰性）側」と書いていた。これは誤りで、実測すると偽陽性を作る向き**だった。訂正したうえで、**塞ぐと `items[index]` のようなごく普通の書き方 8 種がすべて測れなくなる**ため、**オーナーの判断で現状維持に決着**。
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」（確かめずに向きを書いた形）だが、**判断そのものの誤り**なので `plan` を優先する
- 次にどう防ぐか: 今回は記録のみ（**この決着は 23 / 27 / 28 / 32 / 34 / 35 / 39 巡目でも据え置きを確かめている**）

### 39. Windows の区切りの指定子を相対指定として畳んでいなかった

- 分類: `over-guard`
- 出どころ: レビュー（19 巡目）
- 内容: `ModulePath::from_specifier` が `pub` で doc の契約に反していたので直した。**ただし指摘が挙げた「重なりが過大に出る」は再現しない**（TypeScript で逆立ちを書く唯一の方法はエスケープで、エスケープを含む指定子は先に読み取れない側へ落ちる。3 通りとも `Err`）。特性テストにそう書いた。
- 既存ルール: rules/naming.md「`specifier` と `module path` を混ぜない」
- 次にどう防ぐか: 今回は記録のみ

### 40. 輸出元の無い `export { load as require };` の別名を束縛と数えた

- 分類: `over-guard`
- 出どころ: レビュー（19 巡目）
- 内容: **14 巡目に足した判定の穴。** 再輸出を足したとき輸出元を見る側にまとめたので、輸出元の無い形で別名まで数えていた。`alias` の欄で分けた。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 41. doc ブロックが、説明していない定数に付いていた

- 分類: `comment-mismatch`
- 出どころ: レビュー（20 巡目・Copilot）
- 内容: 13 巡目に `TYPE_ONLY_MARKER_KIND` を既存の doc ブロックの直後へ差し込んだため、`TYPE_ONLY_KINDS` の説明が新しい定数に付き、**説明される側が doc を失って**いた。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ。**rustdoc では落とせない**（doc の中身は正しく、付いている先が違うだけなので警告にならない）。機械で落とせるのは 42（壊れたリンク）だけ。下の「規約への反映」の注を参照

### 42. doc のリンクが存在しない定数を指していた

- 分類: `comment-mismatch`
- 出どころ: レビュー（20 巡目・Copilot）
- 内容: `[`TYPE_MEMBER_KINDS`]` は定義が無く、実体は `TYPE_ONLY_KINDS` だった。**rustdoc が警告として出すのは ``--document-private-items`` を付けたときだけ**（この doc が付いていた `is_use_that_cannot_load` は私有関数。素の `cargo doc` は私有項目の壊れたリンクを報告しない。下の「規約への反映」の注で実測した）。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: ``RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --document-private-items`` を層 1 のゲートにする案を挙げた（**この 1 件だけが機械で落とせる**。呼び方・オプション・件数は下の「規約への反映」の注で測り直した）

### 43. 関数を差し込んだときに、隣の関数の doc を持っていってしまった

- 分類: `comment-mismatch`
- 出どころ: レビュー（20 巡目・Copilot）
- 内容: 19 巡目に `with_forward_separators` を `is_relative` の直前へ差し込んだとき、`is_relative` の doc がそのまま新しい関数に付いた。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ

### 44. 名前を書けない葉の一覧が、綴りの一致の枝に効いていなかった

- 分類: `over-guard`
- 出どころ: レビュー（21 巡目）
- 内容: `<p>require</p>` や `/require/` でファイル全体が落ちていた。**一覧そのものは既にあったのに、`text_of(node) == REQUIRE_FUNCTION_NAME` の枝が素通りして**いた（一覧を見ていたのは逆立ちの判定だけ）。**この一覧は 2 箇所で共有してよい**ことを実測で確かめて doc に書いた（2 箇所とも広すぎると偽陽性へ倒れるので、安全な倒れ方が同じ）。
- 既存ルール: rules/coding.md「同じ一覧を、安全な倒れ方が違う 2 箇所で使い回さない」
- 次にどう防ぐか: 今回は記録のみ

### 45. `new` で呼ぶ計算された読み込みを落とした

- 分類: `enum-omission-direction`
- 出どころ: レビュー（22 巡目）
- 内容: `new module["re" + "quire"]("./stock")` の親が `new_expression` なので呼び出しと見なされず、**この PR で 4 件目の偽陽性**（実測で判定まで壊れていた）。**「呼ばれているか」の一覧は分けた**（`is_invoked_or_constructed` を別に作った）。
- 既存ルール: rules/coding.md「同じ一覧を、安全な倒れ方が違う 2 箇所で使い回さない」
- 次にどう防ぐか: 今回は記録のみ

### 46. 型を包む構文（配列型・修飾された型名）越しの型名が抜けていた

- 分類: `over-guard`
- 出どころ: レビュー（22 巡目）
- 内容: `value as require[]` / `value as ns.require` で直接の親だけを見ていたため届かなかった。`array_type` / `nested_type_identifier` を足した（一覧 16 → 18）。**30 巡目に「辿る」形へ変えたので、この 2 つは後に死んで外している**（指摘 66。19 → 17）。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 47. 計算された欄の名前 `{ ["require"]: false }` が抜けていた

- 分類: `over-guard`
- 出どころ: レビュー（22 巡目）
- 内容: `computed_property_name` が 1 段挟まるため、引用符の形の直しが届いていなかった。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 48. ラベル `require: { break require; }` を束縛と数えた

- 分類: `over-guard`
- 出どころ: レビュー（22 巡目）
- 内容: ラベルは別の名前空間。宣言側も分岐先も `statement_identifier` なので 1 つの種別で両方を覆えた。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 49. 本体を持たない宣言の引数名 `require` が抜けていた

- 分類: `over-guard`
- 出どころ: レビュー（23 巡目・**同じ内容が 2 スレッドに分かれて届いた**ので 1 件として数える）
- 内容: `function parse(require: string): void;` の `function_signature` が一覧に無かった（18 → 19）。実装を伴う `function_declaration` は値のままなので、対照のテストで境目を固定した。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 50. JSX の組み込みタグ `<require />` を束縛と数えた

- 分類: `over-guard`
- 出どころ: レビュー（23 巡目・**同じ内容が 2 スレッドに分かれて届いた**ので 1 件として数える）
- 内容: JSX の除外が属性の名前だけを覆っていた。実測すると**3 種類のタグすべて**が該当していたので、まとめて外した。`<require.Sub />` と `<div {...require} />` は数えたまま残る。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 51. `require["resolve"]` を要素の読み出しと見なさなかった

- 分類: `over-guard`
- 出どころ: レビュー（23 巡目）
- 内容: 読み込まない要素の判定が `.` の形だけを見ていたため、同じ要素を添字で読む書き方が `Rebound` に落ちていた。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 52. 呼ばれていない動的な欄の読み出しで、ファイル全体を落とした

- 分類: `over-guard`
- 出どころ: レビュー（23 巡目）
- 内容: ``const title = config[`title-${locale}`]`` のような普通の読み出しが測れない側へ落ちていた。**指摘どおりに呼び出しの条件を足すと 14 巡目に塞いだ穴が開く**ので、そのまま採らず、**「綴りが書いた時点で決まるか」で呼ばれているかを見る / 見ないを分ける**線を引き直した。
- 既存ルール: rules/coding.md「同じ一覧を、安全な倒れ方が違う 2 箇所で使い回さない」
- 次にどう防ぐか: 今回は記録のみ

### 53. 名前空間つきの JSX の名前が抜けていた

- 分類: `over-guard`
- 出どころ: レビュー（24 巡目）
- 内容: `<Widget require:mode="x" />` で `jsx_namespace_name` が 1 段挟まる。**指摘は属性の 1 通りだったが、`AGENTS.md`「計画の入力を鵜呑みにしない」に沿って周辺を測ったら 4 通りすべてが `Err` だった**ので、タグの側も同時に直した。
- 既存ルール: AGENTS.md「実装を始める前に」（計画の入力を鵜呑みにしない）
- 次にどう防ぐか: 今回は記録のみ

### 54. 分解して受ける欄の名前 `const { require: load } = registry`（採らなかった）

- 分類: 付けない（指摘どおりに直すと偽陽性が 1 件増えるため、欠陥として確立していない）
- 出どころ: レビュー（25 巡目）
- 内容: 木の上の見立て（`pair_pattern` の `key`）は実測どおりだったが、**そこは欄を作る側ではなく読む側**（= `const load = module.require;`）で、外すと `EXTRACT-CANDIDATE`・重なり `1.00` になることを実測した。実装は変えず、**この判断をテスト 4 件と doc で固定**した。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ（**この軸が読み取れない綴りの枝へ通っていなかったことが 27 巡目に分かる**。指摘 59）

### 55. 文字列で書いた輸入・輸出の名前が抜けていた

- 分類: `over-guard`
- 出どころ: レビュー（26 巡目）
- 内容: `import { "require" as load }` / `export { load as "require" }` で `string` が 1 段挟まり、欄を見る判定に届かなかった。**指摘は 2 通りだったが、再輸出の形でも同じ**だったので 3 通りまとめて直した。見る欄は変えていない。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 56. 包みが挟まったエスケープつきの添字で、読み込みを落とした

- 分類: `enum-omission-direction`
- 出どころ: レビュー（26 巡目）
- 内容: `const load = module[("requ⟨esc-i⟩re")]; load("./stock")` で**この PR で 5 件目の偽陽性**。指摘は括弧だけだったが、`as` でも同じ穴が開いていた。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ。**このスレッドの返信で、エスケープの 6 文字が投稿経路で復号され 3 度置き直した**（記法 `⟨esc-i⟩` を定義して決着）。同じ形は指摘 57 / 80 にも出ている

### 57. 逆立ちのある名前を、無害な位置でも束縛と数えた

- 分類: `over-guard`
- 出どころ: レビュー（26 巡目）
- 内容: `type requ⟨esc-i⟩re = string` / `{ requ⟨esc-i⟩re: false }` が `Err` になっていた。**指摘の後半（既存の除外の一覧を丸ごと使い回す）はそのままでは採れない**（読み込みを 1 件落とす）ので、位置で言えることだけを戻した。**呼ばれる側は戻していない。**
- 既存ルール: rules/coding.md「同じ一覧を、安全な倒れ方が違う 2 箇所で使い回さない」
- 次にどう防ぐか: 今回は記録のみ

### 58. 型を先に書く言い当てに包まれた添字で、読み込みを落とした

- 分類: `enum-omission-direction`
- 出どころ: レビュー（27 巡目）
- 内容: `module[<string>"requ⟨esc-i⟩re"]` で**6 件目の偽陽性**。`type_assertion` は「値が最後の子」の側なので、包みの一覧が上下で揃っていなかった。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 59. 分解して受ける欄のエスケープつきの綴りで、読み込みを落とした

- 分類: `enum-omission-direction`
- 出どころ: レビュー（27 巡目）
- 内容: `const { ["requ⟨esc-i⟩re"]: load } = module` で**7 件目の偽陽性**。**指摘は 1 通りだったが、実測したら 9 通り壊れていた。** 25 巡目に決めた「欄を読む位置」の軸が、読み取れない綴りの枝へ通っていなかったのが原因で、見る位置を「添字」から「欄を読む位置」へ広げて揃えた。
- 既存ルール: rules/coding.md「同じ一覧を、安全な倒れ方が違う 2 箇所で使い回さない」
- 次にどう防ぐか: 今回は記録のみ

### 60. 測れない依存先の重なりが、`EXTRACT-CANDIDATE` を止めない（判断待ち）

- 分類: `missing-signal`
- 出どころ: レビュー（27 巡目・未解決のまま）
- 内容: `Unavailable(UnreadableDeclaration)` が `Lean::Neither` へ落ち、置き場所が `Same` なら `EXTRACT-CANDIDATE` になる（`classification` は純粋なのでシグナルを直に組んで確かめた）。**依存先が食い違うと測れたときより緩い。** ただし `NoDeclarations` でも同じで、**`origin/main` の `NoImports` も同様**だったので、この PR が作った経路ではない。**決定木の変更**なので `rules/tdd.md` に沿って別 Issue にしたい。
- 既存ルール: rules/architecture.md「取れなかったシグナルを既定値で埋めない」 / rules/tdd.md「判定ルールを変えるときは、先にテストを足す」
- 次にどう防ぐか: 今回は記録のみ（別 Issue の案を Issue #161 に書いた）

### 61. 素の文字列リテラル `const label = "require"` でファイル全体を落とす（判断待ち）

- 分類: `over-guard`
- 出どころ: レビュー（28 巡目・未解決のまま）
- 内容: 指摘の前半は実測どおり。**ただし「欄を読む位置でなければ外す」では足りない**（文字列は離れた場所から欄の名前として使える。4 通り実測し、外すとどれも偽陽性になる）。追うにはデータフロー解析が要り `syntax` の持ち場を越える。**8 巡目に定数畳み込みを却下したのと同じ形。**
- 既存ルール: rules/architecture.md「依存方向のルール」（`syntax` の持ち場） / rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 62. 置換とエスケープが混ざった欄で、呼ばれていなくても落とした

- 分類: `over-guard`
- 出どころ: レビュー（28 巡目）
- 内容: ``config[`title⟨esc-i⟩-${locale}`]`` が `Err` になっていた。**23 巡目に決めた軸の当て方が誤っていた**（「エスケープを含むか」で見ていたが、置換が混ざれば綴りは書いた時点で決まらない）。判定の名前も `spells_an_escape` → `spells_a_static_name` へ改めた。
- 既存ルール: rules/naming.md「名前と実体を一致させる」（「エスケープを含むか」は実装の手順で、判定したいことではない）
- 次にどう防ぐか: 今回は記録のみ

### 63. 分解して受ける読み取れない欄（18 巡目の決着の族）

- 分類: 付けない（18 巡目にオーナーの判断で「塞がない」と決着した族と同じ形のため）
- 出どころ: レビュー（28 巡目）
- 内容: `const { ["re" + "quire"]: load } = module; load("./stock")` は、`const load = module["re" + "quire"]` と**同じ族であることを実測で示した**うえで据え置いた。
- 既存ルール: —（決着済みの判断）
- 次にどう防ぐか: 今回は記録のみ

### 64. 順に評価して最後を返す並びに包まれた添字で、読み込みを落とした

- 分類: `enum-omission-direction`
- 出どころ: レビュー（29 巡目）
- 内容: `module[(0, "requ⟨esc-i⟩re")]` で**9 件目の偽陽性**。隣の指摘（65）と同じ根だったので、**上へ辿る側と下へ剥がす側の包みを 1 つの表に揃えた**（`value_child_of`）。
- 既存ルール: rules/coding.md「同じ一覧を、安全な倒れ方が違う 2 箇所で使い回さない」の**裏返し**（上下が同じ答えを返さないと壊れる）
- 次にどう防ぐか: 「同じ問いに 2 つの答えを持たせない」を `rules/coding.md` へ裏返しの項目として足すかを判断待ちにした（29 / 30 / 35 巡目で 3 回出ている）

### 65. 型を先に書く言い当てを、指定子の側では剥がしていなかった

- 分類: `over-guard`
- 出どころ: レビュー（29 巡目）
- 内容: `require(<string>"./stock")` が `Err(UnreadableDeclaration)` になっていた。**27 巡目の直しが上向きの走査だけで止まっていた**のが原因。上だけを広げると `is_invoked` が「無害」と答えるのに `specifier_of` が綴りを採れず、**読み込みを 1 件落とす**。
- 既存ルール: rules/coding.md「同じ一覧を、安全な倒れ方が違う 2 箇所で使い回さない」の裏返し
- 次にどう防ぐか: 同上

### 66. 組み合わせの型の中の型名を、種別を挙げる形で拾おうとしていた

- 分類: `over-guard`
- 出どころ: レビュー（30 巡目）
- 内容: `value as require | null` / `value satisfies require & Marker` が `Err(ReboundSpelling)` になっていた。**「種別を挙げるのをやめて辿る」という指摘の後半まで採り**、言い当ての式まで祖先を辿って値の子から来ていなければ型の側、とした。**副産物として `array_type` / `nested_type_identifier` が死に、一覧が 19 → 17 へ減った**（外しても 1 件もテストが落ちないことを変異で確かめてから外した）。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 67. 綴りが曖昧なだけのファイルを「宣言を読み取れなかった」と答えた

- 分類: `error-variant`
- 出どころ: レビュー（30 巡目）
- 内容: `import { pad } from "./pad"` と `function require(...)` を書いただけのファイルで、**宣言はすべて読めているのに** `--explain` が「依存の宣言を読み取れなかった」と出していた。**利用者を dryguard の穴へ向かせる文**なので分類そのものが誤り。`ReboundSpelling` を足して 3 通りにした。
- 既存ルール: rules/coding.md「エラー型は原因ごとにバリアントを分ける」（利用者が直す先が違う）
- 次にどう防ぐか: 今回は記録のみ（**`..._cannot_be_created` 系のテストの大半がこの誤った分類を固定していた**。変異で確かめたら 51 件が落ちた）

### 68. 言い当ての型の側のリテラル型を、綴りの種別で早期に弾いていた

- 分類: `over-guard`
- 出どころ: レビュー（31 巡目）
- 内容: `value as "require"` の葉は `string_fragment` なので、`type_identifier` かで絞る早期 return が祖先辿りを止めていた。**「構造で決める」形にしたら種別の条件がまるごと要らなくなった**（`TYPE_IDENTIFIER_KIND` も死んだ）。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 69. 綴りの曖昧さと読み取れない宣言の優先順が、doc と食い違っていた

- 分類: `error-variant`
- 出どころ: レビュー（31 巡目）
- 内容: 同じファイルに両方あるとき、早期 return が宣言を見る前に `ReboundSpelling` を返していた。**30 巡目に自分が書いた理由が、この形には当たっていなかった。**
- 既存ルール: rules/coding.md「エラー型は原因ごとにバリアントを分ける」
- 次にどう防ぐか: 今回は記録のみ

### 70. 読み取れなかった宣言が「どこか」を運んでいなかった

- 分類: `missing-signal`
- 出どころ: レビュー（31 巡目）
- 内容: `--explain` が「どこかのファイルに読み取れない宣言がある」としか言えず、利用者が直す先を特定できなかった。**行を持たせた**（`LineNumber` は `Copy` なので `ImportOverlap` の `Copy` を外さずに持てる）。**どちらのファイルかは持たせていない**（`PathBuf` を運ぶと `Copy` が外れる。判断待ち）。
- 既存ルール: rules/architecture.md「理由は落とさない」
- 次にどう防ぐか: 今回は記録のみ

### 71. `await` を挟んだ添字で、読み込みを落とした

- 分類: `enum-omission-direction`
- 出どころ: レビュー（32 巡目）
- 内容: `module[await "requ⟨esc-i⟩re"]` が包みの一覧に無かった。**包みの入れ子（`module[await ("requ⟨esc-i⟩re" as string)]`）は指摘に無く、8 形を測って自分で見つけた。** `await` を入れてよい理由（このモジュールが読むのは書かれた綴りだけ）を doc に書いた。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 72. 型引数だけの式に包まれた添字で、読み込みを落とした

- 分類: `enum-omission-direction`
- 出どころ: レビュー（33 巡目）
- 内容: `((module["re" + "quire"] as GenericLoader)<unknown>)("./stock")` の `instantiation_expression` が一覧に無かった（5 → 6 件）。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 73. `call` / `apply` 越しの計算された読み込みを落とした

- 分類: `enum-omission-direction`
- 出どころ: レビュー（33 巡目）
- 内容: `module["re" + "quire"].call(null, "./stock")` が `Ok({pad})` を返していた（`./stock` が落ちる）。**`bind` は入れなかった**（その場では呼ばない、という理由）。**この理由が狭かったことが次の巡で分かる**（指摘 75）。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 74. 輸入の注記の欄 `with { require: "json" }`（再現せず）

- 分類: 付けない（再現しなかったため）
- 出どころ: レビュー（33 巡目・未解決のまま）
- 内容: 木の上では普通の `pair` で、**既に測れる側**だった。**テストを足すと「`pair` の下にあれば効く」という 1 つの振る舞いを書かれた場所ごとに固定することになる**ので足していない（採否は判断待ち）。
- 既存ルール: rules/testing.md「振る舞いをテストする」
- 次にどう防ぐか: 今回は記録のみ。**この返信の本文に、書いていないバッククォートが 2 箇所入って投稿された**（API から取り直して発見し、訂正を追記）

### 75. その場で呼ばれる `bind` の結果を、読み込みと数えなかった

- 分類: `plan`
- 出どころ: レビュー（34 巡目）
- 内容: `module["re" + "quire"].bind(null)("./stock")` が `Ok({pad})` を返していた。**33 巡目に `bind` を外した理由（その場では呼ばない）は、返った関数を持ち出したままの形にしか当たっていなかった。** 「持ち出しただけ」と「その場で呼ぶ」を分けた。**一覧の漏れではなく、前の巡で自分が下した判断が前提ごと崩れた形**（下の「規約への反映」で 38 / 82 と同じ族として数えている）。
- 既存ルール: AGENTS.md「実装を始める前に」（計画の入力を鵜呑みにしない）と同じ形だが、**自分が下した判断の誤り**なので `plan`
- 次にどう防ぐか: 今回は記録のみ

### 76. 要素の定義の名前 `{ require() {} }` を束縛と数える（判断待ち）

- 分類: `over-guard`
- 出どころ: レビュー（34 巡目・未解決のまま）
- 内容: 再現した。**ただし一度実装して全テストを走らせたところ、既存の対照テスト 1 件だけが落ち、そのコメントが「要素の名前を丸ごと外すな」と今回の変更をそのまま禁じていた**ので、実装を戻して判断を仰いだ。**記録された判断の反転に当たるため独断で進めない**（`AGENTS.md`「設計判断の確認」）。私の見立ては「外すのが筋」。
- 既存ルール: AGENTS.md「設計判断の確認」
- 次にどう防ぐか: 今回は記録のみ

### 77. 数の添字 `handlers[0]()` でファイル全体を落とした

- 分類: `over-guard`
- 出どころ: レビュー（34 巡目）
- 内容: 呼ばれている添字を一律に「読み取れない」としていたため、**普通のコードを 1 行書くだけでファイル全体が測れなく**なっていた。`INDEX_KINDS_THAT_CANNOT_SELECT_REQUIRE` を足した。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 78. `bind` の結果を `call` / `apply` で呼ぶ形を辿れなかった

- 分類: `enum-omission-direction`
- 出どころ: レビュー（35 巡目）
- 内容: `module["re" + "quire"].bind(null).call(null, "./stock")` が `Ok({pad})` を返していた。**34 巡目の `bind` の枝が要素越しの呼び出しを辿れていなかった。** `call` / `apply` / `bind` は重ねられるので、**「その場で呼ばれているか」の入口を 1 つへ揃えた**（`is_invoked_on_the_spot`）。
- 既存ルール: rules/coding.md「同じ一覧を、安全な倒れ方が違う 2 箇所で使い回さない」の裏返し（同じ問いに 2 つの答えを持たせない）
- 次にどう防ぐか: 指摘 64 / 65 と同じ項目の判断待ち

### 79. 束縛し直されたファイルの `require(name)` を、依存の宣言と数えた

- 分類: `error-variant`
- 出どころ: レビュー（36 巡目）
- 内容: `function require(...)` を持つファイルの `require(name)` を宣言として先に見ていたため、引数がリテラルでないことを理由に `UnreadableDeclaration` を返していた。**それは利用者のローカル関数の呼び出しで、依存の宣言ですらない。** **31 巡目に自分が書いた理由が、この形に当たっていなかった。** 綴りに依存しない宣言を先に見る形へ直し、同時に**古くなっていた `specifier_of` の doc**（「綴りが束縛され直しているファイルはここへ来ない」）も直した。
- 既存ルール: rules/coding.md「エラー型は原因ごとにバリアントを分ける」 / rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ

### 80. 代入式が値を返す添字で、読み込みを落とした

- 分類: `enum-omission-direction`
- 出どころ: レビュー（37 巡目）
- 内容: `const load = module[(key = "requ⟨esc-i⟩re")]; load("./stock")` が `Ok({pad})` を返していた。`assignment_expression` を「値が最後の子」の一覧へ足した（2 → 3 件）。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: **自己申告: 最初の実測で「再現しない」と読み違えた。** 確認用ファイルに書いたエスケープの 6 文字が経路で復号され、**素の `require` を測っていた**（回帰テストを書いて `Ok({pad})` が出たことで気付いた）。**非 raw の Rust 文字列でバックスラッシュを 2 つ書く**か、python の `chr(92)` で組み立てると、崩れがコンパイルエラーとして出る

### 81. 真偽値・`null`・空の文字列の添字でファイル全体を落とした

- 分類: `over-guard`
- 出どころ: レビュー（38 巡目）
- 内容: `handlers[false]()` / `handlers[null]()` / `handlers[""]()` が `Err(ReboundSpelling)` になっていた。**34 巡目に「テストが要求していない」として見送った項目**が、今回の指摘で要求されたので足した。
- 既存ルール: rules/tdd.md「テストが要求しない機能を先回りで実装しない」（見送り自体は規約どおり）
- 次にどう防ぐか: 今回は記録のみ

### 82. `.call.call(...)` と重ねた要素越しの呼び出しを辿れなかった

- 分類: `plan`
- 出どころ: レビュー（39 巡目）
- 内容: `module["re" + "quire"].call.call(module["re" + "quire"], module, "./stock")` が `Ok({pad})` を返していた。**33 巡目に同じ形を見て「`this` が `null` なので実行時に落ちる」と判断し追わなかった**もので、**受け手を明示的に渡す書き方では落ちない**。前提が狭すぎた。
- 既存ルール: AGENTS.md「実装を始める前に」（計画の入力を鵜呑みにしない）と同じ形だが、**自分が下した判断の誤り**なので `plan`
- 次にどう防ぐか: 今回は記録のみ（**33 → 39 巡目で、同じ「前の巡の判断が後から崩れた」形が 2 回目**。指摘 38 と合わせて 3 回目）

### 83. 数に演算子を付けた添字でファイル全体を落とした

- 分類: `over-guard`
- 出どころ: レビュー（39 巡目）
- 内容: `handlers[-1]()` が `unary_expression` として残り `Err` になっていた。**「符号つき」を挙げる形ではなく、演算子を数え上げない形**にした（数に単項演算子を付けた結果は数・真偽値・`"number"` / `"undefined"` のどれかにしかならず、要素の名前が `require` になる組み合わせが存在しない）。`-` と `+` を挙げると次に `~` / `!` / `typeof` で同じ指摘が来る。
- 既存ルール: rules/coding.md「漏れの倒れる向きを選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 84. 構文解析の誤りのある `require` 呼び出し（再現せず）

- 分類: 付けない（再現しなかったため）
- 出どころ: レビュー（39 巡目・未解決のまま）
- 内容: 指摘の入力（正しい関数のあとに `const dep = require("./stock"` を EOF で置く）は、**指摘の時点の HEAD でも測れた集合を返していなかった**（既に安全側）。**実装すると等価変異になる**旨と、別の入力があれば測り直す旨を返信した。
- 既存ルール: rules/testing.md「assert は『落ちうるか』で見る」
- 次にどう防ぐか: 今回は記録のみ

### 85. 動的な添字の原因が `ReboundSpelling` として出る（判断待ち）

- 分類: `error-variant`
- 出どころ: レビュー（39 巡目・未解決のまま）
- 内容: `handlers[chosen]()` で `--explain` が「require の綴りが読み込みを指すと言い切れない」と出すが、**そのファイルに `require` は 1 文字も書かれていない**。測れないこと自体は正しく、**理由の文だけが誤り**。直すと `ImportsUnavailable` に 4 つ目のバリアントが増え、`report` の文・`pipeline` の優先順・語彙表に同時に手が入り、**既存テスト 30 件の期待値を書き換える**（実際に経路を切って数えた）。私の見立ては「指摘に賛成」。
- 既存ルール: rules/coding.md「エラー型は原因ごとにバリアントを分ける」
- 次にどう防ぐか: 今回は記録のみ（判断を仰いでいる）

### 86. レビュー返信の見出しに「PR 本文へ足しました」と過去形で書いたが、足していなかった

- 分類: `unverified-claim`
- 出どころ: 自己修正（28 巡目）
- 内容: 返信を書いた時点では PR 本文に反映しておらず、**やっていないことを完了として書いた**。投稿後に気付いて足し、返信でも申告した。
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ（**反映先の案は Issue #161 のコメントに置いた**。下の「規約への反映」）

### 87. 通知が 1 件届かず、その巡の指摘 2 件のうち 1 件を見落としかけた

- 分類: `なし`
- 出どころ: 自己修正（30 巡目）
- 内容: レビューの通知が 1 件だけ届かず、**PR を API から取り直して初めて 2 件目に気付いた**。以降は巡ごとにスレッド一覧を取り直して件数を突き合わせるようにした。
- 既存ルール: なし（届いた通知が全部かを確かめる、に当たる規約が無い）
- 次にどう防ぐか: 今回は記録のみ

### 88. テスト関数の件数を、`git diff` の grep で過大に数えていた

- 分類: `unverified-claim`
- 出どころ: 自己修正（30 巡目）
- 内容: `^+    fn test_` で数えていたため、**rustfmt がシグネチャを折り返した既存のテストまで `+` 側に出て**いた。**マージベースと HEAD で関数名の集合を取って差を見る**方法へ変え、PR 本文の 90 / 94 / 110 / 114 がこの理由でずれていたことを申告した。理由ごとの内訳も、`assert` の出現回数と 1 ファイルだけを混ぜて数えていたので数え方ごと置き換えた。
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 89. `cargo fmt --check` が巡の途中で繰り返し落ちた

- 分類: `なし`
- 出どころ: フック（`harness/githooks/pre-push`・層 2）
- 内容: テストの関数名・`assert_eq!` の引数・配列リテラル・条件式・クロージャの折り返し・長い文字列リテラルで落ちた。**コミットメッセージに記録があるのは 9 件**（`08c016b` / `7228e80` / `1eb2919` / `de097f3` / `27a29a0` / `2355a37` / `ca20f45` / `ef4b467` / `8bc3f8e`）で、32 / 36 / 37 / 39 巡目は返信と Issue にだけ記録した。**33 巡目から「長いテスト名を先に短くする」ようにして 35 / 38 巡目は初回から通ったが、`assert_eq!` の折り返しは名前の長さでは防げない。**
- 既存ルール: なし（層 2 が毎回落としているので、コードは一度も壊れていない）
- 次にどう防ぐか: **編集した後**に `cargo fmt` をかける層 3 のフック（`PostToolUse`）を置くかを判断待ちにした。**編集の直前にかける形では防げない**（整形されるのは編集前の中身で、その編集が入れた崩れは残る）。**層 3 が発火する環境かどうかをこの回では判定できていない**（下の「実行環境メモ」）

---

以下 4 件は **PR #168（記録だけの PR）の回の指摘**。`harness/records/README.md`
「記録だけの PR には記録を作らない」に従い、**次に実装が入る PR = この PR の記録へ併記する**。
`main` の first-parent で #168 の次は #169 なので、宛先はここで合っている。

### 90. 記録に立てた指摘が「手戻り」節にだけ書かれ、`分類:` の行を持っていなかった

- 分類: `unverified-claim`
- 出どころ: レビュー（PR #168 の回・Codex）
- 内容: 共用体の平坦化漏れを「手戻り」にだけ書いており、**`count.sh` は `- 分類:` の行だけを数える**ので `plan` の通算・再発数が 1 件少なくなっていた。4 件目として立て直し、合計を 3 → 4 件に直した。
- 既存ルール: harness/records/README.md「指摘 1 件 = 1 ブロック（まとめない）」
- 次にどう防ぐか: 今回は記録のみ

### 91. PR #166 の rules 介入を `対策済` として書けという指摘（採らなかった）

- 分類: 付けない（理由を示して採らなかったため）
- 出どころ: レビュー（PR #168 の回・Codex・**未解決のまま**）
- 内容: 起動したルーティンの指示が「記録の追記のみ。介入判断は行わない」であること、`pr-107` / `pr-110` / `pr-117` / `pr-129` が同じ状況で同じ扱いをしていること、**その追記では止まらない `plan` が同じ記録の中に 2 件ある**ことの 3 点を挙げて採らなかった。`rules/naming.md` を触った事実は「規約への反映」の表に残してある。
- 既存ルール: AGENTS.md「規約の更新」（「介入が効いたか」を表さない起点を置かない）
- 次にどう防ぐか: 今回は記録のみ

### 92. 「構文木の 1 度の走査へ置き換えた」と、確かめずに書いていた

- 分類: `unverified-claim`
- 出どころ: レビュー（PR #168 の回・Codex）
- 内容: `NormalizedSignature::from_spelling` の経路は木を複数回作る（`SyntaxTree::from_source` が `type_spelling` に 4 箇所・`type_structure` に 1 箇所）。数え直して「一度構造として読み、その構造の上で正規化する」へ書き換えた。
- 既存ルール: harness/records/README.md「推測で埋めない」
- 次にどう防ぐか: 今回は記録のみ

### 93. 記録を 1 件足したあと、PR 本文の件数と表が古いまま残った

- 分類: `unverified-claim`
- 出どころ: レビュー（PR #168 の回・Codex）
- 内容: 指摘 90 の修正で 4 件へ増えたのに、PR 本文は「3 件」と 3 行の表のままだった。`grep -c '^- 分類:'` を根拠に直した。**コミットメッセージは書き換えていない**（そのコミットについての記述として正しく、書き換えるとレビューで 1 件増えた経緯が履歴から消えるため。判断はスレッドに書いて委ねた）。**この返信を書くときに、コメントの URL を記憶から書いて送信前に引き直した**ことも申告している。
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」（書いた時点では正しかった主張が、後から入った変更で古くなる）
- 次にどう防ぐか: 今回は記録のみ

---

以下 3 件は **PR #169 の自己修正**。**レビューで指摘されて初めてブロックに立てた**ので、
番号は PR #168 の分の後ろになっている（`harness/records/README.md`「過去の記録は書き換えない」に
合わせ、既存の番号は振り直さない）。**どれも他のブロックの「次にどう防ぐか」に埋めてあり、
`分類:` の行を持たないまま `count.sh` の集計から漏れていた**（指摘 90 とまったく同じ形）。

### 94. レビュー返信のエスケープが投稿経路で復号され、3 度置き直した

- 分類: `unverified-claim`
- 出どころ: 自己修正（26 / 27 巡目・指摘 56 / 57 のスレッド）
- 内容: **バックスラッシュに続けて `u0069` と書いた 6 文字が、投稿するとそのままの形で通らなかった。** 1 度目は素の `require` に、2 度目はバックスラッシュが増えた形になり、3 通り試して 3 通りとも別の形になった。**返信の主張は「綴りが読めない」ことが前提**なので、そのままだと逆の意味に読める。原因を追うのをやめ、**記法（`⟨esc-i⟩`）を定義して**決着させた。投稿後の本文を API から取り直して毎回見つけている。
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」（レビュー返信も射程）
- 次にどう防ぐか: 今回は記録のみ（この記録も同じ記法で書いている）

### 95. レビュー返信に、書いていないバックティックが 2 箇所入って投稿された

- 分類: `unverified-claim`
- 出どころ: 自己修正（33 巡目・指摘 74 のスレッド）
- 内容: リンクの直前と段落の末尾に **``` `` ``` が 2 箇所**入り、リンクがリンクにならず文の残りが code span になっていた。**送った本文には書いていない文字**で、投稿後に API から取り直して見つけ、訂正を追記した。
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 96. 確認用ファイルのエスケープが復号され、「再現しない」と読み違えた

- 分類: `unverified-claim`
- 出どころ: 自己修正（37 巡目・指摘 80 のスレッド）
- 内容: 指摘 80 を最初に測ったとき `Err(ReboundSpelling)` が返り、**「再現しない」と結論しかけた**。実際は**確認用ファイルに書いたエスケープの 6 文字が経路で復号され、素の `require` を測っていた**。**回帰テストを書いて `Ok({pad})` が出たことで気付いた**（テストは非 raw の Rust 文字列でバックスラッシュを 2 つ書いていたため崩れなかった）。**94 / 95 と違い、報告ではなく測定そのものが崩れており、危うく欠陥を見送るところだった。**
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ。**非 raw の Rust 文字列でバックスラッシュを 2 つ書く**か、python の `chr(92)` で組み立てると、崩れがコンパイルエラーとして出る

**PR #165（記録だけの PR・`pr-164.md` を足した回）の 8 件は、ここには併記していない。**
`main` の first-parent で #165 の次は **#167**（`rules/architecture.md` と
`harness/records/TEMPLATE.md` を変えた PR で、`harness/records/` 配下だけの変更ではない）なので、
宛先は `pr-167.md` になる。**その `pr-167.md` が存在しない**（`ls harness/records/pr-*.md` で確認）。

**今この 8 件は `count.sh` に 1 度も数えられていない。** リポジトリ全体を検索しても、
8 件が出てくるのはこの段落だけ。ここへ引き取れば数えられるようになるが、**宛先が違うまま数える**
ことになり、**後から `pr-167.md` が書かれた時点で二重計上になる**。どちらも `count.sh` を壊すので、
**引き取らず、宛先が空いていることを残す**。

**`pr-167.md` をこの PR で作ることもしない。** それは PR #167 そのものの評価（ゴールと結果・
その回の指摘・規約への反映）を書く仕事で、`AGENTS.md`「タスクの分割」の「独立してマージできるか」を
満たす別の単位になる。棚卸しの回か、`pr-167.md` を書く回が拾えるよう、8 件の出どころを残す:
[PR #165 のレビュースレッド](https://github.com/DIO0550/dryguard/pull/165)（8 本）。

## 手戻り

**計画から外れたのは 2 箇所。**

1. **測れない理由の分け方を、30 巡目に作り直した。** 当初の計画は `NoDeclarations` /
   `UnreadableDeclaration` の 2 通りで、`require` の綴りが束縛され直しているだけのファイルを
   後者に畳んでいた。**宣言はすべて読めているのに「読み取れなかった」と答えていた**ので、
   `ReboundSpelling` を足して 3 通りにした（指摘 67）。`report` の文・`pipeline` の優先順・
   `classification` の受け口に同時に手が入り、**`..._cannot_be_created` 系のテスト 51 件が
   この分類に依存していた**（変異で数えた）。
2. **包みの一覧を 3 度作り直した。** **3 巡目**に「一覧を足す」（指摘 7）→ **4 巡目**に
   「木から数えて 6 通りの表」（指摘 9）→ **27 巡目**に上下で別の一覧（指摘 58）→
   **29 巡目に上下を 1 つの表へ揃えた**（指摘 64 / 65。`value_child_of`）。
   揃える前は、上だけを広げると `is_invoked` が「無害」と答えるのに `specifier_of` が綴りを
   採れず、読み込みを 1 件落とす状態だった。**揃えた後に見つかった穴は、いずれも
   「表に 1 項目足りない」だけで済んでいる**（`await` / `instantiation_expression` /
   `assignment_expression`）。

**どちらも「一覧を足す」で 2 巡続いた時点が分かれ目だった。** 2 巡目で構造の側を直した
（**4 巡目**・29 巡目）ものは以降の穴が小さく、3 巡以上一覧を足し続けたもの
（`TYPE_ONLY_KINDS` は 3 → 17）は指摘が止まらなかった。

## うまくいったこと

- **すべての指摘を、手を付ける前に実測で確かめた**（再現したかどうかを含めて）。木のダンプか
  `ImportSet::from_tree` の戻り値、判定まで関わるものは 2 つのバイナリ（`origin/main` と当該
  コミット）で同じ入力を走らせて比べた。おかげで**再現しなかった指摘 3 件**（21 / 74 / 84）と、
  **指摘が挙げた形より壊れていた範囲が広い回**（53 は 1 通り → 4 通り、59 は 1 通り → 9 通り）を、
  どちらも実測で分けられた。**「すべて再現した」ではない**（再現しなかった 3 件がある）。
- **直した判定にはすべて変異を入れた**（`rules/testing.md`「迷ったら、その 1 件だけ実装を
  壊して走らせる」）。**ただし「狙ったテストだけが落ちた」は全件には当たらない。**
  **落ちるテストが 1 件も無かった変異が 3 つある**（下の「テストが要求していない条件」。
  13 巡目の型だけの輸入の印・17 巡目の指定子の `source` の欄・23 巡目の JSX のタグの
  `name` の欄。どれもコミットメッセージに自己申告として残してある）。
  **等価変異も 2 つ見つけて自己申告し、うち 1 つは対照テストを足して潰した**
  （`handlers[chosen]()` を invoked で書いたテストが 1 件も無かった）。
- **独断で決めなかった。** 記録された判断の反転にあたるもの（76）、決定木の変更（60）、
  既存テスト 30 件の期待値が動くもの（85）は、**選択肢と実測を出して判断を仰ぎ、
  スレッドを開けたまま**にした（`AGENTS.md`「設計判断の確認」）。
- **投稿した本文を毎回 API から取り直して確かめた。** これで、送っていない文字が入った回
  （74）・エスケープが復号された回（56 / 57 / 80）を自分で見つけて訂正できた。
  **記憶から書いた URL（93）はこれとは別で、送信前に GitHub から引き直して直したもの**
  （投稿後の取り直しでは見つからない。同じ「確かめる」でも効く場面が違う）。**ここで崩れていたのは報告と調べ方の側だけで、直した分のコード・テストは
  正しく入っている。**

  **ただし「入っているものはすべて正しい」とは言えない。** 再現したまま判断待ちで残した
  振る舞いが 3 件ある（61 の素の文字列リテラル・76 の要素の定義の名前・**85 の
  `handlers[chosen]()` が出す理由の文**）。85 は `--explain` が利用者に誤った理由を出す状態で
  入っている。**直っていないのは報告だけ、と読ませないためにここに書く。**

## 規約への反映

**`対策済` の行は書かない。** ただし**触った規約はある**ので、先にそれを書く。

### PR #169 が `rules/naming.md` を触っている

| 触った場所 | 内容 | 出どころ |
|---|---|---|
| 語彙表の `import` | 「ソースに書かれた `import` / `export ... from` **/ `require`** そのもの」へ広げた | 実装に合わせた先行の変更（`7ddd421`） |
| 語彙表の `specifier` | 「`from` の後ろ」→「依存の宣言が依存先として書いている文字列（`from` の後ろ・`require` の引数）」 | **指摘 3**（`5ab07a2`） |
| 「`require` を `import` と別の語にしない」＋ Why not | 節を 1 つ足した | 実装に合わせた先行の変更（`7ddd421`） |

（`git diff c03b56b 15d03bd -- rules/naming.md` で確認。15 行。）

**それでも `対策済` の行は書かない。** 理由は 3 つ。

1. **この変更は「指摘の再発を止める介入」ではなく、語彙表を実装に追いつかせたもの。**
   `対策済: naming-vocabulary 層=rules at pr-169` を書くと `count.sh` は
   `naming-vocabulary` の再発をここから数え直すが、**この追記が止めるのは
   「`require` を別の語で呼ぶ」ことだけ**で、語彙表の他の行の取り違えには効かない。
   `AGENTS.md` は「**通算ではなく『最後の介入以降の再発数』で見る**（通算は単調増加するので、
   介入が効いたかを表さない）」としており、**効いたかを表さない起点を置くこと**が
   この行の目的と衝突する。
2. **起動したルーティンの指示が「マージ後は `harness/records/` に記録を追記するだけ」**で、
   集計・規約や skill の変更・介入判断は棚卸しの回の管轄になっている。
3. `AGENTS.md`「規約の更新」も **「数えるのと、規約やフックへ手を入れるのは別の回に行う」**
   （`harness/records/README.md`）としている。

**同じ扱いの前例**: `pr-166.md` は `rules/naming.md` への追記を「規約への反映」の表に残したうえで
`対策済` を書かず、その判断を PR #168 のスレッド（指摘 91）で理由付きで説明している。

**この記録を書く回（PR #171）では、`rules/` にも `AGENTS.md` にも手を入れていない。**

**そのうえで、棚卸しの回が読む材料として次を残す。**

### この記録が足す件数（`grep` で数えた）

| 分類 | この記録 | 直前の状態（`count.sh`。再発 / 通算 / 最終介入） |
|---|---|---|
| `over-guard` | **41** | 3 / 11 / pr-155（層=rules） |
| `enum-omission-direction` | **21** | 2 / 2 / 未介入 |
| `missing-signal` | 5 | 20 / 20 / 未介入 |
| `unverified-claim` | **8** | 31 / 31 / 未介入 |
| `error-variant` | 4 | 0 / 0 / 未介入 |
| `comment-mismatch` | 3 | 5 / 5 / 未介入 |
| `なし` | 3 | 130 / 130 / 未介入 |
| `plan` | **3** | 35 / 35 / 未介入 |
| `naming-vocabulary` | 1 | 0 / 1 / pr-69（層=rules） |

**`over-guard` の 41 件と `enum-omission-direction` の 21 件を、独立した 62 件として読まないこと。**
どちらも**1 つの設計判断から出ている**。dryguard は CommonJS の依存を「`require` という綴りを
探して」見つけるので、綴りが現れる位置を 1 つずつ仕分けることになり、**仕分けの一覧に無い形が
見つかるたびに 1 件になる**。`over-guard` 側は「測れるはずのファイルを測れない」（安全側）、
`enum-omission-direction` 側は「欠けた集合を測れた値として返す」（安全でない側）で、
**規約（`rules/coding.md`「漏れの倒れる向きを選ぶ」）が選ばせた向きどおりに大半が安全側へ
倒れている**。規約違反として 41 回積み上がったのではない。

**それでも数として残す。ただし「62 件すべてが規約の射程内」ではない。**
`AGENTS.md`「規約の更新」は**「ルールに書かれていない指摘が 2 回以上出たら、規約の抜けとして扱う」**
で、条件は**「規約に書かれていない」と「2 回以上」の両方**。**分類ではなく形ごとに確かめる**と、
62 件は 4 つに割れる。

| # | 形 | 件数 | 規約に書かれているか | 抜けの候補か |
|---|---|---|---|---|
| 1 | 一覧の漏れが安全でない側へ倒れた（欠けた集合を `Ok` で返した） | **18**（`enum-omission-direction` 21 件から 64 / 78 / 15 を除いた分） | **書かれている**（`rules/coding.md`「漏れの倒れる向きを選ぶ」）。文法の形を 1 つ見つけるたびに 1 件になる**適用の話** | **当たらない** |
| 2 | **同じ問いに 2 つの答えを持たせた**（上下の走査・呼び出しの入口） | **3**（64 / 65 / 78） | **書かれていない。** 既存の項目は「安全な倒れ方が違う 2 箇所で**使い回さない**」で、**その裏返し（揃えないと壊れる）は無い** | **候補。3 回出ている** |
| 3 | **一覧に無い形のせいで、測れるはずのものを落とした**（無害な位置・無害な添字・値を変えない包み） | **39**（`over-guard` 41 件から 65 と 39 を除いた分） | **`over-guard` に対応する `rules/` の見出しは無い。**「漏れの倒れる向きを選ぶ」が決めているのは**倒れる向きだけ**で、**一覧を増やし続けることへの歯止めは無い** | **候補。ただし 39 件が 1 つの設計判断（綴りを探す方式）から出ている** |
| 4 | **`ModulePath` の解決の穴**（指摘 15・区切りの無い `.` / 指摘 39・Windows の区切り） | **2** | **書かれている**（`rules/naming.md`「`specifier` と `module path` を混ぜない」）。**どちらも `origin/main` から在り、ESM だけでも起きる。この方式とは無関係** | **当たらない** |

**分類と形は 1 対 1 ではない。** 2 行目の 3 件は分類が揃っていない（64 / 78 は
`enum-omission-direction`、**65 は `over-guard`**）。分類は**利用者に見える壊れ方**で付け、
形は**直し方**で括っているため。`18 + 3 + 39 + 2 = 62` で、どの件も 1 行にしか入らない。

**4 行目を分けた理由。** どちらも `ModulePath` の解決の話で、**`require` の綴りの位置を
仕分ける話ではなく**、既存の規約に当てはまり、**`origin/main` から在って ESM だけでも起きる**
（指摘 15 は実測で確かめ、指摘 39 は利用者に見える失敗が再現しなかった）。
他の行に入れると「歯止めが無い形」「この方式の適用漏れ」を水増しすることになる。

**前の版では「すべて射程内」と書いていたが、これは誤り**（`over-guard` に見出しが無いことも、
64 / 65 / 78 の反映先が判断待ちであることも、この記録自身が別の箇所で書いていた）。
**その後さらに 2 度割り直している**: 指摘 39 / 15（`ModulePath` の解決）を 4 行目へ分け、
指摘 2 の分類を `missing-signal` から `enum-omission-direction` へ直した
（**一覧の漏れ**なので、この記録が自分で置いた軸ではそちらに当たる）。

**残すのは、条件を分けて読めるようにするため。** `count.sh` の再発数は
「最後の介入以降に同じ形が何度出たか」であって、規約の抜けの有無とは別の軸。
棚卸しの回が見るのは次の 2 つになる。

| 見るもの | この回の状態 |
|---|---|
| 規約の抜けか（**規約に書かれていない** + 2 回以上） | **2 つの形が候補**（上の表の 2 行目 3 件・3 行目 39 件） |
| 既に規約がある形が再発し続けているか（層を 1 つ上げるかの判断） | **4 行目の 2 件（15 / 39）がこれに当たる**（下） |

**層を上げる材料になるのは 4 行目の 2 件だけ。** 指摘 15 / 39 はどちらも
`rules/naming.md`「`specifier` と `module path` を混ぜない」の射程内で、**その規約が
`origin/main`（`c03b56b`）の時点で既に在った**のに 2 件見つかっている（実測で確認）。
**規約がある形が 2 回再発した**ので、`AGENTS.md`「同じ層で再発したら層を 1 つ上げる」の
入力になる。ただし**分類タグは割れている**（15 は `enum-omission-direction`、
39 は `over-guard`）ので、`count.sh` の数からは見えない。

**`count.sh` が出す `over-guard` の 44 件は、層を上げる根拠にしない。** あれは
**タグ単位の合計**であって、「pr-155 で置いた介入が効かなかった回数」ではない。

| | pr-155 の介入 | この回の 3 行目 39 件 |
|---|---|---|
| 何の話か | **印の有無を走査全体で 1 つにまとめない**（`rules/naming.md` へ追記） | **`require` の綴りが現れる位置の仕分け** |
| 直す先 | `project root` をファイルごとに持つ | 無害な位置の一覧 / 綴りを探す方式そのもの |

**同じ形の再発ではないので、`AGENTS.md`「同じ層で再発したら層を 1 つ上げる」の入力にならない。**
この回の `over-guard` 41 件は、形で見ると 3 行目に 39 件・2 行目に 1 件（65）・4 行目に 1 件（39）。
`enum-omission-direction` 21 件は 1 行目に 18 件・2 行目に 2 件（64 / 78）・4 行目に 1 件（15）。
**規約が無い側は前の 2 つ**で、4 行目は既存の規約に当たる。`44` は**タグの合計としてしか読めない**。

### 2 回以上出た形（棚卸しの回への引き継ぎ）

| 形 | この回の件数 | 反映先の案 |
|---|---|---|
| **同じ問いに 2 つの答えを持たせない**（上下の走査・呼び出しの入口） | 3（64 / 65 / 78） | `rules/coding.md`「同じ一覧を、安全な倒れ方が違う 2 箇所で使い回さない」の**裏返し**として足すか。判断待ち（Issue #161） |
| **一覧に無い形のせいで、測れるはずのものを落とす** | 39 | **`over-guard` に対応する `rules/` の見出しが無い。** 一覧を増やし続けることへの歯止めを置くか、「綴りを探す方式」自体を見直すか。**39 件が 1 PR に偏っているので、次の回の件数と合わせて見る**（`count.sh` の `over-guard` 44 件は**タグの合計**で、pr-155 の介入とは別の形。上を参照） |
| **前の巡で自分が下した判断が、後から前提ごと崩れた** | 3（38 / 75 / 82） | `AGENTS.md`「実装を始める前に」に当たるか。**`plan` の 3 件がそのままこの 3 件** |
| **出したもの・測ったものが意図どおりかを確かめる** | **7**（86 / 88 / 92 / 93 / 94 / 95 / 96） | `AGENTS.md` への追記案を Issue #161 のコメントに置いた。**この PR には含めていない**。**送信前に一次情報から出し直す**（88 / 93 / 96）と**送信後に届いた形を取り直す**（94 / 95）は、**片方だけでは他方の失敗を捕まえられない** |
| **doc の付け先が、定数や関数を差し込んだときに取り残される** | **2**（41 / 43） | **rustdoc では落とせない**（doc の中身は正しく、付いている先が違うだけ）。別の手当てが要る（下の注） |
| doc のリンクが存在しない先を指す（**1 件だけ。2 回以上ではない**） | 1（42） | ``RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --document-private-items`` で機械的に落とせる（下の注）。**41 / 43 とは remedy が違うので分けて数える** |
| **`cargo fmt --check` が落ちる** | 1 ブロック（9 コミットに記録） | **編集した後**に `cargo fmt` をかける層 3 のフック（下の注）。**ただし層 3 の発火を判定できていない**（さらに下） |

**注（`cargo doc` の呼び方・オプション・今この回で測り直した数）**: `cargo doc` に `-D` は無い
（``cargo doc -D warnings --no-deps`` は `error: unexpected argument '-D' found` で終了コード 1）。
rustdoc へ渡すには ``RUSTDOCFLAGS='-D warnings' cargo doc`` か
``cargo rustdoc -- -D warnings`` を使う。**この形で今の `main`（`e1a8105`）を測ると
rustdoc の警告は 8 件**（未解決リンク 1 件 + 私有項目へのリンク 7 件）で、ゲートを入れるなら
先にこの 8 件を直すことになる。

**`--document-private-items` が要る。** 使い捨てのクレートで実測した。

| doc の付き先 | 素の `cargo doc --no-deps` | `--document-private-items` あり |
|---|---|---|
| 公開の関数の壊れたリンク | **報告される** | 報告される |
| **私有の関数の壊れたリンク** | **報告されない** | **報告される** |

**指摘 42 の `TYPE_MEMBER_KINDS` は私有関数（`is_use_that_cannot_load`）の doc に
付いていた**ので、素の呼び方では落ちない。

**このゲートで防げるのは 42 の 1 件だけ。** 41 と 43 は**doc の中身が正しく、
付いている先が違うだけ**なので、rustdoc から見れば正しい doc であり警告にならない
（`rustdoc -W help` の `missing-docs` も allow 既定かつ公開項目のみで、
この形は捕まえられない）。**3 件をまとめて防ぐ案としては誤りだった。**

> **自己申告**: この欄は最初「`cargo doc -D warnings`」「既存の未解決リンクが 3 件」と書いていた。
> **どちらも PR #169 の本文から出し直さずに転記したもの**で、呼び方は動かず、件数は
> PR #169 で 3 件が直ったあとの今の `main` とは合っていなかった。上の 2 つは実際に走らせて
> 出し直した値。`AGENTS.md`「記録・PR 本文に数値や主張を書く前に」がまさに名指ししている形
> （**前の記録・前の巡からの転記のまま書かない**）で、**この記録が `unverified-claim` を
> 5 件数えている当の記録でやっていた。**

**注（フックのタイミング）**: 編集の**直前**に `cargo fmt` をかけても、整形されるのは
**編集前の中身**で、その編集が入れた崩れは残る。最後の編集だったときは層 2 の
`cargo fmt --check` まで誰も直さない。**編集の後に走る形**（`PostToolUse`）か、
コマンドの直前に走る形にする必要がある。

### 実行環境メモ（層 3 を判定できていない）

`echo hook-canary` は deny されずに通った。**これを「フックが発火しない環境」の根拠にはしない。**
`.claude/settings.json` を読むと、配線されているのは `SessionStart` だけで **`PreToolUse` が無い**。
canary を deny する配線がそもそも無いので、**層 3 が発火する環境かどうかはこの回では判定できていない**
（`AGENTS.md`「実装を始める前に」。pr-95 / pr-97 で 2 回続けて誤断定したもの）。

層 1（CI）と層 2（git hooks）は実際に走っている。push 時に
`core.hooksPath = harness/githooks` の pre-push が発火して 3 つとも走ったことを出力で確認し、
CI は branch の 44 回すべてが `success` だった。
