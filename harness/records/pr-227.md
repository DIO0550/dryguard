# PR #227 ✨ [Phase 3] #33 --explain で尋ねなかったシグナルと当てた閾値まで出す

- マージ日: 2026-09-22
- 関連 Issue: #33
- 差分規模: 9 ファイル / +483 -80

**PR #226 の回に併記すべき指摘は無い**（`harness/records/README.md`「記録だけの PR には
記録を作らない」— PR #226（マージコミット `f40dc50`）は `harness/records/pr-225.md` だけを
足す PR で（`git diff --stat 187f9a9 f40dc50` が 1 ファイル / +120）、
この PR #227 がその次に実装が入った PR に当たる。**その回はレビュー 0・レビュースレッド 0・
コメント 0** で、チェックラン 2 件（`差分規模の宣言` / `fmt / clippy / test`）も一発 success
だった）。

## この記録が数える件数

**`- 分類:` の行は 5 本**（`count.sh` が数えるのはこの行）。すべて**自己修正**で、
レビュー由来と CI 由来は 0 件。

分類ごとでは `test-assert-weak` 2 / `plan` 1 / `rule-staleness` 1 / `comment-mismatch` 1。
**`対策済:` の行は置いていない**ので、`count.sh` の再発数の起点は動かない。

## ゴールと結果

Issue #33 のゴールは「`--explain` で判定に使ったシグナルの値をすべて出す」で、
出すものとして 4 項目（実測値 / 傾き / 適用された閾値 / 取れなかったシグナルと理由）が
挙がっていた。

**実装と 1 つずつ突き合わせたところ、出ていなかったのは 2 つだけだった。**

| Issue が挙げた項目 | 着手前の状態 |
|---|---|
| 各シグナルの実測値 | 5 シグナルとも出ていた |
| 各シグナルがどちらへ傾けたか | 出ていた |
| 適用された閾値 | **構造類似度の行だけ**だった |
| 取れなかったシグナルと理由 | 出ていた（`SemanticsUnavailable::NotAsked` だけ行ごと落としていた） |

そのうえで **`--explain` は `src/cli.rs` が受け取るだけで `src/main.rs` が使っておらず、
指定しても出力が 1 文字も変わらなかった**（Issue #99 が挙げている状態）。

達成したのは 3 つ。(1) `--explain` を `src/main.rs` へ配線した。(2) `--explain` のときだけ
各シグナルに当てた閾値を値の隣に併記するようにした。(3) `--explain` のときだけ
「LSP へ尋ねなかった」シグナルの行を理由付きで出すようにした。

閾値は `classification::reason::Reason` が運ぶ形にし、`report` が定数を読みに行かないように
した。`classification` 側には `AppliedThresholds` を 1 つ置き、**傾きを出す側（`Leans::of`）と
根拠に載せる側（`reasons_of`）が同じ値を読む**ようにしてある。

**ずれ**: 無し。Issue が挙げた 4 項目はすべて出るようになった。ただし Issue の実測値の
一覧にある**呼び出し先の重なり**は、まだシグナルになっていない（#220 のゴール）ので
`--explain` に載せる行が存在しない。スコープ外にした理由は着手時の計画コメントに残した。

**#99 の「決めること」に答えを出した**: 既定の出力は削らない。今の出力を「`--explain` 相当」
と見なして既定を削ると、Phase 0 から読めていた根拠が減る（#99 自身が懸念として挙げている形）。
`--explain` は「理由を見せる」ではなく「**閾値との突き合わせまで見せる**」に振った。
#99 にもその旨をコメントした。

## 指摘

**レビュー由来は 0 件。** `copilot-pull-request-reviewer` の 1 巡だけで
`**Findings:** None` / 🔵 Needs a closer look（「判定の中核と公開関数のシグネチャを変える
ので最終的な人のレビューが要る」という理由で、欠陥の指摘は 0 件）。レビュースレッドは 0 本。
**CI 由来も 0 件**（チェックラン 3 件 — `差分規模の宣言` / `fmt / clippy / test` /
`copilot-pull-request-reviewer` — がいずれも一発で success）。

以下の 5 件はすべて**自己修正**で、push 前に直している。

### 1. モジュール距離のテストで、測った段数と当てる閾値が同じ入力を選びかけた

- 分類: `test-assert-weak`
- 出どころ: 自己修正
- 内容: `--explain` が閾値を併記することを確かめるテストを、既存のヘルパー
  `separate_directories()`（2 段）で書こうとした。`SEPARATE_DIRECTORY_STEPS` も **2** なので、
  `モジュール距離 2 段 (閾値 2 段)` は**測った段数をもう一度出す実装でも通る**。
  1 段になるヘルパー（`nested_directories()`）を足して `モジュール距離 1 段 (閾値 2 段)` を
  期待値にした。assert を書いている途中で気づいたので、誤ったテストは commit していない。
- 既存ルール: `rules/testing.md`「assert は『落ちうるか』で見る」の
  「既定値と違う答えになる入力を選ぶ」
- 次にどう防ぐか: 今回は記録のみ。既存ルールが射程に入れており、そのルールの文
  （「規則で決まる答え」と「既定値」が同じ入力を使わない）をそのまま当てて気づけている。

### 2. 3 つの閾値がすべて 0.5 で、どの定数を当てたかをテストが区別できていなかった

- 分類: `test-assert-weak`
- 出どころ: 自己修正
- 内容: `DEFAULT_STRUCTURAL_SIMILARITY_THRESHOLD` / `SHARED_IMPORTS_THRESHOLD` /
  `SHARED_CALLER_DOMAINS_THRESHOLD` は**今どれも 0.5**。既定値のまま `--explain` の出力を
  見るテストだけでは、**構造類似度の閾値をすべてのシグナルへ流用する実装でも通る**。
  指摘 1 と同じ形だが、こちらは**テストを 6 件書き終えてから**気づいた。
  `--threshold 0.8` を渡すテストを 1 件足し（3 本目のコミット）、
  `構造類似度: 0.94 (閾値 0.8)` と `依存先の重なり 0.00 (閾値 0.5)` が同時に出ることを固定した。
- 既存ルール: 指摘 1 と同じ（`rules/testing.md`「既定値と違う答えになる入力を選ぶ」）
- 次にどう防ぐか: 今回は記録のみ。**残る 2 つ（依存先の重なりと呼び出し元ドメインの重なり）は
  外から動かす手立てが無いので、互いの取り違えはこのテストでも落ちない**ことを PR 本文と
  コミット本文に明記した。閾値の外出し（#35）が入れば同じ形で分けられる。

### 3. 計画では `reasons_of` が定数を直接書くつもりだった

- 分類: `plan`
- 出どころ: 自己修正
- 内容: Issue に投稿した計画は「閾値を `Reason` が運ぶ」までしか決めておらず、
  `reasons_of` が `SHARED_IMPORTS_THRESHOLD` 等を直接書く形を想定していた。実装に入ると、
  それだと**傾きを出す側（`*_lean_of`）と根拠に載せる側（`reasons_of`）が別々に定数を読む**
  ので、`classification` の中で同じ取り違えが起きうることが分かった。定数をどのシグナルに
  当てるかを決める場所を `AppliedThresholds::of` の 1 箇所に寄せ、両方がそこから受け取る形にした。
- 既存ルール: `rules/architecture.md`「判定は 1 箇所にだけ置く」（計画はこの規約を
  `report` と `classification` の境界にだけ当てており、`classification` の内側に当て損ねていた）
- 次にどう防ぐか: 今回は記録のみ。計画の段階で 2 箇所目を見落としたが、実装中に気づいて
  同じ PR で閉じている。**Issue のコメントに「計画に無かった判断」として追記済み。**

### 4. `rules/naming.md` の `reason` の定義が、同じ PR の変更で古くなった

- 分類: `rule-staleness`
- 出どころ: 自己修正
- 内容: 語彙表は `reason` を「シグナルの値と、それが傾けた向きの組」と定義していたが、
  この PR で `Reason` に**当てた閾値**を持たせたため、**規約が実装より狭い定義を説明したまま**に
  なった。`explanation` の行を足すついでに気づき、`reason` の行も同じ PR で出し直した。
- 既存ルール: `AGENTS.md`「実装を変えたら、rules/ 配下の記述も同じ PR で出し直す」
- 次にどう防ぐか: 今回は記録のみ。**規約が想定しているとおりの形（同じ PR の中で実装が
  変わり、`rules/` の記述が置き去りになる）で、規約に従って同じ PR のうちに直せている。**
  見つけ方は「新しい語を語彙表へ足すときに、隣の行も読み直す」だった。

### 5. `src/report.rs` の module doc が、同じ定義を引用したまま古くなった

- 分類: `comment-mismatch`
- 出どころ: 自己修正
- 内容: 指摘 4 を直した直後に、同じ文言（「シグナルの値と、それが傾けた向き」）を
  `grep -rn` でリポジトリ全体から引いたところ、`src/report.rs` の module doc が
  **`rules/naming.md` の定義をそのまま引用していて**同じく古くなっていた。同じ PR で直した。
- 既存ルール: `rules/coding.md`「コメントは doc と Why / Why not に絞る」（内容が実装と食い違う）
- 次にどう防ぐか: 今回は記録のみ。**規約の文言を引用したコメントは、規約を直したときに
  一緒に古くなる**という形。指摘 4 を直した流れで grep をかけたので見つかったが、
  **grep をかけるところまでは規約に書かれていない**。同じ形がもう 1 度出たら、
  「`rules/` の記述を直したら、同じ文言をリポジトリ全体から引く」をルールへ足す候補になる。

## 手戻り

- **`report` のテストに書いた Rust の文字列継続（`\` + 改行）が、ファイルへ書き出す手順の
  都合でリテラルの空白に潰れ、`test_explained_text_of_reports_the_signals_that_were_not_asked`
  が 1 度落ちた。** 継続の書き方を直して green。実装側の問題ではない。
- **`classification` のテスト 10 箇所へ期待値のフィールドを機械的に足したとき、1 箇所
  取りこぼした。** 入れ子の `}`（`CallerDomainOverlap::Unavailable { reason: .. }`）で
  スクリプトの状態が戻ってしまう形だった。コンパイルエラー（`missing field threshold`）で
  分かったので手で足した。
- **`AppliedThresholds` を計画に無いまま足した**（指摘 3）。`classification` の中で閉じており、
  公開 API は変わらないので分けなかった。
- **実装中に実行環境の作業ディレクトリが一度 `/home/user` へ移った**（コンテナ側の更新）。
  コミットは push 済みで、成果物への影響は無い。

## うまくいったこと

- **計画を書く前に、Issue の「何を出すか」4 項目を実装と 1 つずつ突き合わせた**
  （`AGENTS.md`「実装を始める前に」の「計画の入力を鵜呑みにしない」）。
  結果、**4 項目のうち 2 項目は既に出ている**ことが分かり、「やること」を実際の差分に絞れた。
  Issue の一覧をそのまま計画にしていたら、既に出ているものを作り直すところだった。
- **`--explain` に関係する既存 Issue を先に洗った。** #99（`--fail-on` / `--explain` の配線）が
  同じ `--explain` を扱っており、しかも**この PR が答えを出すべき問い**（既定の出力をどこまで
  削るか）を「決めること」として持っていた。#99 にもその答えと、残る `--fail-on` の分を
  コメントで残した。
- **`echo hook-canary` が通ったことを「フック不発環境」と断定しなかった。**
  `.claude/settings.json` を読んで `PreToolUse` が配線されていない（`SessionStart` だけ）ことを
  確かめ、「canary が無いので層 3 を判定できなかった」と PR 本文・通知・Issue へ書いた
  （`AGENTS.md`「実装を始める前に」の 3 つ目）。
- **push 前の検査を 1 つずつ独立に実行し、それぞれの結果行を確認した。**
  `cargo fmt --check` / `cargo clippy --all-targets -- -D warnings` / `cargo test`。
- **差分規模の宣言を `harness/ci/pr-body-diff.sh` にローカルで通してから PR を作った。**
  CI の同じ検査も一発で success。
- **実際に CLI を `--explain` 有り / 無しで走らせ、その出力を PR 本文に貼った。**
  テストが通ることと、利用者が見る出力が変わることは別なので、両方確かめた。

## 規約への反映

**無し。** この回の指摘 5 件のうち 4 件（1 / 2 / 3 / 4）は既存の規約が射程に入れており
（`rules/testing.md`「既定値と違う答えになる入力を選ぶ」・`rules/architecture.md`
「判定は 1 箇所にだけ置く」・`AGENTS.md`「実装を変えたら、rules/ 配下の記述も同じ PR で
出し直す」）、**規約の抜けは出ていない**。5 件ともレビュー・CI ではなく push 前の自己修正で、
レビュー由来 0 件・CI 由来 0 件だった。

指摘 5 だけは「**規約の文言を引用したコメントが、規約を直したときに一緒に古くなる**」形で、
既存の `comment-mismatch` が指す範囲ではあるものの、**見つけ方（`rules/` を直したら同じ文言を
リポジトリ全体から引く）が規約に書かれていない**。ただし**この形は今回が 1 件目**なので、
`AGENTS.md`「規約の更新」の「2 回以上出たら規約の抜けとして扱う」に従って今回は記録だけにする。

集計（`count.sh`）と、規約・フックへの介入判断はこの回では行っていない
（`harness/records/README.md`「記録を数えて規約やフックへ手を入れるのは、別の機会に行う」）。
