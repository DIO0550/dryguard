# PR #203 「尋ねていない」の理由を 2 つに分け、案内が嘘になる側を言い切らない

- マージ日: 2026-09-14
- 関連 Issue: #188（切った Issue: #202 / #204）
- 差分規模: 10 ファイル / +371 -57

**この記録は PR #201 の回の指摘も併記する**（`harness/records/README.md`「記録だけの PR には
記録を作らない」— PR #201 は `harness/records/pr-199.md` だけを変える PR で、この PR #203 が
その次に実装が入った PR に当たる）。

## この記録が数える件数

**`- 分類:` の行は 13 本**（`count.sh` が数えるのはこの行）。

| 回 | 件数 | 内訳 |
|---|---|---|
| PR #203 | **8** | レビュー 4（P1 3 / P2 1）+ 自己修正 4 |
| PR #201 | **5** | レビュー 5（すべて P2） |

分類ごとでは `unverified-claim` 8 / `なし` 3 / `comment-mismatch` 1 / `naming-vocabulary` 1。
**`対策済:` の行は置いていない**ので、`count.sh` の再発数の起点は動かない。

## ゴールと結果

**#188 のゴールは「集める場所の一覧に漏れがあると、書かれた型名が『尋ねていない』に落ちる」への対処を
決めることで、Issue の推しは「案 3 を先に、案 2 をその後」だった。案 3 を採り、案 2 は #202 へ分けた。**

| ゴール | 結果 |
|---|---|
| 案 3（理由を分け、案内を正しくする） | **採用。** `UntracedReason` を 2 つに分けた |
| 案 2（名前付きチャンクでも代入先の注釈を集める） | **#202 へ分離。** 既存の判断とそれを固定するテストを変えるので `AGENTS.md`「設計判断の確認」に当たる |
| 案 1（漏れを 1 つずつ足す） | Issue 本文の判断どおり、単独では採らない |
| `rules/` 側の記述 | 同じ PR で出し直した（`9783225`） |

### 現象は実サーバで再現してから着手した

`typescript-language-server 6.0.0` / `typescript 5.9.3` で、`main`（`190e6c7`）に対して Issue の形を再現。

```
型シグネチャ: 測れない (比較に残る型名がソースに書かれていない: 型注釈を書くと辿れる)
```

`Input` は代入先の注釈に**書かれている**のに、この文が出ていた。

### 落ちる枝が 2 つあり、言い切れることが違った

`single_outcome_of` でこの枝へ倒れる条件は 2 つで、**片方は確かめてあり、もう片方は確かめられていない**。

| 枝 | 条件 | 言い切れること |
|---|---|---|
| A | `value_type == Omitted` かつ値の型の位置に型名が残る | 注釈が無いことを構文木から確かめてある |
| B | 比較に残る綴りの型名に辿った記録が無い | 書かれていないのか、集め損ねたのか区別できない |

`||` で 1 つに畳んでいたので、**B でも A の案内が出ていた**。分けたのはこの 2 つ。

### バリアントの振り分けは変えていない

同じ条件式を 2 つの return へ割っただけなので、`Normalized` / `UntracedTypeName` /
`UnopenedTypeName` のどれに落ちるかは変わらない。**そのため実コーパス（rxjs）の内訳は測っていない。**
変わるのは出力に出る文だけ。

**その文は `--explain` に限らない。** head（`17983d5`）で `report_compare` / `report_scan` は
どちらも常に `report::text_of` を描画しており、`CommonOptions::explain` は
**`src/cli.rs` のパース用テスト以外から読まれていない**（`grep -n explain src/`）。
`--explain` を付けずに `compare` を走らせても同じ行が出ることを実測で確かめた。
**つまり通常の `compare` / `scan` の出力が変わる。**

## 指摘（この PR の回）

**8 件**（レビュースレッド 4 本 = 4 件 + 自己修正 4 件）。レビューはすべて Codex で **4 巡**
（`9783225` 1 件 / `b90e712` 1 件 / `6619e05` 2 件 / `99350b0` **0 件**）。**P1 が 3 本、P2 が 1 本。**

CI は**この branch の Rust ワークフロー 4 回すべて `success`**（run 412 / 414 / 415 / 416）で、
**CI 由来の指摘は無い**。

### 1. ペアの両側が別の理由でも、or パターンが左側を掴んでいた

- 分類: `なし`
- 出どころ: レビュー（`9783225`・P2・`#discussion_r3999951035`）
- 内容: `(UntracedTypeName { reason }, _) | (_, UntracedTypeName { reason })` は両側が当たると
  **常に左側の `reason` を掴む**。片方が `OmittedValueTypeAnnotation`、もう片方が `NoTracedRecord` の
  とき、`compare` の引数を入れ替えるだけで出力の案内が変わっていた（`scan` では候補ペアの
  並び順で変わる）。倒れる向きが悪く、**確かめてある理由が確かめられていない理由で覆われる**ので、
  この PR が分けたばかりの区別が並び順で消える。`b90e712` で確かめてある理由を先に掴む枝を足した
- 既存ルール: なし。**「ペアの両側から 1 つを選ぶとき、受け取った順に任せない」に当たる規約が無い。**
  `rules/coding.md`「列挙で判定を組むときは、漏れの倒れる向きを選ぶ」は**一覧に無い形**が対象で、
  ここは一覧に在る 2 つの間の優先順位なので射程が違う。
  `rules/architecture.md`「取れなかったシグナルを既定値で埋めない」の「理由は落とさない」は
  理由を畳まないことが対象で、**入れ替わり**は見ていない
- 次にどう防ぐか: 今回は記録のみ（規約への反映は棚卸しの管轄）

### 2. ペアから理由を選ぶ並びを `semantics` に置いていた

- 分類: `なし`
- 出どころ: レビュー（`b90e712`・P1・`#discussion_r3999976393`）
- 内容: 指摘 1 の修正で足した `reported_untraced_reason_of` を `semantics::type_signature` に置いた。
  `semantics` の責務は「LSP への問い合わせと応答の正規化」で**片側ずつの outcome を作るところまで**
  なので、ペアの両側から 1 つを選ぶ並びを置くと段の責務を越える。`6619e05` で `pipeline` へ移した
- 既存ルール: rules/architecture.md「3 ステージのパイプライン」の責務の表
  （`semantics` は応答の正規化まで）。**規約は在る。**
- 次にどう防ぐか: 今回は記録のみ。**分類を `なし` にしたのは、規約が無いからではなく
  この責務の表に紐づくタグが語彙に無いから**（下の「分類の語彙の穴」）

### 3. 移した先（`pipeline`）も責務の表と合っていなかった

- 分類: `なし`
- 出どころ: レビュー（`6619e05`・P1・`#discussion_r3999999425`）
- 内容: **指摘 2 と同じ形が 2 件目。** `pipeline` の責務は「ステージを呼ぶ順序」で、
  「シグナルの統合と判定・理由の組み立て」は `classification` の持ち物。`99350b0` で
  **関数を置く場所を探すのをやめ、優先順位を `type_signature_match_of` の枝のパターンで書いた**
  （`SiteDependentSpelling` / `UnopenedTypeName` の枝と同じ形。指摘 1 が求めていた
  "an explicit precedence just like the surrounding outcome branches" そのもの）。
  これでこの PR が足す「ペアから 1 つを選ぶ関数」は 0 件になった。
  **残る点（`type_signature_match_of` 自身の置き場所）はこの PR より前からの構造**で、
  `git grep` で数えると同じ族が `pipeline` に 9 件ある（`signals_of` /
  `structural_similarity_of` / `import_overlap_of` / `type_signature_match_of` /
  `unifiable_match_of` / `caller_domain_overlap_of` / `asked_type_signature_match_of` /
  `asked_caller_domain_overlap_of` / `signals_with_semantics_of`）。1 つだけ移すと族が割れるので
  **#204 へ分けて判断を仰ぐ形にした**（ステージをまたぐ移動 = `AGENTS.md`「設計判断の確認」）
- 既存ルール: rules/architecture.md「3 ステージのパイプライン」の責務の表
  （`pipeline` はステージを呼ぶ順序まで / `classification` がシグナルの統合）。**規約は在る。**
- 次にどう防ぐか: 今回は記録のみ。**未決の判断は #204 が持つ**

#### 分類の語彙の穴（指摘 2 / 3 に共通）

**規約は在るのに、それに紐づく分類タグが語彙に無い。** `TEMPLATE.md` の表で
`rules/architecture.md` に紐づくのは `layer-dependency`（依存方向のルール）・
`verdict-placement`（判定は 1 箇所にだけ置く）・`missing-signal`（取れなかったシグナル）・
`module-api`（モジュールの公開 API）・`query-granularity`（ファイル単位の問い合わせ）で、
**「3 ステージのパイプライン」の責務の表に対応するタグが無い**。

依存の向きは壊れていない（置き場所が表と合っていない）ので、`layer-dependency` へ寄せると
向きが壊れた指摘と置き場所の指摘が 1 つのタグに畳まれる。`TEMPLATE.md`「どれにも当てはまらないなら
`なし` と書く（それが規約の抜けの候補になる）」に従って `なし` にしたが、
**ここで言う「抜け」は分類の語彙の抜けであって、規約の抜けではない。**
タグを足すかどうかは棚卸しの回の判断なので、材料として残すところまでにする。

### 4. `NoTracedRecord` を足したのに、隣のコメントが「注釈を書けば辿れる」のままだった

- 分類: `comment-mismatch`
- 出どころ: レビュー（`6619e05`・P1・`#discussion_r3999999428`）
- 内容: `semantics::type_signature` の `SiteDependentSpelling` rustdoc と `single_outcome_of` の
  優先順位のコメントが、**すべての「尋ねていない型名」は注釈を書けば直ると言い切ったまま**だった。
  `NoTracedRecord` は**注釈が書かれているのに集め損ねた**形を含むので、どちらも嘘になる。
  `99350b0` で直した（指摘の 2 箇所に加え、`classification::signal` の同じ対比も揃えたので 3 箇所）
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」（内容が実装・事実と食い違う）
- 次にどう防ぐか: 今回は記録のみ。**`rules/` 側の同じ対比は 1 巡目（`9783225`）で直していたのに、
  コード内コメントの同じ食い違いを取りこぼした。** `AGENTS.md`「実装を変えたら、rules/ 配下の
  記述も同じ PR で出し直す」は `rules/*.md` が射程で、コード内コメントは `comment-mismatch` の側

### 5. `b90e712` のコミットメッセージのレビュー URL を、一次情報に当たらず書いた

- 分類: `unverified-claim`
- 出どころ: 自己修正
- 内容: 指摘 1 に対応するコミットの「参考」節に `#discussion_r<番号>` の URL を**確かめずに置いた**。
  `git commit` の直後に気づき、`pull_request_read` で実際のコメント ID（`r3999951035`）を
  取ってから `--amend` で直したので**リモートには出ていない**
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」（コミットメッセージも射程）
- 次にどう防ぐか: 今回は記録のみ

### 6. `6619e05` のコミットメッセージのレビュー URL を、一次情報に当たらず書いた

- 分類: `unverified-claim`
- 出どころ: 自己修正
- 内容: 指摘 5 と同じ形の 2 件目。指摘 2 に対応するコミットで同じことをした。
  正しい ID は `r3999976393`。**指摘 5 で一度直しているのに、次のコミットで繰り返している**
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 7. `99350b0` のコミットメッセージのレビュー URL を、一次情報に当たらず書いた

- 分類: `unverified-claim`
- 出どころ: 自己修正
- 内容: 指摘 5 / 6 と同じ形の 3 件目。指摘 3 / 4 に対応するコミットで、URL を 2 つとも
  確かめずに置いた。正しい ID は `r3999999425` / `r3999999428`。
  **3 回続けて同じ手順で同じことをしている**ので、1 件にまとめず別々に数える
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ。**手順としては「指摘に対応するコミットを書く前に、
  そのスレッドの ID を取る」で潰せる形**

### 8. 既にある語で言えるものを語彙表へ足しかけた

- 分類: `naming-vocabulary`
- 出どころ: 自己修正（`9783225` を書く過程。commit には残っていない）
- 内容: `rules/naming.md` の語彙表に `omitted value type annotation` / `no traced record` の
  2 行を足したが、**どちらも既にある語（`value type annotation` / `traced type name`）で言える**ので
  同じ作業のうちに外し、`untraced type name` の内訳として書き直した
- 既存ルール: rules/naming.md「語を増やすときは、既にある語で言えないかを先に確かめる」
- 次にどう防ぐか: 今回は記録のみ

## 指摘（PR #201 の回・記録だけの PR なので併記）

**5 件。** PR #201（`4a4bf29`）は `harness/records/pr-199.md` だけを変える PR で、
`harness/records/README.md`「記録だけの PR には記録を作らない」に当たる。
レビューは Codex の 1 巡（`12a98a9`）で **P2 が 5 本**、**5 件とも記録の正確さに関するもの**。
すべて `6587440` で直っている。

### 9. 測ったのは「取り戻した答えが 0 件」なのに「その形が 1 件も現れなかった」と書いた

- 分類: `unverified-claim`
- 出どころ: レビュー（PR #201・`12a98a9`・P2・`#discussion_r3999936955`）
- 内容: `pr-199.md` の「手戻り」が「実コーパスにこの形は 1 件も現れなかった」と書いていたが、
  測っていたのは**その経路が取り戻した `Normalized` の数（0 件）**。**構文としての出現数は
  数えていない**。同じ記録の指摘 12（1 チャンクが `UntracedTypeName` → `UnopenedTypeName` へ
  動いた）とも食い違っていた
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 10. 「14 件すべて再現を取った」が、実際に確かめた範囲より広かった

- 分類: `unverified-claim`
- 出どころ: レビュー（PR #201・`12a98a9`・P2・`#discussion_r3999936963`）
- 内容: `pr-199.md` の「うまくいったこと」が「14 件すべて `compare` / tree-sitter / `tsc` で
  再現を取った」と書いていたが、指摘 3 / 4 / 5 は**ソースを読んで確かめる種類**。
  数え直すと (a) 再現を取った 10 件 / (b) ソースと履歴を読んだ 3 件 / (c) 再現を取らなかった 1 件で、
  **(c) は指摘されていなかったのに数え直しで出てきた**
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 11. 「足したテストごとに落ちる」が、コミットメッセージと食い違っていた

- 分類: `unverified-claim`
- 出どころ: レビュー（PR #201・`12a98a9`・P2・`#discussion_r3999936966`）
- 内容: `d49ece8` は 3 件足して落ちるのは 1 件、`71c3a99` は 2 件足して落ちるのは 1 件目だけ。
  落ちない残りは `rules/testing.md`「「出ない」を確かめるときは、対照を 1 件置く」が求める**対照**で、
  「すべて落ちるべきだ」と読める書き方のほうが規約とぶつかっていた
- 既存ルール: AGENTS.md「記録・PR 本文に数値や主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ

### 12. 既にある分類（`over-guard`）を確かめずに `なし` と書いた

- 分類: `unverified-claim`
- 出どころ: レビュー（PR #201・`12a98a9`・P2・`#discussion_r3999936971`）
- 内容: `pr-199.md` の指摘 6 / 7 / 9 は「この PR の前は取れていた答えを落とした」形で、
  `over-guard` が既に `pr-158.md` 指摘 10・`pr-191.md` 指摘 3 で同じ使われ方をしていた。
  過去の記録に当たらずに `なし` と書いたので、**`count.sh` が確立済みの分類を過少に数える**
  状態になっていた。訂正では同じ基準を当てて指摘 13 も移し、4 件にしている
- 既存ルール: `harness/records/README.md`「推測で埋めない」/ AGENTS.md「記録・PR 本文に数値や
  主張を書く前に」（分類も記録に書く主張）
- 次にどう防ぐか: 今回は記録のみ。**`なし` と書く前に、語彙表のタグが過去の記録でどう使われて
  いるかを読む**で潰せる形

### 13. 射程に入っていない先を `unverified-claim` として数えていた

- 分類: `unverified-claim`
- 出どころ: レビュー（PR #201・`12a98a9`・P2・`#discussion_r3999936976`）
- 内容: `pr-199.md` の指摘 16 が書いた先は **Issue の本文**（#200 の「参照」節）で、
  `AGENTS.md`「記録・PR 本文に数値や主張を書く前に」が名指ししている先
  （記録・PR 本文・Issue コメント・コミットメッセージ・レビュー返信・通知）に**Issue の本文は
  入っていない**。射程外を射程内として数えると、`count.sh` が測るのは「その規約が効いているか」で
  なくなる。`なし` へ直し、射程の穴として材料に残した
- 既存ルール: `harness/records/README.md`「推測で埋めない」/ AGENTS.md「記録・PR 本文に数値や
  主張を書く前に」
- 次にどう防ぐか: 今回は記録のみ。**規約が名指ししている先の一覧に当たってから分類する**で潰せる形

## 手戻り

**実装本体（理由を 2 つに分ける形）の手戻りは無し。** 最初に置いた 2 バリアントのまま最後まで通した。

**手戻ったのは「優先順位をどこに置くか」で、3 巡かかった。**

| 巡 | 置いた場所 | なぜ外れたか |
|---|---|---|
| 1 | `semantics::type_signature` の公開関数 | ペアの話を Stage 2 に置いた |
| 2 | `pipeline` の private 関数 | `pipeline` の責務は順序だけ |
| 3 | **枝のパターン**（関数を作らない） | — |

**3 巡目の形は、1 巡目の指摘が最初から求めていたもの**（"just like the surrounding outcome
branches"）。「順序の持ち場を 1 箇所に集める」を優先して関数にしたが、**周りの枝が既に
パターンで優先順位を書いていた**ので、合わせるだけでよかった。

## うまくいったこと

- **着手前に実サーバで現象を再現し、落ちている枝を特定してから計画を書いた。** Issue が挙げた形が
  2 つの枝のどちらに当たるかを先に確かめたので、対照（注釈を素直に省いた形）も同時に用意できた
- **assert が落ちうるかを、疑わしい 3 件とも実際に壊して確かめた**（`rules/testing.md`
  「迷ったら、その 1 件だけ実装を壊して走らせる」）。指摘 1 の回帰テストは、枝を外すと
  `left: NoTracedRecord` / `right: OmittedValueTypeAnnotation` で落ちる。
  **`99350b0` で形を畳んだ後に 3 件とも measure し直している**（前の形での結果を持ち越していない）
- **PR 本文を push のたびに出し直した。** テストの表・件数・手戻りの節が、最後の push の内容と
  ずれないようにした（`AGENTS.md`「関連する変更が入った後・最終提出の前にも」）
- **判断を仰ぐ 2 件（#202 / #204）を、選択肢と根拠を書いた Issue にして分けた。**
  PR のスレッドに「やらない」とだけ書いて終わらせていない

## 規約への反映

**無し。**

この PR は `rules/architecture.md` と `rules/naming.md` を直しているが、それは
**#188 が示した形で既に無効になっていた記述を出し直したもの**（計画に含まれる作業）で、
再発を数えた結果の介入ではない。

**回数を数えて層を決めるのは棚卸しの管轄**なので、ここでは分類を付けて記録するに留める。
`対策済:` の行は置いていないので、`count.sh` の再発数の起点は動かない。
未決の判断は #202 / #204 が持つ。

## 実行環境のメモ

- `typescript-language-server 6.0.0` / `typescript 5.9.3`（`pnpm install --frozen-lockfile` で導入）。
  実サーバのテスト（`cargo test -- --ignored`）を**このセッションで実際に走らせている**
- **フックの発火は判定できなかった。** `echo hook-canary` は deny されずに通ったが、
  `.claude/settings.json` の `hooks` に配線されているのは `SessionStart` だけで **`PreToolUse` は無い**
  （`.claude/hooks/` にあるのも `wire-githooks.sh` の 1 つ）。
  したがって**canary が無いので層 3 を判定できなかった**（「フックが発火しない環境」とは断定しない）。
  層 2 は生きている（`git config core.hooksPath` が `harness/githooks` を返す）
